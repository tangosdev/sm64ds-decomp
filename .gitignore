# --- Copyrighted material: NEVER commit ---
# The ROM and anything extracted from it stays local. The repo holds only
# our own original work (scripts, hand-written C, notes).
*.nds
*.bin
*.sav
baserom*
extracted/
build/

# --- Build artifacts ---
*.o

# --- Ghidra scaffolds (derived from ROM; local only) ---
ghidra_out/
ghidra_targets.txt

# --- Tooling / OS noise ---
__pycache__/
*.pyc
.venv/
venv/
.idea/
.vscode/
.vs/
Thumbs.db
desktop.ini

# third-party binaries (not our code)
tools/bin/

# proprietary, non-redistributable toolchain
tools/mwccarm/
license.dat

# regenerable reference disassembly (derived from ROM)
asm/

mwccarm.zip

# agent scratch (matched C gets promoted to src/)
match/auto/

# external reference repos (not ours; do not commit)
reference/

# --- Internal working files: kept local, not published ---
knowledge/
match/
progress/
notes/matching-log.md
notes/recon-2026-06-13.md
notes/overnight-plan.md
notes/ac-decomp-comparison.md

# third-party tools cloned for evaluation/integration (not part of this repo)
vendor/

# local-only, never commit
tools/claims_key.txt
tools/claims_handle.txt
/scratchpad/

# per-session runtime scratch (terminal logs, temp files, local MCP setup)
/temp/
/terminals/
/mcps/

# coddog fan-out scratch (agents write candidates here)
_abwork/

# glm fan-out scratch (agents write candidates here)
_glmwork/

# AI-session draft area (Cursor/Grok and friends): unverified candidates live
# here, never in src/ - src/ is oracle-verified matches only
/scratch/
.agents/
.codex/
