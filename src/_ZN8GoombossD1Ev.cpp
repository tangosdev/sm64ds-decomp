//cpp
// @symbol _ZN8GoombossD1Ev
/* recovered: real C++ destructor -- the compiler emits the whole body
 *
 * Three array cleanups and five member destructors, in reverse declaration
 * order out of Goomboss.h. Every element type was already named in the tree:
 * MovingCylinderClsnWithPos[4] at 0x110, ShadowModel[3] at 0x274 and
 * Vector3[3] at 0x3ac, with 0x40, 0x28 and 0xc as their sizes.
 */
#include "Goomboss.h"

Goomboss::~Goomboss()
{
}
