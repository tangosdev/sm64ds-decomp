// @symbol daObjSlIceBlock_c_classInit_SL_ICEBLOCK
/* recovered: globals resolved, declarations from a shared header */
#include "decl_ActorBase.h"
#include "decl_Platform.h"
#include "decl_common.h"
/* recovered: globals resolved */
/* resolved: VT = _ZTV10SlidingIce */
/* Reconstructed source-style name: SM64DS proves daObjSlIceBlock_c through RTTI,
 * allocation size, vtable identity, and the SL_ICEBLOCK registry profile;
 * later EAD lineage supplies classInit. Exact original spelling is not
 * preserved. Historical alias: SlidingIceSpawner_Spawn. */
int *daObjSlIceBlock_c_classInit_SL_ICEBLOCK(void)
{
    int *p = (int *)_ZN7fBase_cnwEj(812);
    if (p) { _ZN10dBgActor_cC2Ev(p); p[0] = (int)_ZTV10SlidingIce; }
    return p;
}
