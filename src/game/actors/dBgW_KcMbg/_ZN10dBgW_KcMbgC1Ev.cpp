//cpp
// @symbol _ZN10dBgW_KcMbgC1Ev
/* recovered: real C++ constructor -- the whole body is synthesized
 *
 * An empty out-of-line definition makes mwcc emit exactly the ROM pair:
 * `bl _ZN7dBgW_KcC2Ev', then the dBgW_KcMbg vptr store. Nothing else
 * happens in this constructor. Same shape as dCapEnemy_c (the tenth
 * landing); the emitted C2 sibling is stripped by objisolate and its ROM
 * body is enrolled separately as src/_ZN10dBgW_KcMbgC2Ev.c (section 1).
 */
#include "dBgW_KcMbg.h"

dBgW_KcMbg::dBgW_KcMbg() {}
