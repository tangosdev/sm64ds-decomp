// @symbol daObjBlockL_c_classInit_BK_FUTA
/* recovered: globals resolved, declarations from a shared header */
#include "decl_ActorBase.h"
#include "decl_Platform.h"
#include "decl_common.h"
/* recovered: globals resolved */
/* resolved: VT = _ZTV13BigBrickBlock */
/* Reconstructed source-style name: SM64DS proves daObjBlockL_c through RTTI,
 * allocation size, vtable identity, and the BK_FUTA registry profile;
 * later EAD lineage supplies classInit. Exact original spelling is not
 * preserved. Historical alias: FortressTowerWall_Spawn. */
int *daObjBlockL_c_classInit_BK_FUTA(void)
{
    int *p = (int *)_ZN7fBase_cnwEj(816);
    if (p) { _ZN10dBgActor_cC2Ev(p); p[0] = (int)_ZTV13BigBrickBlock; }
    return p;
}
