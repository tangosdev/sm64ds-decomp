//cpp
// @symbol _ZN15daObjPathLift_cD1Ev
/* D1, the complete-object destructor. The whole body is the compiler's:
 * PathLift's cleanup inlined (its destructor is inline in the class, on
 * purpose), then the members and bases in reverse declaration order. */
#include "daObjPathLift_c.h"

daObjPathLift_c::~daObjPathLift_c()
{
}
