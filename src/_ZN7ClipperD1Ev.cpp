//cpp
// @symbol _ZN7ClipperD1Ev
/* The compiler emits the complete destructor: Clipper has no base or
 * non-trivial members, so the ROM body is precisely the vptr restoration. */
#include "Clipper.h"

Clipper::~Clipper()
{
}
