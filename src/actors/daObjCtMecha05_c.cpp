//cpp
/* Translation unit, reconstructed and promoted -- generated by tools/tubuild.py create.
 * ov065/daObjCtMecha05_c  (9 function(s))
 *
 * CANONICAL. The ROM build compiles this file and takes every function below
 * from this one object; the per-function sources it replaced are gone. The
 * cartridge's own build emitted this class's members from one translation unit,
 * and reconstructing that unit is what lets the compiler place the destructor
 * variants itself instead of each one being hand-placed under a `// @symbol`
 * marker.
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x0211ab60  src/_ZN16daObjCtMecha05_cD1Ev.cpp
 *   [1] 0x0211abac  src/_ZN16daObjCtMecha05_cD0Ev.cpp
 *   [2] 0x0211ac0c  src/func_ov065_0211ac0c.c
 *   [3] 0x0211ad04  src/_ZN16daObjCtMecha05_c16CleanupResourcesEv.cpp
 *   [4] 0x0211ad48  src/_ZN16daObjCtMecha05_c6RenderEv.cpp
 *   [5] 0x0211ad70  src/func_ov065_0211ad70.c
 *   [6] 0x0211ae08  src/_ZN16daObjCtMecha05_c8BehaviorEv.cpp
 *   [7] 0x0211b1d4  src/_ZN16daObjCtMecha05_c13InitResourcesEv.cpp
 *   [8] 0x0211b328  src/TTC_MovingBar_Spawn.c
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "daObjCtMecha05_c.h"
#include "common.h"
#include "decl_common.h"
#include "SharedFilePtr.h"
#include "dBgW.h"
#include "decl_ActorBase.h"
#include "decl_Platform.h"
#include "decl_ShadowModel.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow struct 'Base' */
struct Base { virtual void v0(); virtual void v1(); virtual void v2(); virtual void v3(); virtual void v4(); virtual void m(int); };

/* shadow struct 'Derived' */
struct Derived { char pad[0xd4]; Base base; };

/* shadow struct 'BMD_File' */
struct BMD_File; struct KCL_File; struct dActor_c; struct Vector3; struct Matrix4x3;

/* shadow struct 'CLPS_Block' */
struct CLPS_Block; struct SharedFilePtr;

/* shadow struct 'dBgCh_Gnd' */
struct dBgCh_Gnd {
    int pad[0x11];
    int result;       // offset 0x44
    int pad2[3];      // pad to 0x54 total
    dBgCh_Gnd();
    void SetObjAndPos(const Vector3 &v, dActor_c *a);
    int DetectClsn();
    ~dBgCh_Gnd();
};

/* shadow struct 'V3' */
struct V3 { int x, y, z; };

#define I32(off) (*(int *)(((int)c + (off))))
#define I8(off)  (*(u8 *)(((int)c + (off))))

extern "C" {
extern void Matrix4x3_FromRotationY(void* m, int angle);
extern void MulVec3Mat4x3(void* a, void* b, void* c);
extern void AddVec3(void* a, void* b, void* c);
extern void _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j( int self, int sm, int mat, int s1, int s2, int s3, unsigned int j);
extern int TTC_MovingBar_ModelFile[];
extern int TTC_MovingBar_ClsnFile[];
extern int RandomIntInternal(int* seed);
extern unsigned char data_0209f2c0;
/* data_ov065_0211c0c8 already declared `extern short` (scalar) by
 * include/decl_common.h:1683, pulled in above; NOT re-declared here as an
 * array (a redeclaration with a conflicting type is a hard error). Every use
 * below indexes through `&data_ov065_0211c0c8` instead of subscripting the
 * identifier directly, which works under either type. */
extern int data_0209e650;
extern signed char data_ov065_0211d26c[];
extern int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(void *self, int a, int b);
extern u16 DecIfAbove0_Short(u16 *p);
extern s16 data_02082214[];
extern void *_ZN9ModelBase7SetFileEP8BMD_Fileii(void *thisp, void *bmd, int a, int b);
extern void _ZN11ShadowModel10InitCuboidEv(void *thisp);
extern void _ZN10dBgActor_c21UpdateModelPosAndRotYEv(void *thisp);
extern void _ZN10dBgActor_c19UpdateClsnPosAndRotEv(void *thisp);
extern void _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
void *thisp, void *kcl, void *mtx, int fix, short sh, void *clps);
extern void *_ZN5Model8LoadFileER13SharedFilePtr(void *fp);
extern void *_ZN7dBgW_Kc8LoadFileER13SharedFilePtr(void *fp);
extern void _ZN4dBgW22UpdatePosWithTransformERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_();
extern void func_020393d4(void *p, void *v);
extern void func_020393a4(void *p, int v);
extern void func_02039394(void *p, int v);
extern "C" int data_ov035_02112258;
extern int _ZTV16daObjCtMecha05_c[];
/* TUBUILD CONFLICT -- alternate declaration of RandomIntInternal, from the legacy file for _ZN16daObjCtMecha05_c8BehaviorEv, NOT applied: extern int RandomIntInternal(int *seed); */
/* TUBUILD CONFLICT -- alternate declaration of data_0209f2c0, from the legacy file for _ZN16daObjCtMecha05_c8BehaviorEv, NOT applied: extern u8 data_0209f2c0; */
/* TUBUILD CONFLICT -- alternate declaration of data_0209f2c0, from the legacy file for _ZN16daObjCtMecha05_c13InitResourcesEv, NOT applied: extern "C" unsigned char data_0209f2c0; */
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 8 -- TTC_MovingBar_Spawn, 0x0211b328, size 0x38 */
/* -------------------------------------------------------------------------- */
// @symbol TTC_MovingBar_Spawn
/* recovered: vtable identified, declarations from a shared header */
/* recovered: vtable identified */
/* vtable identified: VT0 = _ZTV16daObjCtMecha05_c */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int *TTC_MovingBar_Spawn(void)
{
    int *p = (int *)_ZN7fBase_cnwEj(916);
    if (p) {
        _ZN10dBgActor_cC2Ev(p);
        /* This TU now DEFINES _ZTV16daObjCtMecha05_c (it anchors the vtable
         * via the out-of-line destructor below), so the vptr store must
         * skip the two hidden ABI slots with +2 -- see
         * notes/key-function-tu-vptr-store-blocker.md. */
        p[0] = (int)(_ZTV16daObjCtMecha05_c + 2);
        _ZN11ShadowModelC1Ev((char *)p + 0x33c);
    }
    return p;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN16daObjCtMecha05_c13InitResourcesEv, 0x0211b1d4, size 0x154 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16daObjCtMecha05_c13InitResourcesEv
// recovered name: daObjCtMecha05_c_InitResources
/* recovered: shared common types, renamed to Class_Method, declarations from a shared header */
/* recovered: shared common types, renamed to Class_Method */
/* daObjCtMecha05_c::InitResources - recovered from vtable slot identity.
 *
 * Every ROM symbol below is spelled by its already-mangled name -- an
 * ordinary (non-"C") declaration would mangle it a SECOND time and
 * eligible.py's reference check would flag the doubly-mangled name as
 * unresolvable. void* everywhere, mirroring the un-migrated
 * func_ov065_0211b1d4 this file replaces and daObjCtMecha03_c's own
 * InitResources, which took the same route rather than calling through the
 * real Model/ShadowModel/dBgW_KcMbg members now reachable via
 * daObjCtMecha05_c.h -- a class-typed by-value Fix12<int> argument is not
 * safe to assume matches the ROM through a real method call (see the
 * warning in include/dBgActor_c.h and include/dActor_c.h about CW homing
 * class-typed by-value parameters to the stack).
 *
 * ModelLoadFile, MeshColliderLoadFile and UpdatePosWithTransformSym WERE
 * phantom references -- names no module defines, which the byte gate cannot
 * see because match.py compares relocated words as wildcards. This branch read
 * the cartridge and established the real ROM symbols behind them, so the calls
 * below name those directly -- the same idiom daObjCtMecha03_c::InitResources
 * (its sibling in this class's own migration) and SignPost::InitResources
 * (#1554) already use. decl_common.h no longer declares the phantoms.
 *
 * The CLPS_Block symbol at 0x02112258 is the same kind of shared,
 * locally-unowned reference the daObjCtMecha03_c pass found at 0x02112198:
 * an "ambiguous" data symbol declared by twelve OTHER overlays' own
 * symbols.txt but not ov065's, and adding an ov065 entry for it would drag
 * this overlay's own computed base address, and every other ov065 symbol's
 * offset, out of range (tools/modules.py derives an overlay's base from its
 * lowest symbol).
 *
 * SETTLED to ov035 by tools/overlay_residency.py, the same as
 * daObjCtMecha03_c::InitResources's own site: of the twelve candidate
 * overlays (ov009/010/012/013/024/033/035/043/047/052/053/056), residency's
 * loader/level constraints leave exactly one able to coexist with ov065.
 * Spelled as ov035's own symbol name, data_ov035_02112258, so the
 * `complete` marker below is now warranted.
 */
int daObjCtMecha05_c::InitResources()
{
    char *self = (char *)this;

    void *mf = _ZN5Model8LoadFileER13SharedFilePtr(&data_ov065_0211d904);
    _ZN9ModelBase7SetFileEP8BMD_Fileii(self + 0xd4, mf, 1, -1);
    _ZN11ShadowModel10InitCuboidEv(self + 0x33c);
    _ZN10dBgActor_c21UpdateModelPosAndRotYEv(self);
    _ZN10dBgActor_c19UpdateClsnPosAndRotEv(self);
    void *kf = _ZN7dBgW_Kc8LoadFileER13SharedFilePtr(&data_ov065_0211d90c);
    _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
        self + 0x124, kf, self + 0x2ec, 0x199, *(short*)(self + 0x8e), (void*)&data_ov035_02112258);
    func_020393d4(self + 0x124,
        (void*)&_ZN4dBgW22UpdatePosWithTransformERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_);
    func_020393a4(self + 0x124, 0x1c0000);
    func_02039394(self + 0x124, 0x1000);
    *(short*)(self + 0x94) = *(short*)(self + 0x8e);
    *(int*)(self + 0x320) = *(int*)(self + 0x5c);
    *(int*)(self + 0x324) = *(int*)(self + 0x60);
    *(int*)(self + 0x328) = *(int*)(self + 0x64);
    *(short*)(self + 0x334) = (&data_ov065_0211c0c8)[data_0209f2c0];

    V3 v;
    v.x = *(int*)(self + 0x5c);
    v.y = *(int*)(self + 0x60);
    v.z = *(int*)(self + 0x64);
    v.y = v.y - 0xa000;
    dBgCh_Gnd rg;
    rg.SetObjAndPos(*(Vector3*)&v, (dActor_c*)0);
    *(int*)(self + 0x338) = v.y;
    if (rg.DetectClsn() != 0)
        *(int*)(self + 0x338) = rg.result;
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN16daObjCtMecha05_c8BehaviorEv, 0x0211ae08, size 0x3cc */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16daObjCtMecha05_c8BehaviorEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
/* daObjCtMecha05_c::Behavior - recovered from vtable slot identity.
 *
 * Reads/writes mPosX/mPosZ (0x05c/0x064), mAngleY (0x08e) and mPrevAngleY
 * (0x094) through their inherited dActor_c names -- see the header comment
 * in daObjCtMecha05_c.h for why the flat placeholder's unk_05c/08e/094/098
 * were never this class's own storage. mHorzSpeed (0x098) is genuinely
 * reused here as a plain fix12 accumulator, not a physical speed. */
int daObjCtMecha05_c::Behavior()
{
    char *c = (char *)this;

    if (data_0209f2c0 == 3) {
        int ang;
        /* Read UNSIGNED here -- ldrh, not the ldrsh a plain `s16 mPrevAngleY`
           read would emit -- matching the ROM exactly even though mPrevAngleY
           itself is signed (dActor_c.h, evidenced elsewhere). */
        ang = *(u16 *)(c + 0x94);
        mPosX = mHomePosX +
            (int)(((long long)data_02082214[(ang >> 4) << 1] * 0xfa000 + 0x800) >> 12);
        ang = *(u16 *)(c + 0x94);
        mPosZ = mHomePosZ +
            (int)(((long long)data_02082214[((ang >> 4) << 1) + 1] * 0xfa000 + 0x800) >> 12);
        UpdateModelPosAndRotY();
        func_ov065_0211ac0c(c);
        if (_ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(c, 0, 0) != 0)
            UpdateClsnPosAndRot();
        return 1;
    }

    mPrevTravel = mTravel;
    I32(0x32c) += mHorzSpeed;

    switch (mState) {
    case 0:
        if (DecIfAbove0_Short((u16 *)(c + 0x334)) != 0) goto Lend;
        if (data_0209f2c0 == 2) {
            int v = (u16)((unsigned)RandomIntInternal(&data_0209e650) >> 16);
            if (v % 2 == 0) {
                mStateTimer = v % 100 + 20;
            }
        }
        I8(0x336)++;
        mHorzSpeed = -0x8000;
        goto Lend;

    case 1:
        I32(0x98) += 0xbae;
        if (mHorzSpeed > 0) {
            if (DecIfAbove0_Short((u16 *)(c + 0x334)) != 0) {
                mHorzSpeed = 0;
                goto Lend;
            }
            I8(0x336)++;
            mHorzSpeed = 0x1d000;
        }
        goto Lend;

    case 2: {
        int d = mTravel;
        if (d != 0xfa000) {
            int val = (int)(((long long)(0xfa000 - d) * (0xfa000 - mPrevTravel) + 0x800) >> 12);
            if (val >= 0) goto L280;
        }
        {
            int sp = mHorzSpeed;
            if (sp <= -0x8000) goto L280;
            if (sp >= 0x8000) goto L280;
            I8(0x336)++;
            mHorzSpeed = 0;
            mStateTimer = 0x1e;
            goto Lend;
        }
      L280:
        {
            int m = (mTravel < 0xfa000) ? 0x6666 : -0x6666;
            int sp = mHorzSpeed;
            int delta;
            if ((int)(((long long)sp * m + 0x800) >> 12) >= 0)
                delta = m;
            else
                delta = (int)(((long long)m * 0x2599 + 0x800) >> 12);
            I32(0x98) += delta;
            if (data_0209f2c0 != 2) goto Lend;
        }
        if ((int)(((long long)mTravel * mPrevTravel + 0x800) >> 12) >= 0) goto Lend;
        if ((RandomIntInternal(&data_0209e650) & 3) != 0) goto Lend;
        func_ov065_0211ad70(c);
        goto Lend;
    }

    case 3:
        if (DecIfAbove0_Short((u16 *)(c + 0x334)) != 0) goto Lend;
        mHorzSpeed = -0x5000;
        if (mTravel < 0)
            func_ov065_0211ad70(c);
        goto Lend;
    }

Lend:
    UpdatePos(0);
    UpdateModelPosAndRotY();
    func_ov065_0211ac0c(c);
    if (_ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(c, 0x1c0000, 0x1000) != 0)
        UpdateClsnPosAndRot();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- func_ov065_0211ad70, 0x0211ad70, size 0x98 */
/* -------------------------------------------------------------------------- */
extern "C" {  /* .c-derived member: C linkage for the whole block */
void func_ov065_0211ad70(char* c) {
  unsigned char idx;
  *(unsigned char*)(c + 0x336) = 0;
  *(int*)(c + 0x32c) = 0;
  *(int*)(c + 0x98) = 0;
  *(int*)(c + 0x5c) = *(int*)(c + 0x320);
  *(int*)(c + 0x60) = *(int*)(c + 0x324);
  *(int*)(c + 0x64) = *(int*)(c + 0x328);
  idx = data_0209f2c0;
  *(short*)(c + 0x334) = (&data_ov065_0211c0c8)[idx];
  if (idx != 2) return;
  *(short*)(c + 0x334) = data_ov065_0211d26c[(unsigned short)((unsigned)RandomIntInternal(&data_0209e650) >> 16) & 3];
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN16daObjCtMecha05_c6RenderEv, 0x0211ad48, size 0x28 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16daObjCtMecha05_c6RenderEv
/* recovered: named members + shared header, real C++ method */
int daObjCtMecha05_c::Render()
{
    Base *b = &((Derived *)this)->base; b->m(0); return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN16daObjCtMecha05_c16CleanupResourcesEv, 0x0211ad04, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16daObjCtMecha05_c16CleanupResourcesEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
/* The ROM's own bss symbol names for this class's model/collision files really
   are spelled TTC_MovingBar_ModelFile / TTC_MovingBar_ClsnFile -- not a naming
   error carried over from the neighbouring class, just the shared data this
   overlay's un-migrated func_ov065_0211ad04 already referenced. Left as-is. */
int daObjCtMecha05_c::CleanupResources()
{
    if (((dBgW *)((char *)this + 0x124))->IsEnabled())
        ((dBgW *)((char *)this + 0x124))->Disable();
    ((SharedFilePtr *)TTC_MovingBar_ModelFile)->Release();
    ((SharedFilePtr *)TTC_MovingBar_ClsnFile)->Release();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov065_0211ac0c, 0x0211ac0c, size 0xf8 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov065_0211ac0c
/* recovered: shared common types */
extern "C" {  /* .c-derived member: C linkage for the whole block */
/* include/decl_common.h:2748 already declares this `void*` (a generic
 * placeholder); the legacy standalone .c file was compiled alone under
 * -lang c99 with no prior prototype in scope, so its own `int c` parameter
 * never had to agree with anything. In this merged C++ TU decl_common.h's
 * prototype wins (redeclaring with a conflicting type is a hard error), so
 * take the pointer and recover the integer used for byte-offset arithmetic
 * below. */
void func_ov065_0211ac0c(void* cp)
{
    int c = (int)cp;
    struct Vector3 v1;
    struct Vector3 v2;
    int d;

    d = *(int*)(c + 0x60) - *(int*)(c + 0x338);
    if (d < 0) d = -d;
    if (d > 0x9c4000) return;
    if (d < 0xa000) return;

    v1.z = 0;
    v1.z = 0xffebb000;
    v2.x = 0;
    v2.y = 0;
    v2.z = 0;
    v1.x = 0;
    v1.y = 0;

    Matrix4x3_FromRotationY((void*)(c + 0x364), *(short*)(c + 0x8e));
    MulVec3Mat4x3(&v1, (void*)(c + 0x364), &v2);
    AddVec3(&v2, (void*)(c + 0x5c), &v2);

    *(int*)(c + 0x388) = v2.x >> 3;
    *(int*)(c + 0x38c) = (*(int*)(c + 0x338)) >> 3;
    *(int*)(c + 0x390) = v2.z >> 3;

    _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(
        c, c + 0x33c, c + 0x364, 0xdc000, 0x32000, 0x28a000, 0xf);
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinals 1 and 0 -- one definition, two ROM-visible variants:          */
/*   _ZN16daObjCtMecha05_cD1Ev  0x0211ab60  size 0x4c  (complete-object)      */
/*   _ZN16daObjCtMecha05_cD0Ev  0x0211abac  size 0x60  (deleting)            */
/* -------------------------------------------------------------------------- */
/* recovered: real C++ destructor -- the compiler emits the whole body.
 *
 * Two vtable stores and three destructor calls, every one a consequence of
 * `struct daObjCtMecha05_c : dBgActor_c`: its own vptr, then dBgActor_c's --
 * inlined, because dBgActor_c's destructor is defined in its class body --
 * then dBgActor_c's dBgW_KcMbg and Model, then dActor_c. This class
 * adds one member with a destructor of its own, ShadowModel at 0x33c, which
 * the compiler destroys first. D0 additionally returns the object to its
 * heap through an explicit Memory::Deallocate call, matching the un-migrated
 * func_ov065_0211abac this file replaces.
 *
 * In the one-function tree these were two files, each carrying this same
 * empty body and each having objisolate keep the variant its filename named.
 * In TU context one definition produces both -- and a third,
 * _ZN16daObjCtMecha05_cD2Ev, byte-identical to D1, which the ROM does not
 * contain (see decomp-cpp-class-form and the matching PoleLift/Squasher
 * precedent in this same src_tu/actors/ directory).
 */
