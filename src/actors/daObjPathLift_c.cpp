//cpp
/* Translation unit, reconstructed and promoted -- generated by tools/tubuild.py create.
 * ov100/daObjPathLift_c  (8 function(s))
 *
 * CANONICAL. The ROM build compiles this file and takes every function below
 * from this one object; the per-function sources it replaced are gone. The
 * cartridge's own build emitted this class's members from one translation unit,
 * and reconstructing that unit is what lets the compiler place the destructor
 * variants itself instead of each one being hand-placed under a `// @symbol`
 * marker.
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x02146d7c  src/_ZN15daObjPathLift_cD1Ev.cpp
 *   [1] 0x02146dec  src/_ZN15daObjPathLift_cD0Ev.cpp
 *   [2] 0x02146e70  src/func_ov100_02146e70.c
 *   [3] 0x0214700c  src/func_ov100_0214700c.c
 *   [4] 0x02147054  src/_ZN15daObjPathLift_c16CleanupResourcesEv.cpp
 *   [5] 0x021470a4  src/_ZN15daObjPathLift_c6RenderEv.cpp
 *   [6] 0x021470f4  src/_ZN15daObjPathLift_c8BehaviorEv.cpp
 *   [7] 0x021471e0  src/_ZN15daObjPathLift_c13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "daObjPathLift_c.h"
#include "math/Fix12.h"
/* decl_common.h is deliberately NOT included: it declares data_ov100_02148a54
   and data_ov100_02148a5c as plain `int`, less complete than the real
   SharedFilePtr type this TU's own InitResources/CleanupResources need to
   call through the real Model::LoadFile / SharedFilePtr::Release members.
   Everything else it would have supplied is restated below at its real
   type. */
extern "C" {
extern void* data_ov100_0214857c[];
extern int data_ov002_0210d7d4;
extern void func_ov002_020efcf4(void*);
extern void func_ov002_020efc74(void*);
extern void func_ov100_02146e70(void*);
}

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* func_ov100_02146e70.c's `enum { false, true };` (a C-only workaround --
   false/true are keywords in C++) is dropped; the real bool literals do the
   same job in this C++ TU. Render()'s `Obj` virtual-dispatch shadow struct is
   dropped too -- it stood in for mModel (a real dBgActor_c member), see
   InitResources/Render below. */

/* shadow typedef 'Fix12i' */
typedef int Fix12i;

/* shadow namespace 'Sound' */
namespace Sound { unsigned int PlayLong(unsigned int, unsigned int, unsigned int, const Vector3&, short); }

/* shadow struct 'dActor_c' */
struct dActor_c;

/* shadow struct 'dBgCh_Gnd' */
struct dBgCh_Gnd {
  char pad[0x44];
  int result;
  char pad2[0xc];
  dBgCh_Gnd();
  ~dBgCh_Gnd();
  void SetObjAndPos(const Vector3& pos, dActor_c* a);
  int DetectClsn();
};

extern "C" {
extern void* data_ov002_0210af70[];
extern int _ZTV10dBgActor_c[];
void _ZN11ShadowModelD1Ev(void*);
void __destroy_arr(void*,int,int,void(*)(void*));
void _ZN10dBgW_KcMbgD1Ev(void*);
void _ZN5ModelD1Ev(void*);
void _ZN8dActor_cD2Ev(void*);
extern void Matrix4x3_FromRotationY(void *m, short ang);
extern void _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(void *self, void *sm, void *mtx, int a, int b, int d, unsigned int e);
extern short data_02082214[];
extern signed char data_0209f2f8;
extern unsigned char data_0209f2d8;
extern void Matrix4x3_FromRotationXYZExt(void* dst, int a, int b, int c2);
void _ZN13SharedFilePtr7ReleaseEv(void *);
extern "C" Fix12i Vec3_Dist(const Vector3* a, const Vector3* b);
extern "C" unsigned char DecIfAbove0_Byte(unsigned char* p);
extern "C" int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(void *self, int a, int b);
extern "C" void func_020393a4(int *p, int v);
extern "C" void func_02039394(int *p, int v);
void* _ZN5Model8LoadFileER13SharedFilePtr(void* p);
void _ZN9ModelBase7SetFileEP8BMD_Fileii(void* self, void* f, int a, int b);
void _ZN11ShadowModel10InitCuboidEv(void* self);
int func_ov100_0214700c(char* c);
void _ZN10dBgActor_c19UpdateClsnPosAndRotEv(char* self);
void* _ZN7dBgW_Kc8LoadFileER13SharedFilePtr(void* p);
void _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(void* self, void* kcl, const void* mtx, int fix, short s, void* clps);
void func_020393d4(int* p, int v);
void func_ov002_020efaf0(char* c);
extern void _ZN4dBgW16UpdatePosAndAngsERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_();
}

/* Real type (matches src/_ZN11PyramidLift13InitResourcesEv.cpp), not the
   `int[]` this TU's own CleanupResources legacy file assumed -- that file's
   bare (no `&`) SharedFilePtr::Release call site is fixed below to match. */
extern SharedFilePtr data_ov002_0210d9f0;
extern SharedFilePtr data_ov100_02148a54;
extern SharedFilePtr data_ov100_02148a5c;

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN15daObjPathLift_c13InitResourcesEv, 0x021471e0, size 0x148 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15daObjPathLift_c13InitResourcesEv
/* recovered: shared common types, renamed to Class_Method, RTTI class fields named, declarations from a shared header */
/* recovered: shared common types, renamed to Class_Method, RTTI class fields named */
// recovered name: daObjPathLift_c_InitResources
/* recovered: shared common types, renamed to Class_Method */
/* daObjPathLift_c::InitResources - name recovered from the vtable slot it fills.
   The body is a decompilation verified against the ROM, not an
   inferred stub. */
int daObjPathLift_c::InitResources() {
  char *c = (char*)this;
  Vector3 pos;
  Model::LoadFile(data_ov002_0210d9f0);
  mModel.SetFile((BMD_File*)Model::LoadFile(data_ov100_02148a54), 1, -1);
  mShadowModel.InitCuboid();
  func_ov100_0214700c(c);
  UpdateClsnPosAndRot();
  /* Not called through the real dBgW_KcMbg::SetFile member: its `Fix12<int>
     scale` parameter has no converting constructor from an int literal, and
     materialising 0x1000 (fixed-point 1.0) into one costs stack traffic the
     ROM does not have -- the same Fix12<int>-aggregate cost documented on
     IsClsnInRange in ::Behavior below. The extern "C" spelling passes the
     raw fixed-point word instead, exactly as the ROM does. */
  _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
      c+0x124, dBgW_Kc::LoadFile(data_ov100_02148a5c),
      c+0x2ec, 0x1000, mAngleY, &data_ov002_0210d7d4);
  func_020393d4((int*)(c+0x124), (int)&_ZN4dBgW16UpdatePosAndAngsERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_);
  /* unk_440/unk_43c/unk_42c are PathLift's own generic tail padding, not this
     class's fields -- see the header comment -- reached by raw offset. */
  *(s32*)(c + 0x440) = 0xa000;
  mHorzSpeed = *(s32*)(c + 0x440);
  func_ov002_020efaf0(c);
  *(s32*)(c + 0x43c) = 1;
  pos.x = mPosX;
  pos.y = mPosY;
  pos.z = mPosZ;
  pos.y -= 0x14000;
  {
    dBgCh_Gnd rg;
    int b;
    rg.SetObjAndPos(pos, 0);
    mGroundY = pos.y;
    if (rg.DetectClsn() != 0)
      mGroundY = rg.result;
    *(u8*)(c + 0x42c) = 1;
    b = (data_0209f2d8 == 1);
    if (b)
      mTimer = 0xb4;
  }
  return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN15daObjPathLift_c8BehaviorEv, 0x021470f4, size 0xec */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15daObjPathLift_c8BehaviorEv
/* recovered: shared common types, renamed to Class_Method, RTTI class fields named, declarations from a shared header */
/* recovered: shared common types, renamed to Class_Method, RTTI class fields named */
// recovered name: daObjPathLift_c_Behavior
/* recovered: shared common types, renamed to Class_Method */
/* daObjPathLift_c::Behavior - name recovered from the vtable slot it fills.
   The body is a decompilation verified against the ROM, not an
   inferred stub. */
/* The fifth parameter is a SIGNED SHORT, not an unsigned int. Declared `unsigned
   int' this mangles _ZN5Sound8PlayLongEjjjRK7Vector3j, which is the symbol that
   correction removed -- and because this file declares the function in C++ and
   lets the compiler mangle it, rather than spelling the mangled name, a textual
   search for the old symbol could never find it. The pre-push reference check
   did: "new unresolved reference: func_ov100_021470f4". */
/* `Platform2` and `Platform3` were phantom CLASSES, and nothing in the source text
   said so -- the names only exist after mangling. `_ZN10dBgActor_c19UpdateClsnPosAndRotEv`
   is dBgActor_c::UpdateClsnPosAndRot, but read as `_ZN9Platform2` + `19UpdateClsn...` it
   splits into a class `Platform2` whose method name is one character shorter. Two
   different shadow structs grew out of one class that way. The length prefix is the
   only thing that distinguishes them, and the retail relocations for both call sites
   land on dBgActor_c's methods. */
/* IsClsnInRange takes two Fix12<int>, not two ints -- the S1_ back-reference in
   _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_ can only come from a repeated class type.
   It is declared by its final name rather than as a member of dBgActor_c above,
   because a faithful `int IsClsnInRange(Fix12<int>, Fix12<int>)` does not reproduce
   the call: Fix12<int> is an aggregate with no converting constructor from int, and
   every way of materialising a zero one (`= {0}`, `Fix12<int>()`, a named local)
   costs stack traffic the ROM does not have -- measured at 0x100 and 0x104 bytes
   against the cartridge's 0xec. The ROM passes the pair in registers exactly as two
   ints, so that is what the call site says. */
int daObjPathLift_c::Behavior()
{
    char *c = (char*)this;
    func_ov002_020efcf4(c);
    BaseBehavior();
    if (Vec3_Dist((Vector3*)(c + 0x5c), (Vector3*)(c + 0x68)) != 0) {
        if (DecIfAbove0_Byte((unsigned char*)(c + 0x4b0)) == 0) {
            mSoundHandle = Sound::PlayLong(mSoundHandle, 3, 0x82, *(Vector3*)(c + 0x74), 0);
        }
    }
    func_ov100_0214700c(c);
    UpdateClsnPosAndRot();
    func_ov100_02146e70(c);
    func_020393a4((int*)(c + 0x124), 0x150000);
    func_02039394((int*)(c + 0x124), 0x1000);
    int b = (int)(data_0209f2d8 == 1);
    if (b != 0) {
        if (mMeshCollider.IsEnabled() == 0) {
            mMeshCollider.Enable(this);
        }
    } else {
        _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(c, 0, 0);
    }
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN15daObjPathLift_c6RenderEv, 0x021470a4, size 0x50 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15daObjPathLift_c6RenderEv
/* daObjPathLift_c::Render - name recovered from the vtable slot it fills.
   The body is a decompilation verified against the ROM, not an
   inferred stub. unk_428 is PathLift's own tail-padding, not this class's --
   see the header comment -- so it is read by raw offset. mModel (0xd4) is
   inherited from dBgActor_c, a real member throughout -- no shadow struct. */
int daObjPathLift_c::Render(){
  unsigned short h = *(unsigned short*)((char*)this + 0x428);
  if(h < 0x5a){
    if(h & 1) return 1;
  }
  func_ov002_020efc74(this);
  mModel.Render(0);
  return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN15daObjPathLift_c16CleanupResourcesEv, 0x02147054, size 0x50 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15daObjPathLift_c16CleanupResourcesEv
/* daObjPathLift_c::CleanupResources -- vtable slot 3. Takes the inherited mesh
 * collider out of the collision world if it is still in it, then releases the
 * three shared files the lift holds.
 *
 * THE FIRST FILE LIVES IN A DIFFERENT OVERLAY: the retail relocation for that
 * site resolves into ov002, the other two into ov100. They were all spelled
 * G0/G1/G2 in the legacy C form, which hid the crossing. */
int daObjPathLift_c::CleanupResources()
{
    if (mMeshCollider.IsEnabled()) {
        mMeshCollider.Disable();
    }
    _ZN13SharedFilePtr7ReleaseEv(&data_ov002_0210d9f0);
    _ZN13SharedFilePtr7ReleaseEv(&data_ov100_02148a54);
    _ZN13SharedFilePtr7ReleaseEv(&data_ov100_02148a5c);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- func_ov100_0214700c, 0x0214700c, size 0x48 */
/* -------------------------------------------------------------------------- */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int func_ov100_0214700c(char* c) {
    Matrix4x3_FromRotationXYZExt(c+0xf0, *(short*)(c+0x8c), *(short*)(c+0x8e), 0);
    *(int*)(c+0x114) = *(int*)(c+0x5c) >> 3;
    *(int*)(c+0x118) = *(int*)(c+0x60) >> 3;
    int z = *(int*)(c+0x64) >> 3;
    *(int*)(c+0x11c) = z;
    return z;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov100_02146e70, 0x02146e70, size 0x19c */
/* -------------------------------------------------------------------------- */
extern "C" {  /* .c-derived member: C linkage for the whole block */
void func_ov100_02146e70(void *c_) {
    char *c = (char*)c_;
    int r6;
    int r5;
    int belse;
    int idx;
    int s0;
    int s1;
    int t;

    if (data_0209f2f8 == 13) return;
    if (*(int*)(c+0x4ac) > *(int*)(c+0x60)) return;

    belse = (*(int*)(c+0x60) - *(int*)(c+0x4ac)) + 0x64000;

    idx = *(unsigned short*)(c+0x8c) >> 4;
    s0 = data_02082214[idx << 1];
    r5 = (int)(((long long)s0 * 0x17c000 + 0x800) >> 12);
    if (r5 < 0) r5 = -r5;
    s1 = data_02082214[(idx << 1) + 1];
    r6 = (int)(((long long)s1 * 0x17c000 + 0x800) >> 12);
    if (r6 < 0) r6 = -r6;

    Matrix4x3_FromRotationY(c+0x478, *(short*)(c+0x8e));
    *(int*)(c+0x49c) = *(int*)(c+0x5c) >> 3;
    *(int*)(c+0x4a0) = (*(int*)(c+0x60) - r5) >> 3;
    *(int*)(c+0x4a4) = *(int*)(c+0x64) >> 3;

    t = data_0209f2d8;
    t = t == 1;
    if (t != false) {
        _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(c, c+0x450, c+0x478, 0x118000, 0x7d0000, r6, 0xf);
    } else {
        _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(c, c+0x450, c+0x478, 0x118000, belse, r6, 0xf);
    }
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 1 -- _ZN15daObjPathLift_cD0Ev, 0x02146dec, size 0x84 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15daObjPathLift_cD0Ev
/* recovered: renamed to Class_Method, declarations from a shared header */
#include "decl_Actor.h"
#include "decl_Model.h"
#include "decl_dBgW_KcMbg.h"
#include "decl_ShadowModel.h"
/* decl_common.h dropped here too -- see the note at the top of this file. */
/* recovered: renamed to Class_Method */
extern "C" {
extern void* data_020a0eac;


void* _ZN15daObjPathLift_cD0Ev(char* p){
  *(void***)p = (void**)data_ov100_0214857c;
  _ZN11ShadowModelD1Ev(p+0x450);
  *(void***)p = (void**)data_ov002_0210af70;
  __destroy_arr(p+0x320, 3, 0x50, _ZN5ModelD1Ev);
  *(void***)p = (void**)_ZTV10dBgActor_c;
  _ZN10dBgW_KcMbgD1Ev(p+0x124);
  _ZN5ModelD1Ev(p+0xd4);
  _ZN8dActor_cD2Ev(p);
  _ZN6Memory10DeallocateEPvP4Heap(p, data_020a0eac);
  return p;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN15daObjPathLift_cD1Ev, 0x02146d7c, size 0x70 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15daObjPathLift_cD1Ev
/* recovered: vtable identified, declarations from a shared header
 *
 * NOT a real out-of-line `daObjPathLift_c::~daObjPathLift_c()`: that form
 * measured 999 differing words under 2004/b56 (build_pin), because PathLift's
 * own destructor (include/PathLift.h) is genuinely out-of-line -- unlike
 * dBgActor_c's, which is inline on purpose precisely so every subclass
 * inlines it -- so a real derived-class destructor would call
 * `_ZN8PathLiftD2Ev`, a symbol that does not exist in the ROM. The ROM
 * instead inlines PathLift's own cleanup directly here, exactly as this free
 * function under the mangled D1 name reproduces. `virtual ~daObjPathLift_c();`
 * in the header stays a declaration only -- nothing ever calls it through a
 * real `delete`, so the linker never needs a body, matching the
 * never-define-the-key-function idiom in include/dActor_c.h.
 */
extern "C" int _ZN15daObjPathLift_cD1Ev(char* c){
  *(int**)c=(int*)data_ov100_0214857c;
  _ZN11ShadowModelD1Ev(c+0x450);
  *(int**)c=(int*)data_ov002_0210af70;
  __destroy_arr(c+0x320,3,0x50,_ZN5ModelD1Ev);
  *(int**)c=(int*)_ZTV10dBgActor_c;
  _ZN10dBgW_KcMbgD1Ev(c+0x124);
  _ZN5ModelD1Ev(c+0xd4);
  _ZN8dActor_cD2Ev(c);
  return (int)c;
}
