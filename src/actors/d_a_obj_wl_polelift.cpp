//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov026/daObjWlPolelift_c  (8 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x021111a0  src/_ZN17daObjWlPolelift_cD1Ev.cpp
 *   [1] 0x021111e0  src/_ZN17daObjWlPolelift_cD0Ev.cpp
 *   [2] 0x02111234  src/func_ov026_02111234.cpp
 *   [3] 0x021112a4  src/func_ov026_021112a4.c
 *   [4] 0x021112e4  src/_ZN17daObjWlPolelift_c16CleanupResourcesEv.cpp
 *   [5] 0x02111308  src/_ZN17daObjWlPolelift_c6RenderEv.cpp
 *   [6] 0x02111330  src/_ZN17daObjWlPolelift_c8BehaviorEv.cpp
 *   [7] 0x02111598  src/_ZN17daObjWlPolelift_c13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "daObjWlPolelift_c.h"
#include "common.h"
#include "SharedFilePtr.h"
#include "dBgCh_Gnd.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow namespace 'cstd' */
namespace cstd {
int fdiv(int a, int b);
}

extern "C" {
extern void _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(void*,void*,void*,int,int,int,unsigned int);
extern void Matrix4x3_FromRotationY(void *, int);
extern SharedFilePtr data_ov026_02113ea0;
u16 DecIfAbove0_Short(u16 *timer);
void Vec3_Sub(Vector3 *out, Vector3 *a, Vector3 *b);
int LenVec3(Vector3 *v);
void Vec3_MulScalar(Vector3 *out, Vector3 *a, int scale);
void SubVec3(Vector3 *out, Vector3 *a, Vector3 *b);
void func_ov026_021112a4(char *t);
void func_ov026_02111234(char *c);
void _ZN10dCcAcPos_c4InitEP8dActor_cRK7Vector35Fix12IiES6_jj(
dCcAcPos_c *self, dActor_c *actor, const Vector3 *offset,
Fix12i radius, Fix12i height, u32 flags, u32 vulnFlags);
extern u32 data_0209caa0[];
}

/* -------------------------------------------------------------------------- */
/* -------------------------------------------------------------------------- */
/* ROM ordinal 8 -- daObjWlPolelift_c_classInit, 0x02111678, size 0x50         */
/* -------------------------------------------------------------------------- */
/* tu_map.py cut the TU at 0x02111678, leaving the class factory outside it.
 * That boundary is wrong: the factory is the only referent of
 * g_profile_WL_POLELIFT, it allocates sizeof(daObjWlPolelift_c), and it owns
 * the class's data records. Corrected here to 0x021111a0..0x021116c8, the same
 * correction the landed ov045/daObjKm2_Gura_c entry documents.
 *
 * mwccarm emits sections in reverse source order, so the factory -- the highest
 * ROM address in the run -- comes first. */

extern "C" {
extern void *_ZN7fBase_cnwEj(u32 size);
extern void _ZN8dActor_cC2Ev(void *self);
extern void _ZN5ModelC1Ev(void *self);
extern void _ZN10dCcAcPos_cC1Ev(void *self);
extern void _ZN7PathPtrC1Ev(void *self);
extern void _ZN11ShadowModelC1Ev(void *self);
}

/* Retail places this vector between the class typeinfo record and the type-name
 * string: the collision offset InitResources and Behavior hand to
 * dCcAcPos_c::Init. Bytes {0, 0x28000, 0} == (0, 40.0, 0) in Fix12i. */
/* NOT typed Vector3: that type declares a destructor (include/types.h explains
 * why -- the ROM destroys arrays of it), and a namespace-scope object of a type
 * with a destructor makes mwcc emit a __sinit_ static initializer retail has no
 * trace of. A POD triple has identical storage and emits nothing. */
struct PoleliftVec3 { s32 x, y, z; };
extern "C" PoleliftVec3 data_ov026_02113a9c = { 0, 0x28000, 0 };

/* This TU defines every virtual of the class, so it holds the key function and
 * the compiler emits the vtable here. mwcc's _ZTV symbol then addresses the
 * vtable OBJECT (0x02113ad8); the address point retail stores is object+8, so
 * the vptr store goes through &_ZTV[2]. (The addend-0 spelling applies only
 * when _ZTV is imported, where the symbol already IS the address point.) */
extern int _ZTV17daObjWlPolelift_c[];

// @symbol daObjWlPolelift_c_classInit
extern "C" daObjWlPolelift_c *daObjWlPolelift_c_classInit()
{
    daObjWlPolelift_c *actor =
        (daObjWlPolelift_c *)_ZN7fBase_cnwEj(sizeof(daObjWlPolelift_c));
    if (actor) {
        _ZN8dActor_cC2Ev(actor);
        *(int *)actor = (int)&_ZTV17daObjWlPolelift_c[2];
        _ZN5ModelC1Ev(&actor->model);
        _ZN10dCcAcPos_cC1Ev(&actor->clsn);
        _ZN7PathPtrC1Ev(&actor->path);
        _ZN11ShadowModelC1Ev(&actor->shadow);
    }
    return actor;
}

struct PoleliftSpawnInfo {
    daObjWlPolelift_c *(*classInit)();
    s16 profileIDAndExecuteOrder;   /* 0x0059 */
    s16 drawOrder;                  /* 0x00fa */
    u32 actorFlags;
    Fix12i clipOffsetY;
    Fix12i clipRadius;
    Fix12i clipDistance;
    Fix12i farDistance;
};
typedef char PoleliftSpawnInfo_size_must_be_0x1c[
    sizeof(PoleliftSpawnInfo) == 0x1c ? 1 : -1];

extern "C" PoleliftSpawnInfo g_profile_WL_POLELIFT = {
    daObjWlPolelift_c_classInit,
    0x0059,
    0x00fa,
    0,
    0,
    0x00400000,
    0x01000000,
    0x01000000
};

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN17daObjWlPolelift_c13InitResourcesEv, 0x02111598, size 0xe0 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN17daObjWlPolelift_c13InitResourcesEv
/* Load the lift model, bind its path/collision state, and initialize the
 * actor-owned shadow and cylinder through the reconstructed class layout. */
s32 daObjWlPolelift_c::InitResources()
{
    model.SetFile((BMD_File *)Model::LoadFile(data_ov026_02113ea0), 1, -1);
    if ((data_0209caa0[1] & 0x204) == 0)
        return 0;

    shadow.InitCuboid();
    path.FromID(param1 & 0xff);
    numNodes = path.NumNodes();
    moveSpeed = 1;

    Vector3 offset;
    offset.x = data_ov026_02113a9c.x;
    offset.y = data_ov026_02113a9c.y;
    offset.z = data_ov026_02113a9c.z;
    _ZN10dCcAcPos_c4InitEP8dActor_cRK7Vector35Fix12IiES6_jj(
        &clsn, this, &offset, 0x50000, 0x12c000, 0x80000c, 0);
    func_ov026_021112a4((char *)this);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN17daObjWlPolelift_c8BehaviorEv, 0x02111330, size 0x268 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN17daObjWlPolelift_c8BehaviorEv
/* Move toward the current path node, reverse or wrap at path ends, and keep
 * the owned collision/shadow state aligned with the lift. */
s32 daObjWlPolelift_c::Behavior()
{
    Vector3 node;
    Vector3 prevNode;
    Vector3 delta;
    Vector3 rayPos;
    Vector3 step;
    Vector3 clsnOffset;
    int moved;
    int prevNodeIdx;
    int speed;
    int current;
    int length;
    int scale;
    int skipCollision;

    if (DecIfAbove0_Short((u16 *)&bounceTimer) == 0) {
        speed = moveSpeed;
        current = curNodeIdx;
        prevNodeIdx = current - speed;
        moved = 0;
        if (speed > 0) {
            if (prevNodeIdx < 0)
                prevNodeIdx = numNodes - 1;
        } else if (prevNodeIdx >= numNodes) {
            prevNodeIdx = moved;
        }

        path.GetNode(node, current);
        path.GetNode(prevNode, prevNodeIdx);
        Vec3_Sub(&delta, (Vector3 *)&mPosX, &node);
        length = LenVec3(&delta);
        if (length == 0 || length <= 0xa000) {
            mPosX = node.x;
            moved = 1;
            mPosY = node.y;
            mPosZ = node.z;
        } else {
            scale = cstd::fdiv(0xa000, length);
            Vec3_MulScalar(&step, &delta, scale);
            SubVec3((Vector3 *)&mPosX, &step, (Vector3 *)&mPosX);
        }

        if (moved) {
            curNodeIdx += moveSpeed;
            if (curNodeIdx < 0) {
                if (path.Loops()) {
                    curNodeIdx = numNodes - 1;
                } else {
                    bounceTimer = 0x3c;
                    moveSpeed = 1;
                    curNodeIdx += moveSpeed << 1;
                }
            }
            if (curNodeIdx >= numNodes) {
                if (path.Loops()) {
                    curNodeIdx = 0;
                } else {
                    bounceTimer = 0x3c;
                    moveSpeed = -1;
                    curNodeIdx += moveSpeed << 1;
                }
            }
        }
    }

    clsn.Clear();
    skipCollision = (mFlags & 8) != 0;
    if (!skipCollision) {
        rayPos = *(Vector3 *)&mPosX;
        rayPos.y -= 0x14000;

        dBgCh_Gnd ray;
        ray.SetObjAndPos(rayPos, 0);
        groundY = rayPos.y;
        if (ray.DetectClsn())
            groundY = ray.clsnY;

        clsnOffset = *(Vector3 *)&data_ov026_02113a9c;
        clsn.SetPosRelativeToActor(clsnOffset);
        clsn.Update();
        func_ov026_021112a4((char *)this);
        func_ov026_02111234((char *)this);
    }
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN17daObjWlPolelift_c6RenderEv, 0x02111308, size 0x28 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN17daObjWlPolelift_c6RenderEv
/* daObjWlPolelift_c::Render -- recovered from vtable slot identity. Real C++
 * method over the shared header; the owned Model renders through its vtable. */
s32 daObjWlPolelift_c::Render()
{
    model.Render(0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN17daObjWlPolelift_c16CleanupResourcesEv, 0x021112e4, size 0x24 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN17daObjWlPolelift_c16CleanupResourcesEv
/* daObjWlPolelift_c::CleanupResources -- vtable slot 3. Releases the one shared
 * file the class holds and reports success; it never touches `this`. */
s32 daObjWlPolelift_c::CleanupResources()
{
    data_ov026_02113ea0.Release();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- func_ov026_021112a4, 0x021112a4, size 0x40 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov026_021112a4
extern "C" {  /* .c-derived member: C linkage for the whole block */
void func_ov026_021112a4(char *t)
{
    Matrix4x3_FromRotationY(t + 0xf0, *(short *)(t + 0x8e));
    *(int *)(t + 0x114) = *(int *)(t + 0x5c) >> 3;
    *(int *)(t + 0x118) = *(int *)(t + 0x60) >> 3;
    *(int *)(t + 0x11c) = *(int *)(t + 0x64) >> 3;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov026_02111234, 0x02111234, size 0x70 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov026_02111234
/* recovered: shared common types */
// tu_create.py: the definition was inside an extern "C" block; the block was closed before it and the definition given explicit C linkage
/* The ROM copies all twelve words of the matrix uniformly (three ldm/stm).
 * Matrix4x3 has two spellings guarded by the same MATRIX4X3_DEFINED macro:
 * common.h's flat `s32 m[12]`, and math/Matrix.h's `{ Matrix3x3 r; Vector3 t; }`.
 * Both are 0x30, but only the flat one copies uniformly -- the two-member one
 * makes mwcc block-copy `r` and scalarize `t`. Which one a TU sees is decided by
 * include order, so pin the reproducing spelling here rather than depend on it. */
struct Matrix4x3_flat { s32 m[12]; };

extern "C" void func_ov026_02111234(char* c){
  *(struct Matrix4x3_flat*)(c+0x1b0) = *(struct Matrix4x3_flat*)(c+0xf0);
  *(int*)(c+0x1d8) = *(int*)(c+0x1e0) >> 3;
  _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(
    c, c+0x188, c+0x1b0, 0x64000, 0xc8000, 0x64000, 0xf);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 1 -- _ZN17daObjWlPolelift_cD0Ev, 0x021111e0, size 0x54 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN17daObjWlPolelift_cD0Ev
/* D0 is the DELETING destructor: destroy through this class and its bases, then
 * return the object to its heap. The deallocation is an inline operator delete,
 * which is why no heap call appears in the retail bytes. */

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN17daObjWlPolelift_cD1Ev, 0x021111a0, size 0x40 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN17daObjWlPolelift_cD1Ev
/* D1 is the complete-object destructor: store this class's vtable over the one
 * the base constructor left, destroy the members with non-trivial destructors in
 * reverse declaration order (ShadowModel, dCcAcPos_c, Model -- PathPtr is POD),
 * then run the dActor_c subobject destructor. All of that follows from
 * `struct daObjWlPolelift_c : dActor_c` and the member types, so the source body
 * is empty and the retail bytes still reproduce.
 *
 * Neither destructor is defined here: the body is inline in the class header, and
 * these two blocks record only the ROM ordinals the manifest licenses. See the
 * header for why inline is required rather than merely tidy. */
