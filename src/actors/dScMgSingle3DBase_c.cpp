//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov006/dScMgSingle3DBase_c  (9 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x0210a4b0  src/_ZN19dScMgSingle3DBase_cD1Ev.cpp
 *   [1] 0x0210a4e8  src/_ZN19dScMgSingle3DBase_cD0Ev.cpp
 *   [2] 0x0210a534  src/func_ov006_0210a534.cpp
 *   [3] 0x0210a600  src/func_ov006_0210a600.c
 *   [4] 0x0210a608  src/_ZN19dScMgSingle3DBase_c21AfterCleanupResourcesEj.cpp
 *   [5] 0x0210a664  src/_ZN19dScMgSingle3DBase_c12BeforeRenderEv.cpp
 *   [6] 0x0210a698  src/_ZN19dScMgSingle3DBase_c14BeforeBehaviorEv.cpp
 *   [7] 0x0210a6e4  src/_ZN19dScMgSingle3DBase_c18AfterInitResourcesEj.cpp
 *   [8] 0x0210a708  src/func_ov006_0210a708.c
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "dScMgSingle3DBase_c.h"
#include "types.h"
#include "decl_common.h"

/* Local shadow declarations. RECONCILED against include/ -- the generator
 * carried eight of these over verbatim; two are gone because a real header
 * already wins, and the six that remain have no project header to lose to.
 *
 * Dropped, now supplied by decl_common.h (which wraps its whole body in
 * extern "C", so the linkage matches too):
 *   Deallocate       -- decl_common.h:1862, `extern void Deallocate(void*)`
 *   func_020562b4    -- decl_common.h:2111, same three-parameter signature
 *
 * G2S and GXS are real namespaces (their bodies are in arm9, e.g.
 * _ZN3G2S13GetBG3CharPtrEv at arm9:0x02054d38 and _ZN3GXS10LoadBGPlttEPKvjj),
 * but the tree has no G2S.h/GXS.h:
 * sixteen and seventeen other sources respectively carry this same one-line
 * declaration. Following that idiom rather than inventing a header here.
 *
 * LoadFile is NOT the LoadFile in Animation.h / Model.h / SharedFilePtr.h --
 * those are static members taking SharedFilePtr&. This is a distinct free
 * function taking a file id. DecompressLZ16 and data_0209d454 appear in
 * include/ only inside comments, never as declarations.
 *
 * RECONCILED SIGNATURES: the two legacy sources merged here disagreed --
 * src/func_ov006_0210a534.cpp typed the loaded file buffer as `int`, while
 * src/func_ov006_0210a708.c typed the same buffer as `void *` and passed it
 * straight to the same DecompressLZ16 / Ov004_Deallocate. One TU cannot hold
 * both, and the pointer form is the true one -- it is a buffer address, and the
 * `int` spelling forced four casts back to `void*` at the call sites. The
 * pointer typing is byte-neutral (both are one r0-width value) and verify
 * re-confirms all nine functions.
 *
 * That reconciliation also added a `const` neither legacy file had:
 * DecompressLZ16(const void *src, void *dst).  It is a judgement call, not
 * evidence -- nothing in the ROM distinguishes a const pointer parameter
 * from a non-const one.  It is stated here rather than left implied because
 * it narrows the signature for every future caller; drop the qualifier if a
 * caller that writes through src ever turns up. */
namespace G2S { void* GetBG3CharPtr(); }
namespace GXS { void LoadBGPltt(const void*, u32, u32); }

extern "C" {
void *LoadFile(int fileId);
void DecompressLZ16(const void *src, void *dst);
void Ov004_Deallocate(void *p);
extern u8 data_0209d454;
extern unsigned int data_020a0db0;
}

/* Additional externs reached only by the slot-33 override below. The GX/GXS/G3X
 * entry points are spelled here as their raw mangled names, the way the legacy
 * one-function source did, because writing them as real `GX::SetBankForBG(2)`
 * calls would require a GX.h this tree does not have -- and inventing one is a
 * separate change with its own byte risk. Left as-is deliberately; the call
 * sites read as mangled names, not as idiomatic C++, and that is a known debt. */
extern "C" {
void func_ov004_020b290c(void);
void func_ov004_020b2980(void);
void _ZN2GX12SetBankForBGEt(u16 a);
void _ZN2GX13SetBankForOBJEt(u16 a);
void _ZN2GX13SetBankForTexEt(u16 a);
void _ZN2GX17SetBankForTexPlttEt(u16 a);
void _ZN2GX15SetGraphicsModeEiii(int a, int b, int c);
void _ZN2GX15SetBankForSubBGEt(u16 a);
void _ZN2GX16SetBankForSubOBJEt(u16 a);
s32  GetGameLanguage(void);
void *func_ov004_020adc68(int id);
void _ZN4CP1527FlushAndInvalidateDataCacheEjj(void *p, u32 len);
void _ZN2GX11LoadOBJPlttEPKvjj(const void *p, u32 a, u32 b);
void _ZN3GXS11LoadOBJPlttEPKvjj(const void *p, u32 a, u32 b);
void func_ov004_020b0d30(void);
void _ZN3G3X6SetFogEbiii(int a, int b, int c, int d);
void InitialiseVramGlobals(void);
void FreeGfxSlotsById(int arg);
extern int data_ov004_020beb6c;
extern u8 data_0209d45c;
extern int data_ov006_0213e42c[];
extern int data_0208ee44;
extern int data_ov000_020beb74[];
extern void **data_0209d4a8;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 8 -- func_ov006_0210a708, 0x0210a708, size 0x1b8 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov006_0210a708
/* dScMgSingle3DBase_c's vtable slot 33 (offset +0x84).
 *
 * tools/tu_map.py did NOT put this function in this unit: it carries no class
 * label, still being spelled func_<module>_<address>, so tu_map split it into a
 * single-function unit of its own. The vtable says otherwise. Scanning every
 * _ZTV in ov006, exactly fourteen reference 0x0210a708 at slot 33 --
 * dScMgSingle3DBase_c and all thirteen of its children -- and the SAME fourteen
 * reference 0x0210a600 at slot 26, which tu_map did place inside this unit.
 * Identical reference sets: both are this class's own overrides, inherited
 * unchanged by every child, and the true TU boundary is 0x0210a4b0..0x0210a8c0.
 *
 * It stays a free function for now rather than becoming
 * dScMgSingle3DBase_c::Virtual84 -- a derived class cannot declare an override
 * of a base slot the base itself has not declared, and dScMgBase_c leaves slots
 * 18-35 undeclared. Declaring those eighteen is the follow-up that also lets
 * this class emit its full 36-slot vtable instead of an 18-slot prefix. */
extern "C" void func_ov006_0210a708(char *obj)
{
    void *p;

    *(vu32 *)0x4001000u |= 0x10000u;
    data_ov004_020beb6c = 0;
    func_ov004_020b290c();
    func_ov004_020b2980();
    data_0209d45c = 0x10;
    data_0209d454 = 0x10;
    *(vu32 *)0x4000000u &= ~0x7000000u;
    *(vu32 *)0x4000000u &= ~0x38000000u;
    _ZN2GX12SetBankForBGEt(2);
    _ZN2GX13SetBankForOBJEt(0x10);
    _ZN2GX13SetBankForTexEt(1);
    _ZN2GX17SetBankForTexPlttEt(0x20);
    _ZN2GX15SetGraphicsModeEiii(1, 0, 1);
    *(vu32 *)0x4000000u &= 0xffcfffefu;
    _ZN2GX15SetBankForSubBGEt(4);
    _ZN2GX16SetBankForSubOBJEt(8);
    p = func_ov004_020adc68(data_ov006_0213e42c[GetGameLanguage()]);
    {
        char *dst = (char *)0x6400000; dst += 0x4000;
        DecompressLZ16(p, dst);
    }
    {
        char *dst = (char *)0x6600000; dst += 0x4000;
        DecompressLZ16(p, dst);
    }
    Ov004_Deallocate(p);
    p = func_ov004_020adc68(0xc3);
    _ZN4CP1527FlushAndInvalidateDataCacheEjj(p, 0x100u);
    _ZN2GX11LoadOBJPlttEPKvjj(p, 0x100u, 0x100u);
    _ZN3GXS11LoadOBJPlttEPKvjj(p, 0x100u, 0x100u);
    Ov004_Deallocate(p);
    func_ov004_020b0d30();
    data_0208ee44 = 1;
    _ZN3G3X6SetFogEbiii(0, 0, 2, 0x1000);
    InitialiseVramGlobals();
    FreeGfxSlotsById(0x1d);
    data_ov000_020beb74[1] = (int)obj;
    data_0209d4a8 = (void **)data_ov000_020beb74;
    *(vu32 *)0x40004ccu = 0x7fff;
    *(vu32 *)0x40004ccu = 0x40007fff;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN19dScMgSingle3DBase_c18AfterInitResourcesEj, 0x0210a6e4, size 0x24 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN19dScMgSingle3DBase_c18AfterInitResourcesEj
/* dScMgSingle3DBase_c::AfterInitResources -- vtable slot 2.
 *
 * Attributed by the vtable, not by the old `recovered name:` comment, which said
 * dScMgFlower_c. That is one level too far down: dScMgFlower_c's parent IS this
 * class, and Flower's own overrides are different addresses entirely (slots 0/6/9
 * at 0x0212b480/0x0212ac74/0x0212aacc). 0x0210a6e4 is this class's own slot 2, and
 * the same vtable's slots 16/17 already carry _ZN19dScMgSingle3DBase_cD1Ev/D0Ev --
 * so the class attribution for this table was settled before this file.
 *
 * The pre-migration body chained with `_ZN11dScMgBase_c18AfterInitResourcesEj(c)`,
 * passing only `this`. The real signature takes vfSuccess in r1, and the ROM simply
 * passes its own r1 through, so naming the parameter and forwarding it is what the
 * cartridge already does. Qualified call, so this is a direct `bl` and not a
 * virtual dispatch. */
void dScMgSingle3DBase_c::AfterInitResources(u32 vfSuccess)
{
    dScMgBase_c::AfterInitResources(vfSuccess);
    mSysTracker.Initialise();
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN19dScMgSingle3DBase_c14BeforeBehaviorEv, 0x0210a698, size 0x4c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN19dScMgSingle3DBase_c14BeforeBehaviorEv
/* dScMgSingle3DBase_c::BeforeBehavior -- vtable slot 7.
 *
 * The particle tracker is only stepped when bit 0 of the global at 0x020a0db0 is
 * set, which is why the header's banner calls this the "updates it conditionally"
 * witness for mSysTracker at 0x471c. */
int dScMgSingle3DBase_c::BeforeBehavior()
{
    if (dScMgBase_c::BeforeBehavior() == 0)
        return 0;
    if (data_020a0db0 & 1)
        mSysTracker.Update();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN19dScMgSingle3DBase_c12BeforeRenderEv, 0x0210a664, size 0x34 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN19dScMgSingle3DBase_c12BeforeRenderEv
/* dScMgSingle3DBase_c::BeforeRender -- vtable slot 10.
 *
 * The pre-migration file declared a bare `struct dScene_c;` and took a `dScene_c *self`,
 * because the base call's mangling was all it had to go on. The real receiver is
 * this class, and Particle::RenderAll is a free function in that namespace rather
 * than anything hanging off dScene_c. */
int dScMgSingle3DBase_c::BeforeRender()
{
    if (!dScMgBase_c::BeforeRender())
        return 0;
    Particle::RenderAll();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN19dScMgSingle3DBase_c21AfterCleanupResourcesEj, 0x0210a608, size 0x5c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN19dScMgSingle3DBase_c21AfterCleanupResourcesEj
/* dScMgSingle3DBase_c::AfterCleanupResources -- vtable slot 5.
 *
 * The two volatile stores are the 3D engine's fog/clear registers (0x040004c8 and
 * 0x040004cc), written only on the teardown path where vfSuccess == 2. They stay
 * `volatile` on purpose: this is memory-mapped I/O, not codegen steering, even
 * though tiers.py's no_codegen_trick criterion counts the keyword either way. */
void dScMgSingle3DBase_c::AfterCleanupResources(u32 vfSuccess)
{
    if (vfSuccess == 2) {
        *(volatile int *)0x40004c8 = 0x296a5800;
        *(volatile int *)0x40004cc = 0x7fff;
        CleanCommonModelDataArr();
    }
    dScMgBase_c::AfterCleanupResources(vfSuccess);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- func_ov006_0210a600, 0x0210a600, size 0x8 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov006_0210a600
/* dScMgSingle3DBase_c's vtable slot 26 (offset +0x68), an override of
 * dScMgBase_c::OnHitByCannonBlastedChar (whose own body is at ov004:0x020b04e0).
 *
 * CORRECTED: src/func_ov006_0210a600.c carried `recovered name:
 * dScMgFlower_c_OnHitByCannonBlastedChar`, one level too deep -- the same
 * off-by-one-class error already documented for AfterInitResources. Fourteen
 * vtables in ov006 reference this address at slot 26: dScMgSingle3DBase_c and
 * all thirteen of its children, dScMgFlower_c merely being one of them. A body
 * that every sibling shares belongs to the common ancestor, not to whichever
 * child happened to be looked at first.
 *
 * Left as a free function for the same reason as slot 33 below: dScMgBase_c has
 * not declared slots 18-35, so a derived class cannot yet spell the override. */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int func_ov006_0210a600(void)
{
    return 1;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov006_0210a534, 0x0210a534, size 0xcc */
/* -------------------------------------------------------------------------- */
// @symbol func_ov006_0210a534
extern "C" void func_ov006_0210a534(void){
  void *h = LoadFile(0x26);
  data_0209d454 |= 8;
  *(volatile u16*)0x400100e = (*(volatile u16*)0x400100e & 0x43) | 0x214;
  DecompressLZ16(h, G2S::GetBG3CharPtr());
  Ov004_Deallocate(h);
  h = LoadFile(0x27);
  GXS::LoadBGPltt(h, 0xa0, 0x160);
  Deallocate(h);
  h = LoadFile(0x28);
  func_020562b4(h, 0, 0x800);
  Deallocate(h);
  *(volatile u16*)0x400100e &= ~0x40;
  *(volatile u32*)0x400101c = 0;
  *(volatile u16*)0x400100e = (*(volatile u16*)0x400100e & ~3) | 1;
}

/* --------------------------------------------------------------------------
 * ROM ordinals 1 and 0 -- _ZN19dScMgSingle3DBase_cD0Ev at 0x0210a4e8 (0x4c)
 *                     and _ZN19dScMgSingle3DBase_cD1Ev at 0x0210a4b0 (0x38).
 *
 * Neither is written out here, and neither needs a forcing helper.
 *
 * ~dScMgSingle3DBase_c() is defined inline in the class body
 * (include/dScMgSingle3DBase_c.h) and must stay there: all thirteen direct
 * RTTI children inline its D2, and _ZN19dScMgSingle3DBase_cD2Ev exists
 * nowhere in the ROM, so an out-of-line definition would leave every child
 * with an undefined external.
 *
 * What emits the out-of-line D1/D0 pair is this TU's KEY FUNCTION. The first
 * DECLARED non-inline virtual of the class is AfterInitResources (the inline
 * destructor is declared first but, being inline, cannot be the key
 * function), and this TU defines it -- so mwcc emits _ZTV19dScMgSingle3DBase_c
 * right here. Slots 16 and 17 of that table name D1 and D0, which odr-uses
 * both, and the compiler emits the pair out of line. That is exactly the
 * mechanism the retail ROM used, and it is why 0x0210a4b0 and 0x0210a4e8
 * exist at all.
 *
 * The two `..._EmitDestructor` / `..._EmitDeletingDestructor` helpers the
 * legacy one-function sources carried are therefore deleted, not ported: they
 * were scaffolding for a file that had no key function to lean on. Keeping
 * them would add two unlicensed .text symbols and change the vtable's data.
 * -------------------------------------------------------------------------- */
