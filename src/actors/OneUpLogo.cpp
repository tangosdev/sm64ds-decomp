//cpp
/* Translation unit, reconstructed and promoted -- generated by tools/tubuild.py create.
 * ov002/OneUpLogo  (8 function(s))
 *
 * CANONICAL. The ROM build compiles this file and takes every function below
 * from this one object; the per-function sources it replaced are gone. The
 * cartridge's own build emitted this class's members from one translation unit,
 * and reconstructing that unit is what lets the compiler place the destructor
 * variants itself instead of each one being hand-placed under a `// @symbol`
 * marker.
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x020f0dd0  src/_ZN9OneUpLogoD1Ev.cpp
 *   [1] 0x020f0e08  src/_ZN9OneUpLogoD0Ev.cpp
 *   [2] 0x020f0e54  src/func_ov002_020f0e54.c
 *   [3] 0x020f0e78  src/_ZN9OneUpLogo16CleanupResourcesEv.cpp
 *   [4] 0x020f0ea8  src/_ZN9OneUpLogo6RenderEv.cpp
 *   [5] 0x020f0f08  src/_ZN9OneUpLogo8BehaviorEv.cpp
 *   [6] 0x020f107c  src/_ZN9OneUpLogo13InitResourcesEv.cpp
 *   [7] 0x020f1170  src/OneUpLogo_Spawn.c
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "OneUpLogo.h"
#include "SharedFilePtr.h"
#include "TextureSequence.h"
#include "decl_Actor.h"
#include "decl_ActorBase.h"
#include "decl_Model.h"
#include "decl_TextureSequence.h"
#include "decl_common.h"

extern "C" {
/* SharedFilePtr.h declares the real class with NO fields (layout not
   recovered project-wide; see its own header comment). This TU needs both
   .file field access and a .Release() method call on the same globals, so
   the externs are backed by a local layout shadow and each real-class call
   site casts through it -- same pattern as the Number pilot (#1733). */
struct SharedFilePtrLayout { int a, file; };
extern SharedFilePtrLayout data_ov002_02110aa4;
extern SharedFilePtrLayout data_ov002_02110a9c;
void _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_(u32 id, s32 x, s32 y, s32 z);
void Matrix4x3_FromTranslation(Matrix4x3 *matrix, s32 x, s32 y, s32 z);
extern void _ZN15TextureSequence8LoadFileER13SharedFilePtr(SharedFilePtr&);
extern void* _ZN5Model8LoadFileER13SharedFilePtr(SharedFilePtr&);
extern int _ZN9ModelBase7SetFileEP8BMD_Fileii(void* self, void* f, int a, int b);
extern void _ZN15TextureSequence7SetFileER8BTP_Filei5Fix12IiEj(void* self, void* btp, int a, int c, unsigned int n);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- OneUpLogo_Spawn, 0x020f1170, size 0x40 */
/* -------------------------------------------------------------------------- */
// @symbol OneUpLogo_Spawn
/* recovered: vtable identified, globals resolved, declarations from a shared header */
/* recovered: vtable identified, globals resolved */
/* resolved: VT0 = _ZTV9OneUpLogo */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int *OneUpLogo_Spawn(void)
{
    int *p = (int *)_ZN7fBase_cnwEj(336);
    if (p) {
        _ZN8dActor_cC2Ev(p);
        p[0] = (int)(_ZTV9OneUpLogo + 2);
        _ZN5ModelC1Ev((char *)p + 0xd4);
        _ZN15TextureSequenceC1Ev((char *)p + 0x124);
    }
    return p;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN9OneUpLogo13InitResourcesEv, 0x020f107c, size 0xf4 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN9OneUpLogo13InitResourcesEv
/* recovered: named members + shared header, real C++ method */
int OneUpLogo::InitResources()
{
  unsigned short n;
  {
    unsigned int v = param1;
    n = (unsigned short)(v > 8 ? 7 : (v - 1));
  }
  _ZN15TextureSequence8LoadFileER13SharedFilePtr(*(SharedFilePtr*)&data_ov002_02110a9c);
  if (_ZN9ModelBase7SetFileEP8BMD_Fileii(&mModel, _ZN5Model8LoadFileER13SharedFilePtr(*(SharedFilePtr*)&data_ov002_02110aa4), 1, -1) == 0)
    return 0;
  TextureSequence::Prepare(*(BMD_File*)data_ov002_02110aa4.file, *(BTP_File*)data_ov002_02110a9c.file);
  _ZN15TextureSequence7SetFileER8BTP_Filei5Fix12IiEj(&mTextureSequence, (void*)data_ov002_02110a9c.file, 0x40000000, 0, n);
  mState = 0;
  mInitialPos.x = mPosX;
  mInitialPos.y = mPosY;
  mInitialPos.z = mPosZ;
  mVertSpeed = 0x14000;
  mVertAccel = -0x2000;
  mTerminalVelocity = -0x32000;
  mDelay = 0;
  mFollowActorID = 0;
  mFollowYOffset = 0;
  return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN9OneUpLogo8BehaviorEv, 0x020f0f08, size 0x174 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN9OneUpLogo8BehaviorEv
int OneUpLogo::Behavior()
{
    Vector3 pos;

    if (mDelay != 0)
        return 1;

    mVertSpeed += mVertAccel;
    if (mVertSpeed < mTerminalVelocity)
        mVertSpeed = mTerminalVelocity;
    mPosY += mVertSpeed;

    switch (mState) {
    case 0:
        if (mPosY < mInitialPos.y) {
            mPosY = mInitialPos.y;
            mVertSpeed = 0xf000;
            mState++;
        }
        break;
    case 1:
        if (mPosY < mInitialPos.y) {
            _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_(
                0xd2, mPosX, *(volatile s32 *)&mPosY, mPosZ);
            MarkForDestruction();
        }
        break;
    }

    pos.x = mPosX;
    pos.y = mPosY;
    pos.z = mPosZ;
    if (mFollowActorID != 0) {
        dActor_c *other = FindWithID(mFollowActorID);
        if (other != 0) {
            Vector3 *otherPos = (Vector3 *)&other->mPosX;
            s32 otherY;
            pos.x = otherPos->x;
            pos.y = otherY = otherPos->y;
            pos.z = otherPos->z;
            pos.y = otherY + (mFollowYOffset + (mPosY - mInitialPos.y));
        }
    }
    Matrix4x3_FromTranslation(&mModel.mat4x3, pos.x >> 3, pos.y >> 3,
                              (*(volatile s32 *)&pos.z) >> 3);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN9OneUpLogo6RenderEv, 0x020f0ea8, size 0x60 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN9OneUpLogo6RenderEv
int OneUpLogo::Render()
{
    if (mDelay != 0) {
        mDelay--;
        return 1;
    }
    mTextureSequence.Update(mModel.data);
    mModel.Render(0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN9OneUpLogo16CleanupResourcesEv, 0x020f0e78, size 0x30 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN9OneUpLogo16CleanupResourcesEv
int OneUpLogo::CleanupResources()
{
    ((SharedFilePtr*)&data_ov002_02110aa4)->Release();
    ((SharedFilePtr*)&data_ov002_02110a9c)->Release();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov002_020f0e54, 0x020f0e54, size 0x24 */
/* -------------------------------------------------------------------------- */
extern "C" {  /* .c-derived member: C linkage for the whole block */
void func_ov002_020f0e54(char *a, char *b)
{
    if (b == 0) return;
    *(int *)(a + 0x138) = *(int *)(b + 4);
    *(int *)(a + 0x148) = *(int *)(a + 0x60) - *(int *)(b + 0x60);
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 1 -- _ZN9OneUpLogoD0Ev, 0x020f0e08, size 0x4c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN9OneUpLogoD0Ev
/* Deleted: both legacy files independently reconstructed a real out-of-line
   OneUpLogo::~OneUpLogo(){} -- the D0/D1 collapse artifact (plain
   redefinition, not the mwcc ICE; same shape as daObjCannonShutter_c/#1730 and
   VirtualDoor/#1725). Keeping D1's body below; the compiler emits both
   variants from the one definition. */

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN9OneUpLogoD1Ev, 0x020f0dd0, size 0x38 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN9OneUpLogoD1Ev
