# Luigi readability repair handoff

This commit is a producer checkpoint for PR #2876, not formal source acceptance.

- Input: `6300fa4281c9ecd2b0078c3ea6113116cc98049a`; author parent: `4679d2a9a319abc7c5b43a04211a0e6b877d471e`.
- Producer: `codex-luigi2876-repair-0920`, actual child `/root/repair2747`. This child performed earlier preflight and then explicitly changed to producer; a different agent must verify.
- Task: `pr2876-source-repair-0920`; stage: `reconstruct`. Root `codex-integrator-0920` owns current-main composition, ledger reconciliation and publication.
- Active workflow pin: `18af52b49b9fb5663d417bdfa86109b7b157aaa3`.
- Owned changes: Luigi TU, its seven directly superseded manifest notes, this handoff and [durable experiments](../../experiments/pr2876-source-repair-0920.json). No header, enrollment, symbol, profile or shared-ledger edits.

The original author improvements remain. This repair uses actual scene member-function pointers at six dispatch sites, a genuine virtual OnYoshiTryEat call and Sound namespace, direct WrapPicture fields, and additional matching position/timer accesses. It deletes the invented receiver hosts and two artificial volatile member writes. Declared return/pointer contracts now agree with the audited APIs in this owned TU. Unsigned views of the signed timer field are retained where direct arithmetic changes the measured instructions.

The pinned 2004/b56 full object remains 36,744 bytes with SHA256 `10ea92081f073477437f791b9739f83c28763f935b5cce8cd22be8f90eee13d3`. Fresh strict checks verify all58 functions with zero blind relocations/differences. All11 emitted metadata records match canonical relocated bytes including the complete vtable preamble; ordinary data classification is6 VERIFIED/5 PARTIAL. Compiler-only policy, licensed order and tubuild pass, with no unlicensed output. This does not enroll ROM-gap metadata. Header sources are unchanged; the actual include closure is recorded.

The six originally asserted failed sites are now backed by exact variants. Several succeeded and were adopted. CheckTouch's three individual named scalar substitutions each add4 bytes; replacing its calls/arrays also differs. Ordinary ChooseTarget and InitResources tile-fill locals shorten those functions by8 and16 bytes. Those three existing volatile locals remain measured reconstruction constraints, not hardware claims or a universal compiler wall. Remaining curtain/reward storage and ResetBoard views are outside this bounded correction; no whole-class completion or automatically invented follow-up issue is claimed.

## Findings retained

- **LUI2876-01 (provenance)**: fixed_in_owned_source. Removed the inaccurate change-count/member-velocity summary. Original author ancestry and actual source delta are retained; root must use an accurate final PR body.
- **LUI2876-02 (reconstruction)**: bounded_reconstruction_measured. All six asserted sites now have exact candidate patches and pinned results. Adopted StartReward/AddPenalty positions, DrawTimer fields, named timer addresses, and TickTimer scalar access. Full named timer arithmetic and CheckTouch variants differ; measured alternatives are not proof of an absolute wall.
- **LUI2876-03 (correctness)**: owned_repairs_fixed_pending_shared_dependencies. Genuine Sound namespace plus unsigned jingle result, void four-argument render veneer, pointer matrix argument, pointer sprite/table views all preserve the entire object. Keep u16 MultiStore16 calls. Memory task owns the pending four-argument void veneer production/shadow definitions and unsigned16 fill definition. No whole-candidate correctness acceptance until those exact dependencies compose.
- **LUI2876-04 (reconstruction)**: bounded_reconstruction_measured. Removed all six invented PMF receiver families, placeholder virtual slots and WrapPicture shadow, and removed two artificial volatile member writes. Three retained volatile halfword stack locals have actual plain-u16 probes (-8 ChooseTarget, -16 InitResources). Raw curtain/reward storage and ResetBoard layout are explicit remaining scope limits; no invented new issue or whole-class completion claim.
- **LUI2876-05 (provenance)**: root_integration_pending. Original input queue row1/2518 was stale versus2529 lines. This candidate has 2336 source lines; root derives the final affected queue row after composition and corrects inherited parent labeling separately. No producer ledger edit.

## Integration requirements

Memory task `pr2875-source-repair-0920` must land/compose first: it owns both production/shadow definitions of `func_ov004_020b1e34` as void with the four already-emitted arguments, and the unsigned16 `MultiStore16` formal/store correction. These definitions are evidence-supported reconstructions, not uniquely recovered original prototypes. This Luigi stage changes neither callee and must not claim LUI2876-03 finally closed before composition. Root also derives the final queue row; original2518/input2529/final source count remain separate facts.

Local commands: pinned canary via build_pin.verify; `python tools/tubuild.py verify ov006/dScMgLuigi_c`; per-function objisolate+linkcheck; full emitted-data relocation comparison; `check_decl_agreement.py --changed 6300fa4281c9ecd2b0078c3ea6113116cc98049a`; `check_tubuild_conflicts.py ov006/dScMgLuigi_c --list`; `check_dead_references.py`; `port_refcheck.py`; `source_coverage.py --check --base 6300fa4281c9ecd2b0078c3ea6113116cc98049a`; `git diff --check`. Coordinator ignored evidence records exact exits; postcommit credit is measured separately. No full current-main ROM claim is made at this intermediate author-base stage.

Final local static checks pass: declaration scope has no new disagreements (325 inherited scoped findings), port418, exact conflict accounting, references, no coverage loss and diff whitespace. The full experiment contains the commands/results; no inherited finding was banked or waived.
