# Rickshaw axle production composition

Task `pr2873-source-repair-0920`; composition producer `codex-integrator-0920`.
Source offer `2f526b3468a1f888c18df276b544e40961330e6f`; tested main `5ff95cd1ec5f05f3bd56c28edcfeb29e839f2a6a`.
The queue records this exact output commit. This is producer evidence; final
independent Source review and terminal private validation are separate gates.

The source retains the repaired four-function TU unchanged: two real methods and
two C helper entries, 468 bytes at ov002 `[0x020b6a80,0x020b6c54)`. Named model,
collider, resource and actor fields replace raw views where the pinned compiler
preserves the bytes. The angle uses a normal compound update. A widened flag
comparison is retained with exact rejected alternatives in the source experiment.
Both destructors remain separately enrolled; no whole-class completion is claimed.

The common axle class now owns the Resources layout passed by its descendants.
Its model, collision and surface-table pointers agree with both observed caller
records and the shared setup helper. This replaces a duplicate local structure;
all five complete header-consumer objects remain byte-identical. Actor fields,
vtable layout and the inherited opaque helper entry contract are unchanged.
KJ2873-07 records this source-ownership correction. No ratchet baseline is raised.

The Cleanup entry restores its inherited opaque boundary and obtains the real
base actor and inferred three-word resource view locally. Both unchanged derived
callers are verified. This repairs the newly narrowed receiver assertion without
claiming canonical C++ prototype agreement across all callers. No reserved shared
header or descendant source is edited.

Integration preserves four original symbol credits, one existing converted
Render identity and every unrelated main record. Four declaration path keys carry
seven existing fingerprints to the TU; the healed matrix receiver bank stays
removed. Exactly two newly introduced Cleanup caller banks are pruned, retaining
the empty map. The axle promotion row now records three source owners and 167
lines (29 + 33 + 105). Attribution conflict resolution is audited against both
original histories and exact current-main mappings, rather than choosing a side.

KJ2873-01 through KJ2873-07 remain recorded. The source-stage handoff's instruction
to post a broad continuation issue is refined here: these six findings are fixed
or supported by a bounded compiler experiment within the explicit four-function
scope. The broader class remains partial. General future destructor consolidation
and canonical interface work do not become a new deferred finding solely because
they are outside this license; no known requested correction is silently waived.
The optional issue draft remains unposted. Independent final review must still
resolve any additional concrete finding it discovers.

Proof includes the complete 6,608-byte source object, all 12 focused relocation
checks (four licensed, three emitted duplicate, two retained destructor and three
caller bodies), correct output order/policies, and all 253 emitted metadata bytes
across eleven records. The complete vtable includes its eight-byte preamble.
Ordinary metadata reporting remains seven VERIFIED and four PARTIAL: un-emitted
name gap padding is not source coverage. Global inherited symbol/metadata limits
are retained separately from this exact local proof.

See [composition evidence](../../experiments/pr2873-integration-0920.json) for
exact accounting and completed gates, and the preserved
[source experiment](../../experiments/pr2873-source-repair-0920.json) for variants.
Only completed, commit-bound results in those records are claimed.
