# Memory2 source repair handoff

This commit is the reconstruct-stage checkpoint for PR #2875, not independent or current-main acceptance.

- Task `pr2875-source-repair-0920`; input `e8599a26b2bd3f4d6873c088bc0ce4265b790207`; author parent `4679d2a9a319abc7c5b43a04211a0e6b877d471e`.
- Producer `codex-memory2875-repair-0920`, actual child `/root/repair2747`. The same child performed preflight before switching explicitly to producer; another agent must verify.
- Coordinator `codex-integrator-0920` owns composition, ledger rows, publication and merge order. Workflow pin `18af52b49b9fb5663d417bdfa86109b7b157aaa3`.
- Initial owned paths: Memory2 TU/manifest, this handoff and [durable experiments](../../experiments/pr2875-source-repair-0920.json). Explicit pre-claim amendments also own the production/shadow drawing veneer and MultiStore16 definition. No shared header, external caller, enrollment, symbol, profile or global ledger edits.

Original author improvements are preserved. The repair adds genuine Sound namespace calls and correct unsigned return contracts. Further byte-identical forms use the real player/card fields in PlayerDrop, JudgePair and CardMove, real card indexing in UpdateCards/ResultTurnCards, and additional scalar members. Gameplay intent stays in the source; exact alternatives and historical claims stay in the experiment.

The source object remains27,096 bytes, SHA256 `7e9a029d4cca1ad4906e6f4d755107180143af14fa02dc38dd7efa4dc568be3e`. Fresh strict52 checks have zero blind relocations/differences. All13 emitted metadata records match canonical relocated storage, including the152-byte full vtable preamble; ordinary metadata is7 VERIFIED/6 PARTIAL. Compiler-only policy, full licensed output/order and tubuild pass. Metadata is still ROM-gap owned, not newly enrolled.

The production drawing veneer and its shadow definition now consistently accept receiver plus three unused ints, preserving all15 production callers' four-register setup. This is an evidence-supported reconstructed interface, not unique original arity. The veneer target/body/callers are unchanged. Fresh complete production/shadow object identities and all41 shadow functions plus the16-byte production veneer verify strictly. MultiStore16 uses an unsigned short formal and store for raw16-bit fill; complete object and24-byte strict proof are unchanged. Its full72-file population,49 unsigned16/20 legacy-int declarations and48 healed/zero new declaration fingerprints are recorded. No caller is rewritten merely to silence a checker.

All eleven retained-method claims were actually tested. Several were false in general and matching forms were adopted. Failed pointer/indexed forms and the ordinary factory96-to64-byte result are preserved with exact patches/objects/instruction differences. Mechanical probe-generation failures are labeled and retested; they are not compiler constraints. Existing layout views and the literal factory remain an explicit partial source boundary, without a whole-class completion claim or automatically invented issue.

## Findings retained

- **MEM2875-01 (provenance)**: fixed_in_owned_source. The inaccurate eight-alias/new-mPlayers summary and broad count-based audit narration are removed. Original author ancestry and named-field improvements remain; coordinator must use the accurate final PR body.
- **MEM2875-02 (reconstruction)**: bounded_reconstruction_measured. All eleven listed methods have exact ordinary-form patches and pinned outcomes. Real PlayerDrop/JudgePair/CardMove fields, UpdateCards/ResultTurnCards indexing and several scalar accesses match and are adopted. Failed pointer/indexed loop alternatives are preserved; no claim that all ordinary forms are impossible.
- **MEM2875-03 (correctness)**: fixed_in_owned_source_and_shared_dependencies. Uses the actual Sound namespace/unsigned result and unsigned jingle contract. Both shared veneer definitions consistently accept the four retail argument registers with unused trailing formals. MultiStore16 defines the unsigned16 bit-pattern fill supported by caller setup/storage semantics. Every changed complete object and all95 relevant function instances remain exact; independent final review still required.
- **MEM2875-04 (provenance)**: fixed_provenance. Destructor prose now states automatic mShared then base destruction. The inherited factory-unavailability claim was also tested and disproved: ordinary new compiles but changes factory96 to64 bytes and fails strict verification. Current prose records only that bounded negative result; source bridge remains explicit.
- **MEM2875-05 (provenance)**: root_integration_pending. Original queue row1/1683 matched author parent; author input grew to1693 lines. Final producer TU has1551 lines. Root owns deriving only the final affected row; no global metadata edit here.

## Integration requirements

Compose this repair before Luigi's offered `b8fb4e4ef4474a779acc05c9d921b4fa568e4e84` so its shared veneer/fill dependencies are present. Root derives the final Memory2 queue row and preserves original1683/input1693/final counts as distinct facts. All original legacy-source and compiler-only policies remain unchanged. No attribution or converted-count promotion is claimed.

Commands: pinned build_pin canary; `tubuild.py verify ov006/dScMgMemory2_c`; all52+43 strict objisolate/linkcheck instances; complete metadata relocation comparison; `check_decl_agreement.py --changed e8599a26b2bd3f4d6873c088bc0ce4265b790207`; `check_tubuild_conflicts.py ov006/dScMgMemory2_c --list`; `check_dead_references.py`; `port_refcheck.py`; `source_coverage.py --check --base e8599a26b2bd3f4d6873c088bc0ce4265b790207`; `git diff --check`. Exact final static exits and credit are recorded separately by the producer before offering. Full current-main ROM proof remains for the coordinator stage.

Final static checks pass:244 declaration-scope files/1857 inherited findings with zero new disagreements, port418, exact conflict accounting, references, unchanged source coverage and whitespace. No declaration baseline was edited or new contradiction banked. PlayerDrop uses the existing three-entry player view; the unchanged InitPlayers raw cap-five overlap and other inherited layout views are not declared fully reconstructed.
