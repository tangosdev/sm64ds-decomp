# Handoff: pr2744-repair-0918

This document describes this commit. The coordinator records its immutable output SHA.

## Identity and scope

- PR: https://github.com/tangosdev/sm64ds-decomp/pull/2744.
- Task/stage: `pr2744-repair-0918` / `repair`, producer.
- Session alias: `codex-repair-number-0918`; actual Codex child `/root/repair2747`,
  previously also `codex-repair-2747-0918`, `codex-attribution-tools-0918` and
  `codex-repair-polelift-0918`. Earlier producer/preflight work is not independent review.
- Branch/worktree: `repair/pr2744-0918`, `C:/tmp/sm64ds-r2744-0918`.
- Exact input: `5add4b136b53f98b1fa040adec1db9b9cb8dc43f`; task comparison base: `4e844d388f0d37e688ebf9899a7d532dff9d7146`.
- Task workflow pin: `162d1796ff405da01b39d21ad753ab40aac71acb`.
- Status: local producer candidate, `formal_acceptance: false`. No queue publication,
  push, issue creation, external comment or merge performed by this producer.
- Class/ranges: Number/daObjNumber_c; ov002 `0x020f0894..0x020f0dd0` and
  arm9 SpawnNumber `0x02010044..0x020100dc`.

The claim owns the Number TU, its header and manifest, SpawnNumber, this handoff,
and `notes/experiments/number-2744-source-forms.md`. Its atomic expansion also owns
the exact Number edits in `notes/data/tu-merge-candidates.json`,
`notes/data/c-cpp-classification.tsv`, `notes/plan-tu-merge-queue.md`,
`symbols/actor_renames_report.txt`, `notes/cpp-conversion-enemies.md`, and
`notes/actor-leaf-provenance.md`. No global attribution, converted counter,
declaration baseline, enrollment or other shared header is edited.

## Changes and finding dispositions

| Finding | Producer disposition | Evidence |
| --- | --- | --- |
| NUMBER-2744-01 | Repaired | Particle NewSimple bridge returns void*, agreeing with its definition; Behavior strictly verifies. |
| NUMBER-2744-02 | Repaired locally | SetFile bridge takes TextureSequence*, BTP_File*, s32, s32, u16. The last type agrees with the implemented callee. The contradictory shared header remains separately scoped. |
| NUMBER-2744-03 | Repaired | Attachment helper and sole caller consistently use dActor_c*(dActor_c*, dActor_c*); both owner/null-owner paths return the popup. The helper's 36 bytes and SpawnNumber's 152 bytes strictly verify. |
| NUMBER-2744-04 | Repaired | Six legacy paths restored; old original failed 7/8 evidence retained under a historical snapshot; current proof separate. Five historical inventories/transcripts restored without inventing renamed past inputs, and current leaf provenance names Vector3 fields. Fresh boundary is seven class-labelled functions plus the anonymous adjacent factory, medium confidence. |
| NUMBER-2744-05 | Partly repaired; remaining work unresolved pending independent disposition | Both volatile reads, integer-pointer barriers and NumberPos shadow removed. Ordinary updates, real Vector3 storage and named PowerStar field match. Five precise remaining alternatives fail with measured sizes in the experiment note. Position views, SharedFilePtr+4, two scalar bridges and the PowerStar halfword remain. |

The remaining work has a bounded issue draft under coordinator review, next owner
`codex-integrator-0918`. Issue publication approval is pending. There is no accepted
issue-backed deferral and no self-acceptance. Findings 01-03 are not deferred.

Source comments now explain popup behavior and the specific retained constraints.
Experiments are in [the owned note](../../experiments/number-2744-source-forms.md),
with exact final sources, compiler invocation and machine-readable result recipe.

## Object ownership and credit

The eight Number functions still occupy 1340 bytes: six compiler-spelled class
method entries including D1/D0, the utility and the factory. No assembly or draft
is promoted. The inline destructor/key function own natural RTTI/vtable emission.
The ordinary Vector3 member and local cause a four-byte weak D1 duplicate;
`deadstrip-duplicate` explicitly licenses only its existing arm9 home 0x020072c0.
Independent strict checking proves body `1eff2fe1`, zero diffs/blind slots. Full
rombuild enforces this policy before discarding the duplicate. The matched count
and Number enrollment do not gain a function.

The existing eight path#symbol credits are retained (six tangosdev, two ruspecial).
Original contributor history is preserved. Accepted attribution tooling at
`c1dc8be2d142e0d617d994a89ef1b7e51342ce71` is the required same-basename promotion
validator; the source snapshot contains its older false-positive implementation.
Post-commit credit validation is recorded separately by the producer/coordinator.

## Validation

All commands run in the wired producer worktree. Logs are under
`build/number-probes/`; the toolchain setup canary returned `(True, '2004/b56')`.

| Command | Result / artifact |
| --- | --- |
| `python tools/tubuild.py verify ov002/daObjNumber_c` | 8/8 MATCH; objisolate, order and destinations clean; explicit Vector3 duplicate policy. `tubuild-verify.log`, fresh manifest verification. |
| `python tools/prepush_linkcheck.py --files src/game/actors/d_a_obj_number.cpp src/_ZN8dActor_c11SpawnNumberERK7Vector3jbtPS_.cpp --json build/number-probes/final-linkcheck.json` | 9/9 VERIFIED, no warnings/blockers, diffs=[] and blind=0. |
| Strict isolated `_ZN7Vector3D1Ev` from the actual TU object, arm9:0x020072c0, size4 | VERIFIED, `vector3-duplicate.json`; object SHA-256 faaf4b003ffb07afde4023c61079fb26b418f7ef0fb16301cc196c2b5d8791ef. |
| `python tools/romdata_check.py --files src/game/actors/d_a_obj_number.cpp --json build/number-probes/final-romdata.json` | 6 verified/184 bytes, 3 partial/24 bytes, 0 differing, 0 unnamed. Own RTTI12/name16/vtable124 all verified. |
| `python tools/affected_src.py include/daObjNumber_c.h` | Exactly the Number TU and SpawnNumber; both checked above and in full ROM. |
| `python tools/check_decl_agreement.py --changed 4e844d388f0d37e688ebf9899a7d532dff9d7146` | No new disagreements, `final-decl.log`; baseline untouched. |
| `python tools/check_decl_agreement.py --symbol func_ov002_020f0918` | Both declarations and definition agree, zero disagreements; `attachment-contract.log`. |
| `python tools/port_refcheck.py` | All 423 references resolve. |
| `python tools/check_dead_references.py` | No new dead references or broken links; no baseline edits. `dead-references.log`. |
| `python tools/rombuild.py -j16 --report-json build/number-probes/final-rombuild.json --data-json build/number-probes/full-romdata.json` | PASS, 106/106 modules exact, 11,206 source functions reproduce/0 mismatch, 26 data claims and 3 BSS claims pass. |
| `python tools/rtti_extract.py --out build/rtti.json`, `python tools/rtti_vtables.py --out build/rtti_vtables.json`, `python tools/tu_map.py --out build/number-probes/tu-map.json` | Fresh current boundary observation retained separately from history. |
| Final experiment reproduction in owned note | Control 8/8; five alternatives each retain seven matches and produce the documented affected-function mismatch. `final-probes.json`. |

Full ROM: 16,777,216 bytes, SHA-256
`d1506e90efae5e2d2cf119926a4ac2a291bd5ca78349d09d5024e1a918c478e8`,
identical to the configured stock artifact. The scratch baseline has nine
pre-existing symbol errors; production has zero new errors. Whole-tree emitted-data
diagnostics remain 720 verified, 232 partial, 4 differing, 367 unnamed; this is not
a claim that all inherited metadata debt is repaired. Number has zero differing
or unnamed data. The source repair does not alter those unrelated compiler inputs.

Current-main composition, private validation of the published head and independent
source acceptance are coordinator/verifier work still required. The local ROM pass
does not accept the retained reconstruction debt or establish a live-base merge gate.
