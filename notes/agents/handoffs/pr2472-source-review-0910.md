# daBar_c review repairs — 2026-09-10

This repairs PR #2472 at input `4131f9a18a947f253f15240869f45d3831f9858b`.
Its actual common source base is `c1c8846df622f3a49b6b107e0845e9929473d059`;
the cached GitHub base is not an ancestor. Final current-main composition,
generated queue counts and private validation remain separate acceptance steps.

The factory keeps natural `new daBar_c`, with the local allocator forwarding to
the actor heap. Its inlined implicit constructor **calls** `dActor_c` C2 and
`dCcAc_c` C1; the manifest no longer says those constructor bodies are inlined.
The profile stores unsigned behavior/render priorities at +4/+6, as the
`fBase_c` constructor reads them. Actor ID comes from the spawn global, not
these fields. The 28-byte profile object is unchanged.

The source retains concise actor behavior and reconstructed-name provenance.
The process label and broad typed-caller claim were removed. Local adapters
remain explicit; no mangled declarations were added to shared actor/collision
headers. `affected_src.py include/daBar_c.h` checks the actual consumer scope.

## Findings carried from the prior PR reviews

| ID | Disposition | Result |
| --- | --- | --- |
| BAR-01 | Fixed | Shared-header conflicts remain resolved; this repair edits only the Pole header. |
| BAR-02 | Fixed | No general caller-stack or unavailable-interface claim remains. |
| BAR-03 | Fixed | Descriptor fields are unsigned priorities, not packed actor IDs. |
| BAR-04 | Fixed | Factory/profile spellings remain explicitly reconstructed. |
| BAR-05 | Fixed | Height is `10 * ((param1 & 0xff) - 10)`, clamped to one unit. |
| BAR-06 | Fixed | Constructor calls are distinguished from inlined constructor bodies. |
| BAR-07 | Partial | Remaining clipping/collision interface reconstruction is owned by @andrewboudreau in #2562. |

## Measured proof

Pinned compiler: `2004/b56`, using `build_pin.flags_for` for the production TU.
All eight functions match: 376 text bytes, 16 module-qualified references,
zero blind or differing words. Emitted metadata is six VERIFIED and four
PARTIAL records, with no differences. The complete emitted object before/after
is identical: SHA-256 `b7eae01d6d901a13ddf10f61c5403e29feb839ec8334f60e3b63e70be5833c0f`.
All ten emitted data objects, including the 28-byte profile, are unchanged.

Three ordinary caller replacements for `InitClsn` were tested against
`daBar_c::InitResources`, ov002 `0x020b067c`, retail size `0x94`:

| Real `mClsn.Init(this, radius, height, flags, 0)` argument form | Emitted size | Result |
| --- | --- | --- |
| Named `Fix12<int>` aggregates initialized with braces | `0xac` | Wrong size; one unresolved relocation |
| Direct `(Fix12<int>){value}` compound values | `0xa0` | Wrong size; one unresolved relocation |
| Named `Fix12<int>` values assigned through `.val` | `0xa8` | Wrong bytes/size; zero unresolved relocations |

Each replacement retained the original flags expression and all other source.
These are bounded failed experiments, not proof that every typed caller is
impossible. They are excluded from the production TU. The exact scalar
adapter remains while #2562 tracks the wider interface work.

Local artifacts: `build/pole-baseline/`, `build/pole-candidate/`, and
`build/pole-probes/`, generated by `build/prove-pole.py` and
`build/probe-pole-init.py`. ROM/compiler inputs and objects are not committed.
