# StarCamera and Heyho adapter production packaging repair

This commit composes original PR #3195 candidate `56df821f7174e64f6d5591190e648623e212d974` with main `b5ac7a82089355259c6c9bd05a4b7e44b81b935d`. The original two function bodies are unchanged; one production source owns each existing matched function. `StarCamera` is the existing profile-derived label, not a recovered most-derived class identity or original single-function TU boundary.

## Ownership and repair scope

Task `pr3195-source-repair-0926`; producer `codex-pr3195-repair-0926`. The user explicitly extended the narrow shared-file repair exception to this PR. Existing reservations on attribution, the converted baseline, promotion ledger and ov006 delinks are preserved, as are every unrelated main entry and all leases. Attribution is a recursive three-way identity merge; both migrated functions retain tangosdev credit. The single converted identity is replayed onto main. Both `already_promoted` fields are updated, and the pilot note points at the current production source.

The ov006 delinks composition preserves merged Tamaire PR3196 at mainb5ac7a8208. No Tamaire owner or verifier task was taken over. Both new credit overrides are placed next to the existing legacy Heyho credit pair, preserving all unrelated key ordering; the single-function converted identity remains at its retired identity list position.

## Source acceptance and remaining work

Completion is **partial**. Independent source review is required for this exact commit and composed base before integration. No producer byte pass is a source acceptance claim.

[Follow-up issue #3198](https://github.com/tangosdev/sm64ds-decomp/issues/3198), continuation owner `codex-integrate-open-0926`:

- SC-RECON-01: resolve STAR_CAMERA's four extra allocation bytes and most-derived identity. Allocation is 0xd4 versus dActor_c size0xd0; only base constructor/vptr is observed.
- SC-RECON-02: reconstruct natural C++ allocation/construction after pinned experiments. Retained mangled extern C calls are deferred work, not a measured compiler constraint.
- SC-RECON-03: recover original TU boundaries independently from this production packaging; the pilot places the factory in a broader Star-family run.

The Heyho adapter is the RTTI-named base of dMgJump3DMario_c. Its source comment distinguishes the unlicensed C1 emission from vtable/type-name/RTTI copies compared against ROM then discarded by text isolation. No source body or object-layout change is made.

## Independent review repair

SC-PROV-02 (fixed): the Heyho adapter manifest previously called its 8-byte RTTI record single-inheritance RTTI. The class has no base; ov0060x0213b000 relocates to arm9 `_ZTVN3abi17__class_type_infoE` at0x0209a774. The explanation now names a plain class RTTI record. Payload, canonical address and source bodies are unchanged. Root reviewer identified this on intermediate candidate5135f31; the repaired commit requires independent recheck.

## Evidence and handoff

Before repair, a fresh wired check of exact original candidate56df821 passed11214/11214 functions and106/106 modules. Its baseline also passed106/106, and direct examination of func02071844's pool found0x020737a8 and0x02073a24. The previously reported universal baseline blocker did not reproduce. That older candidate result is historical, not proof of this new composition.

The intermediate composition `e9223f72362a374c548e2a45ed4dd17e9b81fd7f` against maina0f2 also passed all local gates, including11214/11214 functions and106/106 modules. It was not published or merged.

Current-composition gates and exact output SHA are recorded in the queue handoff evidence and PR body after they finish. Required gates include both focused TU checks, complete ROM and affected relocation checks, attribution, declarations, dead references, promotion ledger, port references and ratchets. The independent verifier must inspect all changed and retired source/config paths and the shared bookkeeping resolution.

## Single-symbol path repair

SC-PATH-01 (fixed): public tree-shape CI rejected the class-only filenames for these one-function owners. Existing srcpath.rename_target derives src/actors/StarCamera_Spawn.cpp and src/actors/_ZN22dMg3DHeyhoObjAdapter_cC2Ev.cpp. Both use the enrolled symbol basename, preserve the original actors directory, and make no new class identity or original TU-name claim. Source bytes are identical to bf49 at the new paths. Manifests, delinks, both credit keys, the converted identity and pilot link follow the rename. No resolver/checker code or rule was weakened.

The full tree-shape suite (tools.test_srcpath and tools.test_bytegate) is now an explicit acceptance gate for this repair. Earlier bf49 byte/ROM/source review evidence is historical until the repaired exact head is independently rechecked.
