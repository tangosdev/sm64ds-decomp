# Star switch PR2874 source repair

Producer `codex-switch-source-0920`; input `d037ba5c77008badad073a4b23f9bda3fad9897e`, original base `2705c624d2554ee8d21a56641275a2b85cce3a8b`. Task `pr2874-source-repair-0920`, reconstruct stage. This is a bounded source offer for root composition, not independent Source acceptance or merge readiness.

The TU owns ov002 `[0x020b9e64,0x020baa2c)`:20 functions,3016 text bytes. Five named methods and13 C-linkage helpers are written; D1/D0 come from the genuine inline destructor. Callback, factory and initializer bodies outside that range remain separately owned. Existing author and StarSwitch rename ancestry are preserved. Changes are the owned TU/header/manifest, sound wrapper, amended StarMarker declaration and these two evidence files; no global ledgers, shared headers, caller bodies or enrollment edits.

The actor now uses real model rendering with a Vector3 scale, a class-owned resource descriptor and PMF state table, named switch fields, native model loading/SetFile, collider Transform, FindWithID/FindWithActorID, SpawnSoundObj, UpdateModelPosAndRotY and Sound::PlayBank3. Putting the existing common.h first makes ordinary matrix assignment exact; no fabricated matrix host/copy type remains. Field/resource/state labels describe observed use rather than recovered English names.

The transition and its state4 tail caller return void, as the actual void PMF dispatch requires. The sound wrapper returns the actual callee's int handle; the star-switch and unchanged BlueCoin-switch consume it. StarMarker now names the same shared resource as SharedFilePtr after the coordinator amended its exact file/range reservation; whole-object identity proves no call-site code change.

## Retained findings and limits

- **SS2874-01/02 fixed:** truthful state/callee/callback/result contracts with fresh byte proof. Shared scalar free definitions remain explicit; the unchanged Fix12 class declarations are not falsely described as canonical agreement.
- **SS2874-03 compiler_constraint:** remove fake Base/C/Obj/MMC, raw own-actor offsets and local matrix-copy type; adopt matching native forms. Genuine collider SetFile using Fix12<int> grows InitResources1f0 -> 1f4 and differs43 words. Direct boolean tests shrink six bodies (exact sizes in experiment); retain their int materialization. The nested Matrix4x3 failure is superseded by a matching genuine shared-flat assignment, not a final wall. The unnamed foreign PowerStar+438 field remains an explicit external view; other star reads use existing members. No foreign layout is invented.
- **SS2874-04/05 fixed:** false shadow/recovered-Kill/destructor claims removed. Exactly18 written-body markers; destructor score fragments come from the actual inline header. All emitted metadata bytes and D1/D0 order are checked. Manifest retains all12 assembly-conflict records as resolved with their original identities.
- **SS2874-06 integration pending:** root restores OnGroundPounded to original tangosdev credit and preserves all20 address identities/four converted entries. Producer leaves reserved attribution/baselines untouched.
- **SS2874-07 integration pending:** actual run ends020baa2c. Authoritative current source count is1 TU/418 lines; root updates only the final class queue row and PR body after composition.
- **SS2874-08 fixed:** StarMarker resource declaration now agrees with the actual SharedFilePtr interface; no mismatch was banked.

No additional deferred reconstruction finding is asserted merely for the retained13 address-named helpers or the foreign star field outside this scope. This does not claim all helpers have become genuine named methods or all game interfaces are reconstructed. Independent source review may require a narrower follow-up; no issue was posted by this producer.

## Producer proof

Durable experiments and exact patches: `notes/experiments/pr2874-source-repair-0920.json`. Pinned2004/b56. Complete final Switch object:12840 bytes, SHA256 `270df825279819f8b9821105d218dce1adc831b98900e169d858d42d4eeab4ba`. All canonical linked text equals original control; allowed resource relocation spellings now identify the coherent resource table.

- **38 strict checks:**20 licensed functions +2 unchanged BigBrick header consumers +3 sound control/final/callee checks +9 unchanged BlueCoin TU bodies +3 callback/setter/PMF-initializer controls +1 changed StarMarker body. Every check VERIFIED, zero differences and zero blind words. Both header-consumer raw objects remain identical to original-header controls. The StarMarker minimal change preserves its complete raw object as well.
- **Complete metadata:**11 objects/249 emitted bytes equal retail. Includes136-byte vtable storage at02109938 with both preamble words before public02109940. Leaf name has15 characters plus NUL,16 bytes; no emitted padding. Inherited names emit9/9/10/13 bytes; additional3/3/2/3 zero retail gap bytes are not emitted. Standard word report remains7 VERIFIED/4 PARTIAL/0 DIFFERS.
- **TU policy/order:** all20 declared/defined bodies, canonical relocation destinations and retail D1/D0 order pass. All11 explicit compiler-only data policies accepted; zero unlicensed functions/objects/sections. No data/BSS production ownership added. Source contains no fake lifecycle marker regions.
- **Static checks:** conflicts resolved12/12, port refs418/418, converted ratchet2973 preserved; all four original converted members score true from actual body fragments. Scoped declaration check at old input base exposes only the three original Player diagnostics already recorded in preflight; the coordinator reports these have accepted current-main baseline entries. No producer bank edits or blanket declaration PASS. The new StarMarker disagreement is closed.

Language-mode control against exact original input shows local-structure files1578 ->1577 and padding-layout files379 ->378, with every other tracked counter unchanged. The newer chaos-data baseline flags extern-vtable105 vs104 and class106 vs105 on both original and candidate; no override was added. Root must rerun on actual current main.

## Root continuation

Compose on actual main after preceding integrations. Preserve every unrelated reservation and row. Reconcile only authorized original-PR declaration path moves and now-stale fingerprints, all20 tangosdev credits and four converted identities; update the one source-count row from final committed source. Include both paths of original renames/deletions and original shared-header/config changes in formal review coverage. Run the committed current-base declaration gate, full ROM, expanded header consumer/prepush checks and terminal private validation, then independent exact-head Source review. This producer has not pushed the PR branch, merged, posted an issue, edited main or asserted current-main/full-ROM/private-CI acceptance.
