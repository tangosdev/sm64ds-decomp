# Actor fields and ApplyOpacity integration

Task `pr2878-contract-repair-0920`; producer/integrator `codex-integrator-0920`. Original PR #2878 head `502556595825c05a740d505c98c1015d6f548fae` remains in ancestry. Source anchor `8084e4ec7f20ef2a77ccdd36cc53baf77ec0c192` includes actual main `5c075024562a469c6e4336d8540e3dfa9ffa84a1`. This evidence-only successor changes no compiler input. Independent final acceptance and private PR validation remain pending.

Chair, Goomba, Mip and Monkey retain the original named-field cleanups across 125 functions. The repair corrects actual return/argument contracts for sound, player, collision, particle and arithmetic helpers. The particle handle uses its integer member. Calls use genuine existing C++ interfaces where matching; retained bridges identify actual definitions and measured reconstruction limits.

ApplyOpacity now has a coherent two-u32 interface across its declaration, definition, native calls, existing bridges, canonical symbol and ARM9 enrollment. Twenty-five retail call sites set the second argument to zero (15) or one (10). This supports the reconstructed interface without claiming unique original arity or spelling. The former one-u32 call removes a retail register setup and shortens Mip's reflection rendering by four bytes. The unused second formal is unnamed, preserving every existing generated-local label. Four briefly changed manifest policies are restored exactly to main; no overlay configuration change survives.

The user approved the exact API patch, including the two reserved ARM9 entries. Existing Decimal/attribution/declaration owners retain all branches and reservations. Original credit is preserved, 35 stale declaration fingerprints are removed with none added, and the converted baseline remains 2973. The production opacity forwarder covers its complete 84-byte range. Shadow copies remain shadow.

Independent source proof covers 27 complete objects and 273 strict function instances, with zero byte differences or blind relocations in that focused set. Only the canonical ApplyOpacity symbol spelling is normalized for object comparison. All 42 emitted actor metadata records match completely; the ordinary report remains 22 VERIFIED /20 PARTIAL.

The full header-consumer population contains 1,393 files and 3,674 function instances, checked in six complete disjoint partitions. There are 3,671 VERIFIED and three initializer warnings (BLIND-3,BLIND-3,BLIND-6), with no differing bytes. Fresh independent main/candidate controls reproduce these three warnings identically, including complete objects and every relocation target. They concern inherited local constructors/destructors and global-object registration. Full production linking separately passes: 106/106 modules and 11208 source functions reproduce the stock ROM, SHA256 `d1506e90efae5e2d2cf119926a4ac2a291bd5ca78349d09d5024e1a918c478e8`. Thirteen static gates plus eligibility/reference checks pass.

DA2878-03/04 are corrected within the reviewed contract scope. DA2878-01's original field improvements remain proved; DA2878-02 records inherited reconstruction boundaries rather than whole-class acceptance. The old checkpoint with ApplyOpacity open is preserved inside the experiment history and superseded by this measured repair. Named-form failed attempts are not final evidence. Native u32 animation/shadow views versus legacy narrower free definitions remain explicitly unresolved original-width questions, with no demonstrated failure at the actual constant calls.

See [durable proof](../../experiments/pr2878-contract-repair-0920.json) for exact contracts, all current/historical findings, commands, object comparisons, full-ROM accounting and warning controls. Root remains the sole merger and must obtain independent exact-head review and actual terminal private checks before landing.
