# Actor contract repair checkpoint

Source checkpoint: `65682b7d8ee85f608660614699c315f225d0e2f8`; original PR #2878 head: `502556595825c05a740d505c98c1015d6f548fae`.
Producer: `codex-integrator-0920`. Formal acceptance remains false.

The Mip, Monkey and Goomba files use truthful sound, player, collision, particle
handle and arithmetic-helper result types. The integer particle handle now uses
its real member assignment. All three complete compiler objects equal the
original controls; all111 licensed functions freshly compile and pass strict
relocation checks with zero differing or blind words under2004/b56.
The experiment JSON records every exact replacement, result and object hash.

DA2878-03 is locally repaired. DA2878-04 remains open for ApplyOpacity: using
the current one-u32 genuine method removes the retail third-register setup and
shrinks RenderMirrorImage by4bytes. No padding, fake wrapper or type erasure is
introduced to conceal it. A coherent shared API proposal must account for the
reconstructed name, reserved canonical ARM9 symbols and enrollment and all consumers.
The other changes do not authorize editing those reservations.

This checkpoint is local source progress, not current-main or full-ROM acceptance.
The producer task remains running while the shared dependency is resolved.
Original authorship and all four author field-cleanup files are preserved.
