# Handoff: minigame-mcarlo2-readable-0922

This document describes this commit. The queue records the immutable output when the integrator authorizes publication. This is a local producer candidate, not independent source acceptance.

## Identity and scope

- Issue: https://github.com/tangosdev/sm64ds-decomp/issues/2910 (partial reconstruction; remains open).
- Task/stage: `minigame-mcarlo2-readable-0922` / `produce`; producer session `codex-mcarlo2-readable-0922`, Codex.
- Branch: `cleanup/minigame-mcarlo2-readable-0922`; wired worktree `C:/tmp/sm64ds-mg-mcarlo2read0922`.
- Original input/source base and task workflow pin: `0bc0d1c2df4fba865dd48841039d1446f2a105c4`.
- Current composed main and installed tools: `0a609664d09c0377f8680d464a84fd0e6e75e0f4`, merged through `0ff7a9b8301c7e5e9b537afe8fb037ef901a364d`. The source cleanup is unchanged from local checkpoint `89c6c63926766efd0962e12b377af109326d9f44`; retain that patch identity for aggregate composition.
- Reserved classes: `dScMgMCarlo2_c` and `dMgMCarlo2CardObj_c`. Source: `src/minigames/d_s_mg_m_carlo2.cpp`; ov006 text `[0x020f8ef4, 0x020fa75c)`, all 25 manifest functions. The only other tracked change is this handoff. No shared header, manifest, config, symbol or attribution changes.
- Next owner: the parent minigame coordinator assigns independent source/byte verification, then composes the accepted change. The coordinator authorized publishing this exact producer candidate to the queue for verification. Source-branch push, PR publication and merge still await independent review and the integrator.
- Local proof and failed experiment snapshots live under `build/mcarlo2-readability/`; private receipts remain ignored. No ROM/compiler inputs or proof binaries are committed.

## First-pass changes

Read the complete TU, its class and inherited layouts, manifest, and the relevant local callee definitions. Card parameters and traversal locals now distinguish slots, row/column distances, the selected pair, list tails, saved links, sprite frames and weight rows. These are descriptive names inferred from the existing operations, not recovered original identifiers. `Update(int slot)` consumes the traversal ordinal passed by `UpdateBoard`; the shared header's old parameter label `event` is outside this source-only scope.

Use existing `mPromptBlinkCount`, `mPromptEnabled`, `mPromptBlinkTimer`, scene state/timer fields, `mShared` and the declared two-dimensional weight array. Replace the card's pointer/cast decrement with `--mDealDelay`, remove the scalar-as-array expression, and use direct scene state increments. Keep fixed-width locals, evaluation order, list traversal, volatile difficulty/next-link reads, MMIO volatility and destructor placement.

Six local declarations now follow existing definitions: `RandomIntInternal(int*)` (`src/RandomIntInternal.c`), the void return of `RenderOamMainScreen` (`src/RenderOamMainScreen.cpp`), pointer parameters of `DecompressLZ16` (`src/DecompressLZ16.c`), the no-argument scene-global cleanup helper (`src/func_ov004_020ad90c.c`), unsigned result of `Sound::PlayBank2_2D` (`src/_ZN5Sound12PlayBank2_2DEj.cpp`), and unsigned lengths/function-pointer callbacks for `__cxa_vec_ctor` (`src/__cxa_vec_ctor.cpp`). No callee was changed. The camera helper keeps an explicit scene-base byte offset: inherited `pad_4660[0xa0]` covers only the beginning of its 0xbc-byte camera, so passing the padding array would misleadingly suggest a complete subobject. The shared-state counter argument to `func_ov006_020c1604` remains an address; this pass does not infer a value parameter from its legacy scalar spelling elsewhere.

## Findings and measured experiments

- `MCARLO2-READ-01` - fixed: raw member accesses and register/list-index locals in the scene and card methods were replaced as above. The entire unisolated object is unchanged.
- `MCARLO2-DECL-01` - fixed: the six local declaration inconsistencies above were corrected with definition evidence and unchanged emitted output. The ratchet still reports seven pre-existing disagreements in the scoped population; this is not a claim that all inherited/local interfaces have been reconstructed.
- `MCARLO2-CODEGEN-01` - measured constraint, retained: rewriting `BoardReady` to early returns grows its function from `0x78` to `0x80` and changes relocations/branches. The original single-result/single-exit form remains. Snapshot/object/report: `board-ready-early-returns.{cpp,o,json}`. Reproduce by replacing the success assignment/return with successive guards for unequal dealt/visible counts, nonzero animation count, and two selected cards, then returning 1.
- `MCARLO2-CODEGEN-02` - measured constraint, retained: combining the two materialized touch tests in `OnTurnIntoEgg` into `A && B` grows `0x104` to `0x108` in the tested typed source. The old source comment's three-instruction-short observation described a different source context and is superseded here by the saved `touch-fused-predicate.{cpp,o,json}` measurement. The nested test remains. An ignored copy of the exact final source reconfirmed the same result in `touch-fused-final.{cpp,o,json}`.
- `MCARLO2-CODEGEN-03` - resolved: the old state-increment pointer workaround is unnecessary in this typed TU. The first increment experiment and the complete five direct increments both produce the identical object; `behavior-direct-increment.*`, `native-increments.*`, and `final-source.*` preserve the measurements.
- `MCARLO2-RECON-01` - remaining scoped work, offered for independent review as partial under issue #2910; next owner is the minigame reconstruction coordinator. The factory still has explicit ABI construction calls/vptr stores, shared state and inherited camera are partly opaque, many globals/tail fields retain provisional names, and two volatile list/weight access forms remain. This cleanup does not assert these are all measured compiler barriers or recover an original filename. A later header-owning task may correct the stale `event` parameter label and investigate the remaining layouts/interfaces.

## Proof and limits

The baseline was compiled before source edits, directly from the pinned input. `compile_probe.py baseline` and `compile_probe.py final-source` use `match.compile_c` with `build_pin.flags_for` / production C++ flags and compiler `2004/b56`, without object isolation.

- Full raw objects are identical: 21,360 bytes, SHA-256 `e39b643b93e08d9bc6760d72918c6b8674b0bd94c7e490e09bf8f40c8692827a`. All 44 allocated sections, 236 relocation records, and the complete defined-symbol inventory agree. There are 28 positive-size function definitions across all bindings, including both scene destructors with `STB_LOPROC`. The three extra card C2/D2/D0 variants are unchanged and already licensed for dead stripping by the unchanged manifest; no new passenger is introduced.
- `strict_all.py` invokes normal `linkcheck.linkcheck` source preparation for all 25 manifest functions: **25 VERIFIED**, every `diffs: []`, every `blind: 0`; `strict-all.json`. `python tools/prepush_linkcheck.py --files src/minigames/d_s_mg_m_carlo2.cpp --json build/mcarlo2-readability/prepush-files.json`: exit 0, **25/25**, zero warnings/blocking.
- A preliminary direct raw-object `linkcheck` invocation omitted normal vtable relocation preparation and reported five functions WRONG at vtable references (`strict-unprepared-raw.json`). It is not the production gate or a source failure; rerunning through the tool's normal source path gave the strict result above. Raw object equivalence is recorded separately rather than using that incomplete harness as relocation proof.
- `python tools/romdata_check.py --files src/minigames/d_s_mg_m_carlo2.cpp --json build/mcarlo2-readability/romdata.json`: exit 0; inspect verdicts, not exit alone. All 16 emitted records agree with baseline: **9 VERIFIED / 228 bytes**, **7 PARTIAL / 92 compared bytes**, **0 DIFFERS, 0 UNNAMED**. Both class vtables and RTTI records are VERIFIED. The partial records are seven type-name strings (`_ZTS`); their bytes match but the configured ROM extents exceed emitted coverage. `romdata-records.json` preserves baseline and candidate records. Metadata remains externalized by the existing manifest; no new data ownership is claimed.
- `python tools/check_decl_agreement.py --changed 0bc0d1c2df4fba865dd48841039d1446f2a105c4`: exit 0, no new disagreements; `declarations.log`.
- Full ROM/source gate: `python tools/rombuild.py -j16 --no-rom --report-json build/mcarlo2-readability/rombuild-report.json --data-json build/mcarlo2-readability/rombuild-data.json`: exit 0, **11,208/11,208** source-built functions, **106/106** exact modules, **26/26** source-owned initialized data claims and **3 BSS** claims (172 NOBITS bytes), zero new stock symbol errors. The final build repeated after retaining the explicit camera-base address also passed with the same counts; its command uses `rombuild-final-report.json` / `rombuild-final-data.json` and `rombuild-final.log`. Both runs reused 7,944 objects and compiled one. Broad metadata remains 773 VERIFIED / 274 PARTIAL / 3 DIFFERS / 274 UNNAMED; those are whole-tree results, not a claim that all emitted metadata is exact.
- Commit-range strict/attribution checks on checkpoint `89c6c63926766efd0962e12b377af109326d9f44`: both exit 0; 25/25 strict VERIFIED, no warnings/blocking; 8,122 attribution entries tracked, zero changed/lost. `prepush-range.json`, `attribution.json`. The current-main merge adds only the landed declaration checker, its tests and handoff. A complete path diff confirms no source/header/config/build-tool input changed since that proof. The updated checker run `python tools/check_decl_agreement.py --changed 0a609664d09c0377f8680d464a84fd0e6e75e0f4` also exits 0, no new disagreement (`declarations-composed.log`). The source and complete-object/ROM proofs are inherited by checked input identity; no fresh full-ROM execution is claimed for the documentation/decl-checker-only composition. Exact final commit/base binding is in ignored `producer-evidence.json`.
- `git diff --check`: clean. Header-consumer expansion and path-move checks are inapplicable: no header or path changed. No private CI, Source review acceptance, PR or merge is claimed by this producer.
