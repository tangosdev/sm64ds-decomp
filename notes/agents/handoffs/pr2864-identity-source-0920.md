# PR2864: native Koopa source and helper contracts

Producer stage `reconstruct` of `pr2864-identity-promotion-0920`.
Session: `codex-koopa-identity-producer-0920`; actual child `/root/repair2747`.
The earlier shadow repair used `codex-repair-koopa2864-0920` in this same child.
Input: `c4689132a00f3953e13bfac312a9443c5a604493`.
Tested main: `2705c624d2554ee8d21a56641275a2b85cce3a8b`.
Composition checkpoint: `0cbfad427b5c2a219cd05caf847e06eca2658199`.
Active Source policy/workflow pin: `18af52b49b9fb5663d417bdfa86109b7b157aaa3`.

This is an intermediate source handoff, **not production or independent Source
acceptance**. Root owns the following compose stage and all PR publication.
The preserved c468 repair and original author ancestry remain in this branch;
the older separate b694 writer was untouched. Attribution and converted/declaration
baselines, enrollment, profile registries, shared headers other than Koopa.h,
and handler tables were not edited.

## Source result

The real `daNknk_c` class keeps `Koopa` as a compatibility typedef. Its inline
empty destructor and two `new daNknk_c` factories naturally emit D1 before D0,
with no D2. Ten method symbol rows change only the class component; every address,
size and historical `legacy_source` path stays intact. English member names and
the existing reference spelling are disclosed as reconstructed.

Calls now use real actor spawning/lookup, model loading and setup, shadow cylinder,
surface-normal and enemy lifecycle interfaces where the measured forms match.
The Spawn helper uses an ordinary Vector3 and real actor fields; seven bounded
local-order probes found a matching x/z/y declaration order. Sound ID/position,
Hurt return/flag widths, Animation results, particle result and opacity width
agree with actual definitions. The source has 39 symbol markers and the historical
50 conflicts remain visible as 18 resolved plus 32 open, paired with 32 markers.

The expanded scope changed twelve external source files, listed with complete
object hashes and exact function proofs in the
[durable experiment](../../experiments/pr2864-identity-source-0920.json).
The death helper and its callers now share a reconstructed three-pointer
interface: receiver, attacker and nullable collision. No original prototype is
claimed uniquely recovered. Existing handlers consume the attacker and ignore
the forwarded collision value; incoming r3 is overwritten. Removing purported
fourth arguments and daKrb's variadic spelling preserves every tested instruction.
UnloadBlueCoinModel accepts an unused context pointer, MrI passes `this`, and
BookShotSpawner's prototype uses the same opaque pointer spelling.

## Proof and limits

Pinned compiler canary: **2004/b56**. Final raw Koopa object is 26,376 bytes,
SHA256 `d8768734b754cadfe65e74495e2131d5262b8fdc0f3f104799d91e74b63de7e1`.
All **39 Koopa functions** strictly verify with zero differences or blind
relocations. Complete-output and ROM order checks pass. Every byte of eleven
metadata records is compared at its canonical retail home, including every
type-name tail byte and all 132 bytes of the leaf vtable with its preamble.
The weak Vector3 destructor matches its canonical four-byte arm9 body. Twelve
exact output policies leave no unlicensed symbol/section; no data/BSS ownership
or profile/initializer claim is added.

All **12 external complete objects** equal their exact-main controls. Their
**94 functions** strictly verify, including all 35 daKrb, 23 daYurei_Mucho and
27 daPropeller_Heyho functions. Total fresh strict coverage is **133 functions**.
The four affected TU conflict checks, 418 port references, repository reference
check and diff check pass.

The eight non-destructor historical Koopa header consumers strictly verify;
the two old destructor files fail redefinition against the new inline destructor.
They remain unchanged for root's planned retirement of all 39 legacy files.
Consequently no production full-ROM pass is asserted by this source stage.

The actual declaration gate exits 1 with six diagnostics. One is the old Koopa
death-helper call declaration that root will retire. Five are unchanged main
diagnostics: three SetNoControlState return disagreements based on plurality,
and two ov090 resource declarations. The exact nine-file declaration population
and all eleven underlying findings are identical to main2705; root's independent
main inventory also contains the five fingerprints. They are neither caused nor
resolved here, and no baseline was changed. Root must resolve the concrete
integration policy with independent review, not re-bank globally.

The called SetAnim scalar definition is `void(ModelAnim*, BCA_File*, int, int,
u16)`, and the retained bridge agrees. The unused member declaration in the
shared ModelAnim header spells Fix12<int>/u32; this is not the interface this
TU calls. A fresh genuine-member probe grows the helper 0x50 to 0x5c, while the
other 38 functions remain exact. The fresh DropShadow member probe also grows
its helper, to 0xc0. These measurements support those retained scalar forms;
they do not establish an absolute compiler wall.

## Findings retained

| ID | Original kind | Source-stage disposition |
| --- | --- | --- |
| KP2864-01 | correctness | Named actor fields fixed; root corrects original PR body. |
| KP2864-02 | correctness | Owned contracts repaired and byte-proved; obsolete Koopa shard awaits retirement. |
| KP2864-03 | reconstruction | Partial; raw helpers/views, volatile forms, matrix copy and measured scalar bridges remain. |
| KP2864-04 | promotion | Native emission, metadata and order blockers fixed; production inclusion still requires compose and independent verification. |
| KP2864-05 | provenance | Real RTTI-backed identity and bounded name/metadata claims fixed. |
| KP2864-06 | provenance | Markers/conflict accounting fixed; enrollment/credit/counters remain root's next stage. |

All original finding content is preserved in the experiment JSON; no finding
was renamed or erased. Remaining reconstruction is tracked by actual
[issue2871](https://github.com/tangosdev/sm64ds-decomp/issues/2871), next owner
`codex-integrator-0920`. That follow-up does not waive a byte, correctness,
provenance, complete-output or current-base gate.

Root next promotes the TU, updates the intermediate source wording, retires all
39 original files, preserves each symbol's owner and four converted identities,
and runs fresh production/ledger/current-base checks. The independent verifier
must examine the twelve added contract files as well as the Koopa reconstruction.
