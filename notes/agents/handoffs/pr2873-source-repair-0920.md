# Handoff: pr2873-source-repair-0920

This document describes this commit. The queue records its immutable output SHA.

## Identity and scope

Producer `codex-axle-source-0920` (Codex); stage `reconstruct`; branch `fix/pr2873-source-0920`; own wired worktree `C:/tmp/sm64ds-axle2873-0920`.
Input: `9d889e90b7a71db67a9af73221844979c97011a8`, source base `2705c624d2554ee8d21a56641275a2b85cce3a8b`, task workflow `18af52b49b9fb5663d417bdfa86109b7b157aaa3`.
PR: https://github.com/tangosdev/sm64ds-decomp/pull/2873.

Verified local source candidate for integrator composition; **not independent Source acceptance or current-main/CI acceptance**. Reconstruction remains partial. No original PR push, public review, issue posting, ledger edit, queue TSV edit, enrollment change, shared-header edit or descendant-source edit was performed.

The four-function license remains ov002 `[0x020b6a80,0x020b6c54)`, 468 bytes. Two real methods (Behavior/Render) and two linker-named C helpers remain. Both destructors remain in their enrolled individual files. Changed tracked files are the owned TU, its manifest, the D1 provenance comment, this handoff and the experiment JSON.

## Source and retained findings

- **KJ2873-01 fixed:** name and access `mModel.mat4x3`; distinguish model translation at world position / 8 from `mClsnMat`.
- **KJ2873-02 fixed:** the IsClsnInRange scalar bridge now describes its actual definition; no false by-value Fix12 claim.
- **KJ2873-03 source fixed, integration pending:** restore the inherited opaque Cleanup entry, recover the real base pointer locally and use a named descriptor view. Both unchanged descendant calls pass strict checks. Their distinct pointer declarations remain interface debt; void* does not establish canonical typed agreement. Integrator must remove exactly the two newly added caller parameter fingerprints.
- **KJ2873-04 measured constraint:** named collider/model/descriptor fields and direct compound angle update match. Simplified widened comparison matches; direct/bool/masked flag tests shorten Behavior by 12 bytes. Noncompound angle assignment shortens it by 4. Fourteen bounded variants, exact patches and failures are committed. The angle still advances before the suppression test.
- **KJ2873-05 integration pending:** authoritative final source count is 3; total lines 171 = D0 29 + D1 33 + TU 109. Only the integrator may repair the reserved queue row.
- **KJ2873-06 source proof fixed, integration pending:** remove universal destructor-form wording. The historical fourteen-form grid is attributed, not independently reproduced. The measured current header form emits D1 then D0, opposite retail; both copies are exact and their exclusion is explicit. Current-main composition and independent review remain required.

`AxleResources` is an inferred local view, not a recovered original name. Both callers expose its three pointer words. The char* model-helper entry follows the unchanged shared declaration/setup caller and converts locally. The remaining vector-position view, scratch matrix, scalar bridge and destructor scope are recorded partial reconstruction. Follow-up issue draft: primary ignored `build/reviewer-0919/pr2873-followup-issue.md`; unposted, coordinator to handle.

## Producer proof

Durable detail/reproduction: `notes/experiments/pr2873-source-repair-0920.json`. Pin `2004/b56`, actual repository C++ flags. Raw final object SHA256 `ce7faa3de8a2239dc07728b4b1c30b3ac4cc0bfb9780719a2b816bba2302d99a`, 6608 bytes. All allocated sections, symbols and relocations are identical to the original PR control.

- Four licensed functions, three emitted duplicate bodies, both retained destructor sources and all three unchanged helper callers: **12 strict checks VERIFIED**, zero differences and zero blind words. Licensed emission order is retail order. Compiler-only policy accounts for every extra output; no unlicensed output remains.
- Eleven complete emitted metadata objects: **253 bytes exact**, every relocation resolved, including all 136 bytes of vtable storage and both preamble words. Four inherited names emit 9/9/10/13 bytes including NUL; their configured extents are 12/12/12/16. The 3/3/2/3 additional zero bytes are un-emitted retail gap padding. Ordinary word-based report stays **7 VERIFIED / 4 PARTIAL / 0 DIFFERS**.
- `tubuild verify` against an ignored manifest copy: exit 0; 4/4 text, output policy, order and relocation gates pass.
- `rombuild.py -j16 --no-rom`: exit 0; **106/106 modules exact, 11208 functions reproducing, zero mismatches, 26/26 data claims and 3 BSS claims**. All nine linked-symbol diagnostics equal the fresh ROM-gap control; zero new errors. The production object list uses this TU and both retained destructor source objects. Global metadata still has three differing identities and 300 unnamed symbols outside this focused proof; no global clean-metadata claim.
- Scoped declaration overlay check against exact base: no new disagreement. Port reference check: 418/418; conflict accounting: zero notes and zero markers. No eligibility-report-based reference PASS claimed; strict relocation checks and full link evidence are present. Final committed/current-main static checks remain integrator work.

## Integrator continuation

Compose on actual main after Koopa releases overlapping integration surfaces. Preserve four original credits: tangosdev for both helpers and Render, andrewboudreau for Behavior. Preserve the sole converted Render identity. Remove only the two now-stale `func_ov002_020b6ac8` receiver fingerprints at the ov043/ov047 caller paths; do not bank unrelated declarations. Update only this actor's promotion row using the committed source counts. Reserved callers, headers and unrelated ledger rows must remain unchanged.

Publish a suitable follow-up issue for the partial interface/destructor scope, then obtain a different-session source/byte review at the exact final composition/base and terminal required CI. This source offer deliberately does not close the integration findings or assert merge readiness. Ignored local compiler/ROM outputs and the private receipt remain in the producer worktree; no subprocess is intentionally left running at handoff.
