# Sanbo integration, 2026-09-10

This composition preserves source acceptance `c37a24cbd64baf8a91aa27d4f697312eb605484f`
and incorporates main `ffe4a122af239a775463928acb0f88b156b55a1a` after BombKing landed
and its three-file progress refresh.
The class header and both ov096 enrollment files are unchanged from that
accepted source. Integration review corrects the TU comment, manifest census
and historical provenance; the 36 functions and 5,808 text bytes remain a
text-only promotion. Emitted class metadata is not newly owned data.

The converted-function conflict is resolved as the exact three-way set. All
2,715 main identities survive, including seven old Pokey paths mapped to their
existing Sanbo method identities. The combined set has 2,716 entries and passes
the converted-form ratchet. No baseline entry is added merely to make a count fit.
The complete attribution mapping is the recursive three-way merge. Rename,
attempt and provenance records retain their exact three-way multisets: 3,617,
2,073 and 721 records respectively.

The TU summary is regenerated from the staged source and configuration inputs.
The promotion queue agrees with the resulting tree. Main's progress and earlier
class changes retain their existing content and history.

All twelve accepted source findings retain their dispositions. Two additional
findings, `state_table_storage` and `current_queue_count`, correct the constant
PMF records versus their reordered runtime BSS table and distinguish historical
shard counts from the current single-TU queue row. The class facts record all
twelve constant-to-runtime mappings, supported by the exact 392-byte initializer.
No claim about a hypothetical replacement initializer is made. The remaining
raw interfaces, address-named helpers, matrix representation and manual factories
remain bounded reconstruction work under #2558. The measured interface experiments
in the source-review handoff do not establish a universal compiler restriction.

The queue evidence records the exact final candidate, actual target base and
fresh or explicitly equivalent byte, relocation, consumer, metadata and credit
reports. Independent review of that final composition and terminal private
validation remain required before the original PR #2447 lands.
