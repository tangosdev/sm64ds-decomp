# Handoff: pr2745-repair-0918

This document describes this commit. The coordinator records its immutable output SHA.

## Identity and resumption

- PR: https://github.com/tangosdev/sm64ds-decomp/pull/2745.
- Task/stage: `pr2745-repair-0918` / `repair`, producer.
- Session: `codex-repair-polelift-0918`, Codex child `/root/repair2747`. This is
  the same actual session previously named `codex-repair-2747-0918` and
  `codex-attribution-tools-0918`. Neither those aliases nor the earlier static
  preflight constitute independent verification of this repair.
- Branch/worktree: `repair/pr2745-0918`, `C:/tmp/sm64ds-r2745-0918`.
- Exact PR input and installed tool source: `d624eefd17177461646ba675d68370fcc5453af0`.
- Task base / first PR parent: `08ee1194031a68d97c553b41ffbf5139ca6ddb91`.
  The input is a merge that also contains `c67ec9fe1195e8594bae076451a2bbde670fa98e`;
  that original main snapshot supplies the historical sources and attribution control.
- Active Source review policy pin: `18af52b49b9fb5663d417bdfa86109b7b157aaa3`.
  Installed source/tool snapshot retains `162d1796ff405da01b39d21ad753ab40aac71acb`.
- Status: local producer candidate with partial reconstruction; independent
  disposition and current-main composition pending; `formal_acceptance: false`.
- Original contributor history is retained. No function credit, converted
  counters, declaration baselines, symbol ledgers or enrollment files changed.
- No push, external comment, issue creation or queue publication performed.

## What changed and why

Owned scope: class `PoleLift` / `daObjKm2_Ami_Bou_c`, ov045 text
`0x0211150c..0x02111840`, its source/header/manifest and this handoff. The second producer round adds
the eight explicitly reserved documentation/test paths listed below.

The phase now uses a real signed angle member, an unsigned table index, and
`mHeightAng += 0x100`. This removes the integer-this offset store while retaining
the target's signed halfword update and unsigned phase indexing. The collision
member is named `mCylinder`, and sine-table reads use normal array indexing.
The leading source comment describes gameplay; detailed experiments live here
and in local evidence instead of claiming an absolute compiler limitation.

The manifest restores actual historical `_ZN8PoleLift...` predecessor paths.
Old pilot verification, failed whole-TU linking, emitted-output and partial
isolation records are preserved verbatim under `historical_evidence`, copied
from the cited original `PoleLift.json` snapshot. That snapshot did not record
the measurement commit; none is invented. Current enrollment wording is corrected,
and the latest text verification is separate from superseded pilot records.

## Reconstruction dimensions and findings

- Seven functions / 820 text bytes. Six member entries (including D1/D0), four
  lifecycle bodies, a compiler-owned `new` factory and inline destructor. No
  hand-written assembly or draft promotion; four existing converted identities
  and all seven matched identities are retained.
- Named inherited fields, `mCylinder` and signed `mHeightAng` describe this class.
  No local shadow class or integer-this offset access remains in its source.
- RTTI identity remains cartridge-backed; factory/profile spelling is explicitly
  reconstructed, not asserted to be a preserved original identifier.
- Vtable/RTTI compiler output is licensed by exact symbol dispositions. This
  text-only entry does not source-enroll its profile, shared files, CLPS or sinit.
- `POLELIFT-PREFLIGHT-01` (historical evidence/enrollment): fixed as described above.
- `POLELIFT-2745-02` (raw phase access): fixed by the strictly matching named field.
- `POLELIFT-2745-03` (`dBgW_KcMbg::SetFile` bridge): retained pending independent
  disposition. Local/const Fix12 aggregates change InitResources 208 -> 212 bytes;
  inline aggregate-return form gives 220.
- `POLELIFT-2745-04` (`dCcAc_c::Init` bridge): retained pending independent
  disposition. Local/const aggregates give 220 bytes; inline form gives 232.
- `POLELIFT-2745-05` (`IsClsnInRangeOnScreen` bridge): retained pending independent
  disposition. A build-local copied base header was used only for the experiment:
  local/const arguments change Behavior 288 -> 328 bytes; inline form gives 320.
  No shared header was edited. The real base header deliberately omits this method.
- These are bounded measured failed forms, not proof of compiler impossibility.
  Scalar contracts were checked against the actual definitions. A concrete
  follow-up is published as [issue #2754](https://github.com/tangosdev/sm64ds-decomp/issues/2754),
  owned for coordination by `codex-integrator-0918`. The local draft remains at
  `build/polelift-followup-draft.md`. The issue is tracking, not source acceptance;
  independent disposition and a successful claim are still required for further edits.

## Proof

All paths below are relative to this worktree unless stated otherwise. These are
producer checks against the exact input plus the owned working changes, not an
independent acceptance or a current-main composition result.

- Wired setup canary: `(True, 2004/b56)`.
- 25 bounded full-TU candidates: `build/polelift-probes/results.json` and
  `readability-results.json`; each candidate retains source, copied header,
  object and applicable mismatch report. All matching candidates were isolated
  per function and strict-linked with `diffs: []`, `blind: 0`. Baseline and final
  named-field/table/cylinder forms verify all seven entries. The plain unsigned
  member `+=` differs by one word; assignment forms differ by four.
- `python tools/tubuild.py verify ov045/daObjKm2_Ami_Bou_c`: exit 0, 7/7 MATCH,
  clean isolation and relocation destinations, correct emission order; the only
  extra text is the licensed base D2. Log: `build/polelift-final-tu.log`.
- `python tools/affected_src.py include/daObjKm2_Ami_Bou_c.h`: one source consumer,
  the class TU. Its explicit `prepush_linkcheck.py --files ... --json ...` exits 0:
  7 verified, 0 warnings, 0 blocking; `build/polelift-final-consumer.{json,log}`.
  An earlier invocation also passed the header to `--files`, which the CLI treated
  as a spurious NO-SYM class-name warning; the corrected run uses the actual consumer.
- `romdata_check.py --files src/game/actors/d_a_obj_km2_ami_bou.cpp`: exit 0;
  6 VERIFIED / 184 bytes, 5 PARTIAL / 56 bytes, 0 DIFFERS, 0 UNNAMED. Partial
  type-name extents are not full verification. Own vtable and typeinfo are verified.
  Evidence: `build/polelift-final-data.{json,log}`.
- `python tools/rombuild.py -j16 --no-rom`: exit 0; 106/106 modules exact;
  11,206/11,206 source-built functions reproduce, 0 mismatch; 26/26 owned data
  claims reproduce. The baseline control records pre-existing symbol-check errors,
  while intact-TU gates report zero new errors. This command skips final ROM
  packaging; its baseline control separately builds the stock-hash ROM. Global
  metadata summary has 4 existing differences; this TU's own report has none.
  Log: `build/polelift-final-rombuild.log`.
- `python tools/port_refcheck.py`: exit 0, all 423 references resolve.
  `build/polelift-final-port.log`.
- `python tools/check_decl_agreement.py --changed c67ec9fe1195e8594bae076451a2bbde670fa98e`:
  exit 0, no new disagreement (11,028 existing scoped disagreements).
  `build/polelift-final-declarations.log`. No baseline was updated.
- `check_tubuild_conflicts.py ov045/daObjKm2_Ami_Bou_c`: exit 0; no conflicts.
  Historical blocks and seven legacy paths were programmatically compared with
  their cited snapshot and agree exactly. `git diff --check` passes.
- Attribution preflight on original c67 -> d624, using repaired local tooling
  `c823a6a2c878a07856d1ca864d3c5250c967c9f7`: seven preserved, zero changed/lost.
  Original report is in the primary checkout at
  `build/reviewer-0918/preflight-sources/2745/attribution.json`. This repair changes
  no attribution metadata or function identities. Post-commit branch-native tooling
  reports one changed factory file author (tangosdev -> lunavyqo), six preserved
  consolidations and zero lost: the known same-basename bypass that the separate
  tooling repair fixes. Raw report: `build/polelift-final-attribution.json`. No
  path-wide override was added to hide this false positive. The repaired-tool check
  and final immutable identity belong in separately recorded evidence.

## Integration blocker and next owner

`git merge-tree --write-tree d624eefd... 936382480eb656320f00fe65643416f8cd11ea59`
reported conflicts in unreserved `attribution.json` and a modify/delete of
`src_tu/actors/TTC_MovingBar.cpp` (this PR changes a comment; main removes its
promoted shadow). No merge was begun and neither file was edited. Preview:
`build/composition-preview.txt`.

The coordinator must reconcile those reserved integration surfaces and arrange
fresh independent verification of the resulting composition. The earlier static
preflight and existing failed Source review are not acceptance. Current PR CI,
private validation and current-main source acceptance remain pending.

## Second producer round after independent review

Input: `2d62a40d89c759f66cbd7759831bcff027a8b24e`, same actual agent and
`codex-repair-polelift-0918` session. The independent verifier
`codex-verify-polelift-0918` (`/root/bird_codegen`) rejected that candidate for
findings 06–09 while independently confirming the source bytes and accepting
03–05 only as deferred work in a partial reconstruction. This successor remains
`formal_acceptance: false` until independently reviewed.

The claim was expanded before editing these eight paths:

- `notes/tu-reconstruction-pilot-report.md`
- `notes/rtti-name-audit.md`
- `notes/tu-reconstruction-pilot-2-report.md`
- `notes/plan-tu-merge-queue.md`
- `symbols/actor_renames_report.txt`
- `config/tu_manifest.d/arm9/Scene.json`
- `notes/data/tu-merge-candidates.json`
- `tools/test_tubuild.py`

The already-owned class manifest also now separates fresh generated boundaries
from archived observations. No source/header, enrollment, attribution, global
baseline, compiler policy, or function-ownership rows changed in this round.
The source and header are byte-for-byte identical to the input commit. Existing
full-ROM/source-byte evidence is inherited, not represented as a new ROM run.

All nine finding dispositions:

| Finding | Producer disposition in this successor |
| --- | --- |
| `POLELIFT-PREFLIGHT-01` | Fixed and retained: old manifest verification, unlicensed-output and partial-isolation records remain identical to the cited c67 snapshot. Historical boundary fields are now preserved there too. |
| `POLELIFT-2745-02` | Fixed and retained: named signed phase, unsigned index and cylinder member are unchanged from the independently byte-verified input. |
| `POLELIFT-2745-03` | Deferred to [issue #2754](https://github.com/tangosdev/sm64ds-decomp/issues/2754), owner `codex-integrator-0918`; SetFile bridge and bounded measurements remain unchanged. |
| `POLELIFT-2745-04` | Deferred to the same issue/owner; collision Init bridge and bounded measurements remain unchanged. |
| `POLELIFT-2745-05` | Deferred to the same issue/owner; visibility bridge and bounded measurements remain unchanged. |
| `POLELIFT-2745-06` | Fixed: restored PoleLift transcript names, emitted D2/object paths, RTTI HOMELESS sizes and SKIP diagnostics from the pre-promotion c67 snapshot. Each historical report separates its current mapping from the measurements. The OK source table links archived blobs; the exact original transcript remains at its immutable report permalink. No measurement commit was invented. |
| `POLELIFT-2745-07` | Fixed: Scene and the vptr discussion cite the real archived shadow. The plan explicitly says the current return-new factory no longer contains that manual-store comment. |
| `POLELIFT-2745-08` | Fixed: the complete historical census is restored, including all seven actual PoleLift source identities. A separate metadata mapping identifies the single current promoted owner. Six nonexistent renamed per-function paths are gone. |
| `POLELIFT-2745-09` | Fixed: inspect asserts the freshly generated six-member medium run ending at 0x02111808 and its separate adjacent factory. Verify positively checks all eleven emitted RTTI/vtable records in the actual compiled ELF, their exact externalization in the fresh CLI report, and the one licensed base D2. Absence of a leaf D2, unlicensed output, and promotion refusal is still enforced. The manifest reports these current boundaries separately from historical high/corroborated observations. |

Validation for this round:

- Generated fresh prerequisites with `python tools/rtti_extract.py --out build/rtti.json`,
  `python tools/rtti_vtables.py --out build/rtti_vtables.json`, and
  `python tools/tu_map.py`; all exit 0. Logs are
  `build/polelift-rework-rtti.log`, `polelift-rework-vtables.log`, and
  `polelift-rework-tu-map.log`.
- `python -m pytest -q tools/test_check_src_tu.py tools/test_check_src_tu_compiles.py tools/test_sinit_owners.py tools/test_tubuild.py`:
  **110 passed, 1 skipped**, exit 0, with wired 2004/b56. The intentional skip
  remains the obsolete per-function isolation test whose seven source files were
  absorbed. Existing negative compiler-policy tests still run. Evidence:
  `build/polelift-rework-tests.log` and `polelift-rework-tests.exit.txt`.
- `python tools/check_dead_references.py`: the initial run found one remaining
  historical factory path in prose. It was changed to the actual archived blob
  permalink, without modifying either baseline; the final result is recorded in
  `build/polelift-rework-deadrefs-final.log`: exit 0, no new dead references and
  no broken Markdown links.
- `python build/polelift-rework-provenance.py`: exact snapshot identities, archived
  Git blob targets, unchanged source/header and unchanged functional manifest
  fields are checked in `build/polelift-rework-provenance.json`.
- `git diff --check` passes. No broad compiler/ROM rerun is needed for this
  documentation and test successor; the test suite does freshly compile and
  relocation-check all seven functions through the real verify CLI.

The coordinator owns final publication, latest-main composition and contributor
history preservation. The verifier must review this exact successor, carrying all
nine IDs and the three issue-backed deferrals forward.
