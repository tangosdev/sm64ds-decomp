# Handoff: issue-2481-clock-source-review

This document describes this commit. The queue records its immutable output SHA.

## Identity and continuation

- Issue: https://github.com/tangosdev/sm64ds-decomp/issues/2481; task `issue-2481-clock-source-review`, stage `fix`, role producer.
- Session/harness: `codex-clock-humanizer-producer-20260908` / Codex; branch `cpp/humanizer-clock-0908`.
- Accepted input and original source base: `224e660ea0219d0999c58483eb2dd9b38991f5ef`. This continues the already merged PR #2444 Clock class and intact TU.
- Queue/workflow pin: `6ef1dfb896e6074e83d2aa1a03cc3b701d07dcae`, from the installed workflow checkout. Byte tools are pinned by the source input `224e660ea0219d0999c58483eb2dd9b38991f5ef`; `linkcheck.py` is unchanged between those pins, while `tubuild.py` and `rombuild.py` have advanced on main. Wired compiler canary: verified `2004/b56`.
- Status: locally proved correction for independent verification. Next owner: independent verifier, then integrator for the requested correction PR. No normal PR branch push, merge or independent acceptance by this producer.
- Changed surfaces: `src/actors/daObjClock_c.cpp`, comment-only `include/daObjClock_c.h`, manifest notes and this handoff. Original author ancestry and all symbol/attribution/ledger records remain preserved. No source moves, shared-header definitions, profile registry writes or changes to other classes.

## Corrections and evidence

The complete owned scope is nine functions / 752 text bytes at `ov013:0x021113bc..0x021116ac`, and 216 data bytes at `ov013:0x021121a4..0x0211227c`. The data contains the RTTI record, terminated class name and padding, two 28-byte profiles, and the 132-byte vtable object. Its 31 callable slots start at address point `0x02112200`, after the two-word preamble at `0x021121f8`.

- **PR2444-02:** `func_ov013_02111430` now uses a typed local `daObjClock_c*`, the existing inherited angle/position fields, and the owned `mModel.mat4x3.t`. The helper writes the matrix at actor `+0xf0` and translation at `+0x114/+0x118/+0x11c`, scaling position by one eighth. The existing include order selects this structured matrix view. The single cleaner-form probe is strictly VERIFIED; the old raw-offset ownership rationale is removed. The external `char*` declaration, C linkage, address-derived name and both callers remain unchanged; original member/free-function status is not established.
- **PR2444-04:** the two factory suffixes are described as profile disambiguation in the current registry. Both factories and both `g_profile_` globals retain their existing symbols. RTTI supplies `daObjClock_c`; ROM debug strings supply `CLOCK_SHORT` and `CLOCK_LONG`. The complete function/global spellings remain reconstructed project names. The two profiles have separate factories with the same construction behavior, not a claim that relocated call instructions at different addresses are identical.
- **PR2444-05:** the local `int isLongHand` truthfully describes `actorID == 0x125` / `CLOCK_LONG`. Its type, condition, assignments and hand-index mapping remain unchanged: zero is long and one is short.
- **PR2444-06:** the header now explains that matching an inherited virtual signature already makes the override virtual. Repeating `virtual` documents the role; its earlier omission did not turn an inherited override into an ordinary nonvirtual method. Current signatures, declaration order, inline destructor, size and layout are unchanged.
- **Profile roles:** `ClockSpawnInfo` now uses `behaviorPriority` and `renderPriority` for offsets `+4/+6`. The current `fBase_c` constructor obtains actor ID separately, indexes the profile table, and passes these halfwords to the corresponding priority setters. The names describe observed uses, not recovered identifiers. Existing `s16` storage, field order and initializer values are unchanged; positive values and the consumer's current types do not establish original signedness.

## Reconstruction dimensions and remaining work

There are four ordinary virtual methods, two compiler-emitted destructor variants, one external helper and two manual factories. The class owns Model at `+0xd4` and the hand index at `+0x124`; both factories allocate `0x128`, construct the actor base and Model, and install the shared class vptr. Compiler-generated destructors reverse the owned-member/base lifecycle. These existing definitions and the six inherited metadata dispositions are preserved.

Partial reconstruction remains under issue #2481 and its existing coordinator: manual allocation/base/Model construction and vptr stores in both factories; the helper's external boundary; file-table casts and larger external interfaces; and unproved original profile storage types. Broader alternatives were not measured in this bounded correction and are not called compiler barriers. The successful typed-helper experiment needs no raw-offset compatibility form. Promoted status does not assert that every original interface or identifier has been recovered. Historical PR #2444 handoff/verification records remain history; London, volcano and the separate shared actor callback repair are outside this task.

## Fresh local proof

Ignored evidence is `build/clock-proof/proof.json`, with expanded commands, exact source/header hashes, exit codes and logs. `probe.json` and `typed-helper-probe.cpp/.log` preserve the separate typed-helper experiment. Single-symbol `linkcheck.py` prints JSON on stdout; that result is captured directly.

- `python tools/linkcheck.py --name <symbol> --c src/actors/daObjClock_c.cpp --module ov013 --addr <address> --size <size>` for every manifest function: **9/9 VERIFIED**, `blind: 0`, `diffs: []`, exit 0. Expanded commands/results are `strict-0..8.json/.log` and the proof file. Address/size pairs: D1 `0x021113bc/0x30`, D0 `0x021113ec/0x44`, helper `0x02111430/0x48`, CleanupResources `0x02111478/0x2c`, Render `0x021114a4/0x28`, Behavior `0x021114cc/0x100`, InitResources `0x021115cc/0x70`, short factory `0x0211163c/0x38`, long factory `0x02111674/0x38`.
- `python tools/tubuild.py --manifest build/clock-proof/manifest-copy verify ov013/daObjClock_c`: exit 0, **9/9 MATCH**, isolation and destination checks clean, all nine functions in expected order. The ignored manifest copy receives the fresh result; tracked historical verification is unchanged. Log: `tu-verify.log`.
- Fresh raw-object proof through the existing `tubuild` compiler-only policy, nontext ordering and owned-section APIs: **216 owned bytes exact, all 37 relocations OK**, no errors. No byte patch or policy change. Full details/object hash: `owned-data.json`.
- `python tools/romdata_check.py --files src/actors/daObjClock_c.cpp --json build/clock-proof/romdata.json`: exit 0, **7 VERIFIED, 4 PARTIAL, 0 DIFFERS, 0 UNNAMED** across 11 records. The 36 bytes compared by partial records remain partial; the complete owned-data check separately proves the entire class data range.
- `python tools/rombuild.py -j 8 --report-json build/clock-proof/rombuild.json --data-json build/clock-proof/romdata-all.json --rom-out build/clock-proof/sm64ds.nds`: Exit 0; **106/106 modules exact, 11,199 source-built functions reproduce, 25/25 owned data claims reproduce**, and three BSS claims pass object/symbol gates. The 16,777,216-byte packed ROM matches the expected hash `d1506e90efae5e2d2cf119926a4ac2a291bd5ca78349d09d5024e1a918c478e8`. Intact-object gates report zero new symbol errors and exact storage aliases. Log: `rombuild.log`. Expanded command and exit: `rombuild-command.json`.
- Header expansion via `affected_src.affected_sources(['include/daObjClock_c.h'], ROOT)` finds only this promoted TU. All nine functions are covered above. Header non-comment tokens, include order, helper external signature/callers and symbol markers equal the input; manifest values apart from notes are identical. Outside the helper body the only code changes are the three local names. `affected-consumers.json` and `scope-invariants.json` preserve this coverage.
- `python tools/port_refcheck.py --json build/clock-proof/port-refcheck.json`: exit 0, 423 references, zero stale. `python tools/langmode_audit.py --check build/clock-proof/langmode-bank.json --json build/clock-proof/langmode.json`: exit 0 against recorded `chaos-data` commit `b4fab1eb7f1ed58f73955ca84309a6f9a25ffcb1`; no baseline write. `git diff --check`: exit 0. Logs and expanded commands: `static-checks.json`.

The broader metadata audit reports 704 verified symbols, 223 partial, four differing symbols and 389 unnamed symbols; it is not a global metadata pass. The differences are outside Clock: dCcAcPos_c, two daDemo_c nested RTTI records, and daKrb_c. The scratch control retains nine existing `dsd check symbols --fail` errors while module/ROM checks pass; the production intact-TU gate admits zero new symbol errors. The scoped metadata partials remain partial, separately from the complete 216-byte owned-data proof.

Exact-commit attribution and pre-push checks produced after this commit belong in separately recorded ignored evidence, not an amended handoff. A basename `NO-SYM` warning is not function verification; the explicit nine-function checks provide full promoted-TU coverage. Private PR validation and independent review are pending. No source work remains uncommitted after this checkpoint; ignored build/probe files and the private receipt remain local.
