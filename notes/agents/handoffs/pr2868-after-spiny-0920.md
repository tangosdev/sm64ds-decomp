# BookShot composition after Spiny landed

This composes accepted metadata-fixed BookShot
`2de630659a82f027501fe66792068c97ff18f0b6` with actual merged main
`aba8efc2891b1428d73af41fc78c3439fb1dd7bd`.
The clean merge is `291f8997a6cb16754bb30da4bdfef59772795200`; this successor adds
only this handoff and the [measured report](../../experiments/pr2868-after-spiny-0920.json).

Composer `codex-book-after-spiny-composer-0920` owns task
`pr2868-after-spiny-0920`, inheriting `pr2868-conflict-notes-0920` and
`pr2868-composition-0920` with all prior authors/findings. Only these two new report
paths are reserved. Worktree `C:/tmp/sm64ds-bookcompose0920`, branch
`integrate/pr2868-after-spiny-0920`; previous branches are preserved.

Every accepted source, header, manifest and prior report blob is preserved,
including the six resolved conflict-note fixes. Every unrelated main blob and
all four checked shared ledgers match actual main. No manual source, header,
declaration, manifest policy or enrollment change is introduced.

Fresh `2004/b56` proof verifies **31/31 shadow functions** (6040
function bytes) and **8/8 actual BookShot header consumers**, with empty differences
and zero blind words. The complete 22,376-byte object equals this composer's own
accepted-input control; SHA256
`05b5f1dc672fdd24cda95ebcd02e9a2327fe297bc948a889077b75a320620688`.
The actual conflict checker reports **13 open notes / 13 markers / zero problems**.
Scoped declarations, port references and dead references pass on the tested merge.

The complete-TU promotion command still exits **1**: **17 unlicensed outputs**,
**three compiler-only policy errors**, and destructor ordinal pairs `(0, 1)` and
`(2, 3)` remain. These are retained limitations, not waived gates. This remains
an unenrolled shadow; no full-ROM, production promotion, private validator or
independent Source-review success is claimed. The declaration ratchet excludes
`src_tu`; its result does not certify every shadow contract.

All `BOOK2868-01` through `BOOK2868-06` findings retain their kinds and dispositions.
Remaining reconstruction and output ownership stay partial under
[issue #2872](https://github.com/tangosdev/sm64ds-decomp/issues/2872), next owner
`codex-integrator-0920`.

Publish this composer's exact output through its own receipt. Independent reviewer
`/root/repair2747` must claim VERIFY with its own receipt and judge the final
candidate/base. Root owns original-PR publication and merge.
