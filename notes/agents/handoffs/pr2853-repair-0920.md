# Mip Key producer repair, PR2853

- Task: `pr2853-source-repair-0920`, produce stage; session `codex-repair-mip2853-0920`.
- Actual child: `/root/repair2747`, previously used producer aliases for Birds,
  attribution tools, PoleLift and Number. This is a producer handoff, not self-review.
- Active Source policy/task workflow pin: `18af52b49b9fb5663d417bdfa86109b7b157aaa3`.
- Exact input: `e4d76233ccebc942180bcdeb4a19d34d2d65bdd6`.
- Exact task base: `4407c8471ed3299ea16d9cc899020e3232ac6ee9`.
- Own wired worktree: `C:/tmp/sm64ds-mip2853-0920`, branch `fix/pr2853-0920`.
- The private producer receipt remains ignored; no token is included here.

## Owned change

The six reserved files are the Mip Key TU/header/manifest, the historical filename
pilot note, [the experiment note](../../experiments/mip-key-2853-source-forms.md),
and this handoff. No config symbol, enrollment, attribution, converted-tier or
declaration-baseline file was modified. Original PR history and all 15 address
credits remain intact (12 tangosdev, 2 ruspecial, 1 andrewboudreau).

The source now uses actual state/player fields, real-class PMFs, native state
calls, real model/Player/Camera/Message/SharedFilePtr interfaces and Vector3 locals.
The fake padded host, fake render vtable and volatile carry-vector stores are gone.
Sound and unsigned helper contracts agree with their matched definitions. The
historical note links to the real pre-promotion file at an immutable commit.

## Finding dispositions

| ID | Producer disposition |
| --- | --- |
| MIPKEY-2853-01 | Fixed incorrect callee contracts; real declarations used where available. |
| MIPKEY-2853-02 | Fixed fake host/fields/vtable and manual PMF dispatch; all 15 relocated functions remain exact. |
| MIPKEY-2853-03 | Fixed avoidable volatile and vector shadow types with measured ordinary Vector3 forms. |
| MIPKEY-2853-04 | Unsupported original-file-static claims and initializer-range wording corrected. Seven address-named helper entry points remain source reconstruction debt. |
| MIPKEY-2853-05 | Flat Matrix4x3 copy and scalar DropShadowRadHeight bridge retained with valid current-type measurements. No absolute compiler-wall claim. |
| MIPKEY-2853-06 | Historical citation fixed. Four shared declaration identities and current-main composition remain pending coordinated integration. |

Findings 04/05 have a local follow-up issue draft with the coordinator as proposed
next owner. No issue is posted and no deferral or independent source acceptance is
claimed. Promotion is text enrollment, not completion of all genuine-method work.

## Validation

Pinned canary returned `(True, '2004/b56')`. `tubuild.py verify
ov085/daObj_Mip_Key_c` passes 15/15 text functions (2208 bytes), emission order,
objisolate and relocation destinations. `pr_linkcheck.py --files
src/actors/daObj_Mip_Key_c.cpp include/daObj_Mip_Key_c.h --json
build/mip-triage/final-linkcheck.json --fail` reports 15 VERIFIED, empty diffs and
blind0. The header reaches only this TU.

Complete object policy passes with no reasons: D2 is the existing deadstripped
variant; Vector3 D1 is the existing licensed duplicate and independently verifies
against arm9:0x020072c0 (4 bytes, blind0). It is not counted as a new match. Eleven
emitted metadata records retain their explicit gap-owned policy; romdata_check
reports 6 VERIFIED/180 bytes, 5 PARTIAL/52 bytes, 0 DIFFERS and 0 UNNAMED.

`check_decl_agreement.py --changed 4407c8471ed3299ea16d9cc899020e3232ac6ee9`
correctly remains red on four typed global identities: Camera pointer, SharedFilePtr
and two MipKeyState objects disagree with generic placeholders elsewhere. These
are explicit unresolved shared-declaration boundaries. The exact four-row proposal
is local `build/mip-triage/required-declaration-identity.patch`; applying it requires
the still-active owner's coordination. No baseline edits or broad update were made.

`rombuild.py -j16 --no-rom` passes: 106/106 modules exact, 11,208 functions
reproducing, 0 mismatching, all 26 source-data and 3 BSS claims reproducing.
This compiles/links every module; the --no-rom option skips only final ROM assembly.
The final prototype-only alignment to the actual DropShadowRadHeight definition
(typed pointers/u8 opacity) and RotationX matrix pointer preserves the complete
object SHA256 `0fab978517bf4cdb1fdd83035d0a5fae7fdcdd0f0ef4b7196b8b2e0b54485f8d`;
no byte proof was inherited across a changed object.

`port_refcheck.py` passes all 423 references. `check_dead_references.py` passes
with no new dead references or broken links. `tiers_ratchet.py --check` passes
(baseline 2973, current 2981); gains were not banked. `queue_audit.py --check`
remains red. The stored Mip Key row already had shard_count 14 and total_lines 433
at input e4d76233. An original-head control through the actual queue_audit consumer,
reading the exact archived source/header/manifest bytes, requires shard_count 1,
total_lines 626 and the RTTI-only compiler-floor explanation (still>=11). The repaired
candidate required shard_count 1 and total_lines 363 at 1dfbfc439. The final reviewer
pair reduces that line count to 360 with the same compiler-floor explanation.
Thus only the measured line count 626->360 is introduced by this source repair;
the stored shard 14->1 and compiler-floor wording drift are inherited. The promotion
flag was already correctly yes at input; earlier triage's repaired queue status
referred only to that flag. The full audit also reports unrelated stale rows, so a
single-row correction is not claimed to turn the global audit green. Exact rows and
control method are recorded in `build/mip-triage/original-head-queue-control.json`
and `build/mip-triage/queue-row-proposal.json`; the tracked TSV remains untouched.

The exact repaired source commit 564695bfea132515c64ab38b4a76897b46d59ddc passes
postcommit credit verification against base 4407c847: 15 consolidated with original
credit intact, 0 changed and 0 lost. The 1dfbfc439 handoff clarification changed no source,
header, manifest, symbol, enrollment or attribution identity. The four-row proposed
declaration identity overlay also makes the actual scoped declaration consumer pass
in memory, while the real baseline remains byte-identical and the actual gate red.

Gate logs and exact compiler objects are retained in ignored `build/mip-triage/`.
Independent verification and root-owned current-main composition are still required.
No push, queue publication, PR/issue mutation or merge occurred.


## Final bounded reviewer pair

At the independent reviewer's request, the producer removed the s16 angle pointer
and the PMF entry temporary, using direct member operations in both functions.
Fresh before/after compiles at input 1dfbfc439 preserve the complete object SHA256
listed above. All 15 strict relocated functions were rechecked and remain VERIFIED,
empty diffs, blind0. Header, actual manifest policy, symbol/enrollment/attribution
identities and all remaining findings are unchanged. The manifest's vtable reason
now says first declared non-inline virtual/key function, correcting the unsupported
only-virtual claim under MIPKEY-2853-04. The full-ROM and metadata proof carries
from 564695bfe by whole-object identity; no full rebuild was repeated.

Final source line count is 360; only the ignored queue-row proposal was refreshed.
The four declaration diagnostics and findings 04/05 still require their previously
recorded coordination and independent disposition. No shared ledger was edited.
