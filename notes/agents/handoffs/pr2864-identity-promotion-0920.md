# Koopa / KoopaSmall production composition

Task: `pr2864-identity-promotion-0920`. Producer: `codex-integrator-0920`.
Source handoff: `d81de04a24a2c0059c6814c71bf512e1547e0e65`.
Tested source base: `2705c624d2554ee8d21a56641275a2b85cce3a8b`.
The queue records this commit's exact immutable identity. This is producer
evidence; independent source acceptance and terminal private CI are separate.

The real retail `daNknk_c` class and compatible `Koopa` typedef now supply all
39 functions through `src/game/actors/d_a_nknk.cpp`. Both factories use ordinary
`new`; the inline destructor emits the correct D1/D0 order without D2. The
promotion retires39 shards and enrolls ov062 text `[0x021174cc,0x0211975c)`.
Canonical profile, RTTI, vtable and BSS storage ownership is unchanged. All11
emitted metadata objects and the canonical weak Vector3 destructor are accounted
for by exact policies and direct comparison, including the full132-byte vtable.

This promotion preserves all39 symbol credits: tangosdev29, andrewboudreau2,
lunavyqo4, ruspecial4. Four existing converted identities move to their native
member keys; the count and unrelated records remain unchanged. Queue rows are
derived for Koopa and the three directly changed matrix-caller classes only.
The two active shared-ledger reservations are preserved under the user's narrow
PR-specific authorization; the reserved profile registries are unchanged.

The preceding source handoff's twelve external helper repairs are retained.
An additional fourteen source dependencies reconcile the actual translation
matrix helper and thirteen callers with the shared Matrix4x3 layout. Ninety-eight
matched functions verify, plus the canonical weak destructor. Thirteen complete
objects are identical; the remaining object adds only that existing weak body.
The pre-existing ov006 draft remains NONMATCHING with an identical entire object,
the same164 strict differences and actual PR-layer DRAFT classification. No match
or new ROM enrollment is claimed for it. No shared matrix header is edited.

The real linker exposed obsolete `func_ov002_020ada40`, which isolated checks had
resolved by its address-shaped spelling. The source now imports the actual
KillByInvincibleChar symbol through the existing scalar ABI bridge and uses
Vector3_16. Every allocated byte remains identical; only the import name changes.
Four genuine Fix12 member-call forms grew the caller from0x36c to0x374 or0x378.
The callee ignores that final argument. This is a measured bridge limitation,
not canonical C++ prototype agreement or proof that every native form must fail.

Declaration reconciliation removes172 retired or proven healed fingerprints,
including57 matrix diagnostics, and carries seven exact existing shard findings
to the promoted path. Five independently reproduced unchanged-main fingerprints
are separately grandfathered: three Player SetNoControlState return views and
two ov090 resource views. They remain unresolved, plurality-based debt. All3744
unaffected symbol records preserve their original order and text; no whole-tree
re-bank or scanner change is used. Four old matrix pointer views also remain.

Source reconstruction is **partial** under
[#2871](https://github.com/tangosdev/sm64ds-decomp/issues/2871), next owner
`codex-integrator-0920`:27 free helpers, raw field/PMF/volatile/copy forms and
measured SetAnim, DropShadow and KillByInvincibleChar call limitations remain.
KP2864-01 through KP2864-06 are preserved; KP2864-07 records the unresolved-name
link repair and KP2864-08 the matrix contract repair. A production promotion is
not a claim that all functions have become genuine member methods.

The [durable experiment](../../experiments/pr2864-identity-promotion-0920.json)
contains exact patches, hashes, all dependency function checks, accounting,
main controls and completed gate results. Historical source-stage measurements
remain in the [source handoff](pr2864-identity-source-0920.md). Final acceptance
must use this composition and the live source base, not the earlier shadow pass.
