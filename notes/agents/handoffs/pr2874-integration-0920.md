# Star Switch production composition

Task `pr2874-source-repair-0920`; producer `codex-integrator-0920`.
Source offer `c6c70a64fe399f4c2d1ab7e36db3172ed5d27d24`; tested main `f894e1841adc5c5745ce9364c5c04955bcbdd6a9`;
compiled and statically checked source anchor `5c0a7a2ac2bce37817ab25d0f3d808a2af632dbe`.
The queue binds the exact output commit. Independent Source review and terminal
private validation remain separate acceptance gates.

The production TU covers twenty functions / 3016 bytes at ov002
`[0x020b9e64,0x020baa2c)`: five written methods, thirteen C-linkage helpers and
the two variants emitted by the real inline destructor. Its final source has
418 lines. Callback, initializer and factories beyond this boundary remain
separately enrolled. The old broader23-function candidate list is historical.

The actor uses genuine model rendering and Vector3 scale, ordinary matrix
assignment, native collider transformation, real class member-pointer state
dispatch, named actor fields and class-owned resource records. Transition and
tail state handler return void. The sound wrapper forwards the actual integer
handle consumed by Star and BlueCoin switches. The StarMarker declaration now
names the actual SharedFilePtr resource. No contradiction is newly banked.

Scalar collider SetFile and materialized integer comparisons remain where the
committed exact compiler experiments show ordinary alternatives differ. The
earlier nested-matrix failure is superseded by a matching ordinary assignment.
Thirteen address-named helpers and an unnamed foreign PowerStar field remain
explicit boundaries; this is not whole-class/interface completion.

SS2874-01 through09 are preserved in the evidence. The incorrect OnGroundPounded
credit is restored: all twenty original address identities remain tangosdev's.
Four converted identities retain their positions and score from actual bodies,
including genuine inline destructor fragments without synthetic markers.
Declaration reconciliation starts from actual post-axle main, removes only
retired or proved-healed records, and moves only exact surviving fingerprints.
All unrelated Koopa, axle and older-owner work remains intact. The one Star
queue row records one source and418 lines; the native header's stale alias
suffix is removed. Original rename history remains preserved.
The Star section of the field-provenance note now describes the current typed
fields and native calls; all other class sections remain unchanged.

Source proof contains38 focused strict checks, both complete affected header
consumer objects, unchanged StarMarker object, all output/order policies, and
all249 emitted metadata bytes across eleven objects. The vtable proof covers
the complete136 bytes including its preamble. Standard word-level metadata
reporting remains seven VERIFIED and four PARTIAL; un-emitted gap padding is
not source coverage. Full production and expanded relocation results at the
actual tested main are recorded separately, including inherited global data
diagnostics rather than claiming a globally clean metadata inventory.
The broad header-consumer gate also records two inherited blind-relocation
warnings in unrelated existing sources. Exact current-main controls preserve
both complete objects and the same warnings; they are not reported VERIFIED.
Every licensed Star body and its focused affected controls has zero blind words.

The final formatting patch changes only whitespace; token stream, line count
and the complete12840-byte source object are unchanged. The source experiment
retains original findings, all measured variants and superseded constraints.

See [integration evidence](../../experiments/pr2874-integration-0920.json) and
[source experiments](../../experiments/pr2874-source-repair-0920.json). No
substantive deferred finding is hidden by the partial-scope description and no
new follow-up issue was posted. Independent review must resolve each finding
against the exact final candidate and current base.
