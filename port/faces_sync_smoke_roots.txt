# port/faces_sync_smoke_roots.txt -- THE GATE-3A SUBSET OF port/faces_sync.txt.
#
# Lane SMOKELINK3, run link100 wave 10 round 2, continuing SMOKELINK2's lever.
#
# Read port/faces_sync_smoke_heap.txt's header first: it states the whole
# argument for a per-target subset ledger rather than a second target on the
# 1925-row port/faces_sync.txt, and nothing about it changes here.  In one
# line: every row of the big ledger CALLS a C++ member, so the generated
# object carries 1925 references, and smoke_roots links 54 objects.  A
# seven-row ledger costs seven faces.
#
# EVERY ROW BELOW IS COPIED VERBATIM FROM port/faces_sync.txt, columns and all,
# and the same port/tools/facegen.py --sync re-derives each one against the ROM
# address in column three on every build.  Nothing here is hand-written and
# nothing here is a new binding: if the big ledger's row for one of these ever
# changes, the two files disagree and the diff is one grep away.
#
# THE SEVEN ROWS ARE THE __thiscall PART of smoke_roots's thirty unresolved
# names, measured at f7d4b3c3a (out/SMOKELINK2/build_smokelink2_0.log, whose
# per-target counts this lane re-confirmed).  Every one is an ordinary
# non-static member or a constructor, receiver in ECX, and faces_sync.txt's own
# header says what that means: an /alternatename is a NAME bridge and never an
# ABI bridge, so a row like these cannot be closed with one.  A face is what
# closes it.
#
# Four of the seven are the same rows port/faces_sync_smoke_heap.txt carries,
# because gate 3a is gate 2 plus the root-heap layer and inherits its callers.
# They are repeated rather than shared: the two ledgers feed two separate
# generated files on two separate targets, and a row that is spelled the same
# in both is a row that can be grepped across all three files at once.
#
# COLUMNS, and the R/F/D meaning, are port/faces_sync.txt's.  Read that file's
# header before adding a row here.

__ZN10MemoryNode6TargetC1EPS_                                            ??0Target@MemoryNode@@QAE@PAU1@@Z                                0x0204e938 R
__ZN13ExpandingHeap11VDeallocateEPv                                      ?VDeallocate@ExpandingHeap@@UAEXPAX@Z                            0x0203c50c R
__ZN13ExpandingHeapC1EPvjP4HeapP22ExpandingHeapAllocator                 ??0ExpandingHeap@@QAE@PAXIPAUHeap@@PAUExpandingHeapAllocator@@@Z 0x0203ca80 R
__ZN18NestedHeapIteratorC1Ej                                             ??0NestedHeapIterator@@QAE@I@Z                                   0x0204df20 R
__ZN22ExpandingHeapAllocator16AllocateForwardsEjj                        ?AllocateForwards@ExpandingHeapAllocator@@QAEPAXII@Z             0x0204e5c8 R
__ZN22ExpandingHeapAllocator17AllocateBackwardsEjj                       ?AllocateBackwards@ExpandingHeapAllocator@@QAEPAXII@Z            0x0204e504 R
__ZN22ExpandingHeapAllocatorC1EPvj                                       ??0ExpandingHeapAllocator@@QAE@PAXI@Z                            0x0204e828 R

# THE ONE NEW PAIR IN THIS FILE, derived here rather than copied.
#
# NestedHeapIterator::Remove(HeapAllocator *) is the only name on smoke_roots
# that neither port/faces_sync.txt nor port/hal/cxx_aliases.cpp already spells.
# It is reached from HeapAllocator::Remove, which HeapAllocator::Destroy calls,
# which ExpandingHeap::VDestroy calls -- and ExpandingHeap::VDestroy is one of
# the seven source rows this target was missing, so the name arrives with them
# rather than out of the harness.
#
# THE DERIVATION, by address, the way the rest of this ledger is derived:
#   config/arm9/symbols.txt:1999
#     _ZN18NestedHeapIterator6RemoveEP13HeapAllocator kind:function(arm,size=0x70) addr:0x0204dd28
#   src/_ZN18NestedHeapIterator6RemoveEP13HeapAllocator.cpp is that body, and
#   include/NestedHeapIterator.h:28 declares it `void Remove(HeapAllocator *)',
#   NON-static -- the sibling on line 26, FindNested, is the static one and
#   closes with an alias instead (hal/smoke_aliases_roots.cpp, copied from
#   hal/cxx_aliases.cpp:3299).  A non-static member takes the receiver in ECX,
#   so this row could not have been an /alternatename.
#   The right hand side is not guessed: dumpbin /symbols on this tree's
#   build/port/CMakeFiles/smoke_roots.dir/.../
#   _ZN18NestedHeapIterator6RemoveEP13HeapAllocator.cpp.obj prints
#     009 00000000 SECT3 notype () External | ?Remove@NestedHeapIterator@@QAEXPAUHeapAllocator@@@Z
#   which is what the row below carries.
# It is the same shape as line 515 of port/faces_sync.txt, the sibling
# NestedHeapIterator::AddAt at 0x0204dd98, and facegen.py --sync re-derives it
# against 0x0204dd28 on every build exactly as it does that one.

__ZN18NestedHeapIterator6RemoveEP13HeapAllocator                         ?Remove@NestedHeapIterator@@QAEXPAUHeapAllocator@@@Z             0x0204dd28 R
