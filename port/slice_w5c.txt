# slice_w5c.txt -- lane-owned slice for the linkage campaign (run linkw,
# wave 5, lane w5-c). One matched src TU per line, path relative to the repo
# root, comments with #. THIS FILE HAS ONE OWNER (lane w5-c).
#
# THE ov070 ENEMY PACK plus its two engine-side registry companions, cut to
# what the three fills in port/hal/actor_classes_ov070.cpp actually reference
# (the reference-edge law at the top of slice_w1l4.txt). The night census
# probed 59 func_ov070 TUs; this file carries 51 of them, and each exclusion
# is a measurement, not a preference:
#
#   func_ov070_0211f0a4    the probed COMPILE FAIL (C4716, an int body that
#                          falls off its end into a void face) -- built from
#                          the hostgen emission instead (OV70GEN in
#                          port/CMakeLists.txt, FALLS_OFF_RETURN table)
#   _ZN8daKrpa_c13OnYoshiTryEatEv    \  the seven "recovered from vtable slot identity"
#   _ZN8daKpFr_c13OnYoshiTryEatEv    |  marker bodies in the pack. Wave 5 kept all seven
#   func_ov070_02121438    |  out and inlined the four it needed. Lane w3-d
#   func_ov070_02121fb0    |  byte-matched and adjudicated FlyGuy's three
#   _ZN19daPropeller_Heyho_c16OnAimedAtWithEggEv    |  (below); lane mount-ov070 did the same for the
#   _ZN19daPropeller_Heyho_c13OnYoshiTryEatEv    |  other four (end of file), so ALL SEVEN now link
#   _ZN19daPropeller_Heyho_c13OnTurnIntoEggER6Player    /  from src and no transcription remains in
#                             actor_classes_ov070.cpp.
#
# The four class D1Ev.cpp TUs are also deliberately absent: real MSVC
# destructors over TU-local shadow structs (??1Amp@@UAE@XZ against member
# teardowns the port does not define) -- the E2 treatment, host chains in the
# fill file, each verified against the ROM listing.

# ---- the 51 probed pack bodies --------------------------------------------
src/game/actors/d_a_propeller_heyho.cpp
src/game/actors/daBrq_c.cpp
src/game/actors/d_a_krpa.cpp
src/game/actors/d_a_kp_fr.cpp

# ---- the three registered classes' named bodies and factories -------------
# Reference edge: the three fills. The D1Ev TUs are host chains (header).
# RE-KEYED at the main->port sync (lane SYNC7, the wave-9 fold merge): main folded
# this body into src/game/actors/daBrq_c.cpp (ov070 delinks, .text 0x021210ac..0x02121118, one
# absorbed range), which port/slice_w5c.txt:31 and port/slice_dtorfaces.txt:22 already compile, so the
# symbols this row seated now come from there and the per-function file is gone
# from main's tree.
#src/d_a_brq.c
# ...and the collider initialiser both chomp InitResources bodies call (the
# link named it; its decorated free-function spelling is aliased in the fill
# file).
src/_ZN10dCcAcPos_c4InitEP8dActor_cRK7Vector35Fix12IiES6_jj.cpp

# ---- the probe's class-b closure ------------------------------------------
# The two TUs whose absence the census link measured (LNK2019 with all 59
# wired): the FlyGuy state machine names both.
# src/_ZN8dActor_c23HorzAngleToCPlayerOrAngEv.cpp IS NOT LINKED RAW: it is a
# zero-argument Actor::ClosestPlayer reader (the r0-passthrough seam; the
# closestplayer seat gate catches it by name). The receiver-passing host copy
# below stands in, the OverlayReaders recipe; the matched source is unchanged.
port/unmatched/Actor_HorzAngleToCPlayerOrAng.cpp

# ---- ov070's four static initialisers -------------------------------------
# SharedFilePtrs, dtor-chain nodes, and the state PMF copies. Run from
# ov70_bringup AFTER the PMF seat rewrites the source records. Each name is
# referenced by nothing but its own defining TU (swept src-wide; the ov045
# 0x02112280 collision has no counterpart here).
src/__sinit_ov070_02122afc.c
src/__sinit_ov070_02122d80.cpp
src/__sinit_ov070_02122f30.c
src/__sinit_ov070_02123030.c

# ---- the pack's cross-class face targets ----------------------------------
# Player::Shock is a real method (its face is in actor_classes_ov070.cpp);
# the TU's own deps (func_ov002_020d82f0/020d91e0, Player::ChangeState,
# data_ov002_021100c4) are all in today's map, checked one by one.
src/_ZN6Player5ShockEj.cpp

# ---- POPPING_LAVA_BUBBLES (196, ov002), the census's second bonus find ----
# The registered spawn and the one own-slot body that is a faithful matched
# TU (D1). Init/Behavior/D0 carry the inferred-stub marker; their bodies are
# inline transcriptions in actor_classes_ov070.cpp's id-196 section --
# listings quoted there, same rule as the ov070 pair. func_02022c3c, the
# particle position tracker Behavior calls, is NOT here: gate 29 already
# builds it (a line here was the link's one duplicate-definition finding).
src/actors/daObjLava_c.cpp

# ---- the two host files, carried as slice lines ---------------------------
# The wave-2 self-registration pattern (slice lines resolve against the repo
# root, so port/hal/ paths build without touching CMakeLists.txt). NO OTHER
# WRITER re-proved: _ZTV3Amp / _ZTV10FlameChomp / _ZTV14FlameChompFire are
# defined in actor_classes_ov070.cpp and nowhere else (swept port/hal,
# port/unmatched, src/), and all four ov070 vtables are excluded from
# port/ov070_syms.txt by name.
port/hal/actor_classes_ov070.cpp
port/hal/ov070_shadow_faces.cpp

# ============================================================================
# LANE w3-d: FLY_GUY (232), ov070's fourth class and the one wave 5 left
# unregistered. wave 5's own note said its levels were unmounted; level 19
# (Snowman's Land) places it and level 19 IS mounted, and the baseline census
# on cons cc10acd4e proves it -- "[spawn] actor 0xe8 not registered, skipped",
# and 0xe8 is 232.
#
# port/ov070_syms.txt predicted the rest exactly: "a future FlyGuy row then
# needs only a row and a fill." Both of the usual traps were already disarmed
# there -- _ZTV6FlyGuy was left out of the mount as host storage, and
# ov70_seat_state_pmfs already rewrites all twelve of FlyGuy's PMF source
# records with host addresses along with the other eighteen. The one thing
# that note did not cover was g_profile_PROPELLER_HEYHO, which was not mounted either;
# it is now.
#
# A 31-slot table like its three siblings, and ov70_fill_shared serves it, but
# FlyGuy overrides MORE slots than they do: 0/3/6/9/12/16/17 plus its own
# 18 (OnYoshiTryEat, _ZN19daPropeller_Heyho_c13OnYoshiTryEatEv), 19 (OnTurnIntoEgg,
# _ZN19daPropeller_Heyho_c13OnTurnIntoEggER6Player) and 29 (OnAimedAtWithEgg, _ZN19daPropeller_Heyho_c16OnAimedAtWithEggEv). Read
# out of extracted/overlays/overlay_0070.bin at 0x02123168..0x021231e4; every
# other slot is byte-identical to the three tables this file already serves.
#
# _ZN19daPropeller_Heyho_c6RenderEv.cpp IS NOT IN THIS LIST -- the ModelAnim slot-5
# collision, and this one is the real thing rather than a false alarm. The
# matched TU takes a LOCAL six-virtual ROM-order shadow off &mModelAnim and
# calls its slot 5, and the member really is a ModelAnim: both destructors
# call _ZN9ModelAnimD1Ev on this+0x300 and FlyGuy.h puts mModelAnim there.
# _ZTV9ModelAnim cannot be dual-filled (Virtual18 already occupies MSVC's
# slot 5), so this is the Whomp/Butterfly/Fish/BabyPenguin case exactly.
# HOST COPY at the end of port/unmatched/ModelAnim_Renders.cpp; the matched
# TU stays byte-locked in src/ as proof.
#
# BOTH DESTRUCTORS ARE IN THE LIST, unlike SnowmanBody's and SnowmanHead's:
# _ZN19daPropeller_Heyho_cD1Ev is a plain .c body that names every member destructor and
# offset outright, so there are no local shadow classes for MSVC to emit
# undefined member dtors against and no host chain is needed. It stores
# _ZTV6FlyGuy, the same spelling daPropeller_Heyho_c_classInit.c uses. _ZN19daPropeller_Heyho_cD0Ev.c stores
# the SAME ADDRESS under dsd's other name, _ZTV19daPropeller_Heyho_c
# (symbols.txt carries both on 0x02123168), and gets a per-source rename in
# port/CMakeLists.txt -- the same one-TU-one-name shape the two ov072 D0s got.
#
# Both destructors also end in _ZN12dEnemyBase_cD2Ev rather than Actor::D2:
# FlyGuy derives from Enemy, not straight from Actor. That is ov002 code the
# port already links and nothing here has to bridge.
#
# _ZN19daPropeller_Heyho_c13InitResourcesEv.cpp IS NOT IN THIS LIST EITHER -- a host
# MIRROR, port/unmatched/FlyGuy_InitResources.cpp, whose only delta is
# eight declarations moved. The matched TU declares its eight mounted
# globals (seven SharedFilePtr, one PMF) OUTSIDE its own extern "C"
# block, so MSVC gives them C++ linkage and asks for
# ?data_ov070_02123530@@3USharedFilePtr@@A and seven siblings while the
# mount emits C names -- eight LNK2019s. The mirror moves those eight
# lines inside the block and changes nothing else; the body is character
# for character the matched TU's. Coffin_InitResources.cpp is the same
# shape. Matched TU stays byte-locked in src/ as proof.

# ============================================================================
# LANE mount-ov070: the last four marker bodies, byte-matched and adjudicated.
# All four were run through match.py against extracted/overlays/
# overlay_0070.bin (base 0x0211f000, --module ov070, --strict-relocs) and
# MATCH at 2004/b56: 021211bc and 02121bdc are `mov r0,#5; bx lr` (0x8 each),
# 02121438 is FlameChomp's 0xc0-byte state-0 Kill root (Sound::PlayBank0,
# the velocity block, ModelAnim::SetAnim on data_ov070_021234c4, two
# Particle::System::NewSimple), 02121fb0 is FlameChompFire's 0x20-byte one.
# Rulings in port/tools/inferred_stub_adjudicated.txt; the fill's inline
# transcriptions were retired in favour of these.
