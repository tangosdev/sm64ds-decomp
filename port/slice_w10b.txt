# Run linkw wave 10, lane w10-shared, slice B: MEGA_MUSHROOM (277, ov002) x1 on
# level 22. ONE TU, because the class shares its whole implementation with an
# already-registered sibling.
#
# ==== THE SHARED TABLE =====================================================
# Resolving every ov002 spawn-table entry to the table its factory stores puts
# ids 276 and 277 on ONE table, 0x021083c8, and 276 (ONE_UP_MUSHROOM) is already
# registered -- so the host array _ZTV13OneUpMushroom that
# hal/actor_classes.cpp's hal_fill_one_up_mushroom_vtable fills is already
# filled before this class spawns, and the registry row below names that same
# fill. 31 slots, width agreeing two ways (the load reloc run ends at
# 0x02108444 and _ZTS7daBar_c is the next dsd symbol). RTTI at
# vtable[-1] (0x021083c4 -> 0x0210837c) reads "7da1up_c" -- the 1-up mushroom
# class, which is what the mega mushroom is a re-skin of. The vtable stays OUT
# of the mount, which is the rule port/ov002_syms.txt already states.
#
# ==== BOTH ATTRIBUTION ROUTES ==============================================
# Out of extracted/overlays/overlay_0002.bin at base 0x020ad660:
#     id 277  0x02090cb8 -> 0x021083a4  word[0]=0x020b0530 da1up_c_classInit_SCALEUP_KINOKO
#                                       +4 halfword = 277, +6 priority 141
# The spawn word lands inside ov002 and the id halfword matches the slot it was
# reached from.
#
# ==== NO FACTORY WRAPPER ===================================================
# da1up_c_classInit_SCALEUP_KINOKO is ActorBase::operator new(920), Enemy::Enemy(), a single
# `p[0] = _ZTV13OneUpMushroom` by a REAL name, then four member constructors --
# MovingCylinderClsn at +0x110, WithMeshClsn at +0x144, Model at +0x300,
# ShadowModel at +0x350. None of those writes p[0], so the real table is still
# installed when the factory returns and no reseat wrapper is needed.
#
# ==== THE MOUNT ADDS ONE RECORD ============================================
# port/ov002_syms.txt gains g_profile_SCALEUP_KINOKO:0x1c and nothing else. The
# pin is measured against its own neighbour rather than assumed: 276's record is
# at 0x02108388 and 277's at 0x021083a4, exactly 0x1c apart, and the natural
# next-symbol delta of 0x24 would swallow the vtable's Itanium prologue.
# RE-KEYED at the main->port sync (lane SYNC7, the wave-9 fold merge): main folded
# this body into src/actors/da1up_c.cpp (ov002 delinks, .text 0x020b0530..0x020b0580, one
# absorbed range), which port/slice_gate16.txt:196 and port/slice_pmfb2.txt:193 already compile, so the
# symbols this row seated now come from there and the per-function file is gone
# from main's tree.
#src/d_a_1up_scaleup_kinoko.c
