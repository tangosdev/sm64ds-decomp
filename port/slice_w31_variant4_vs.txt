# slice_w31_variant4_vs -- run linkfull, lane VARIANT4, THE VS ENTRY PAIR.
#
# The same D0-arm campaign shape as port/slice_w31_variant4.txt, for the two
# deleting destructors of scene 6 (SCENE_VS_MENU, the VS / wireless entry
# menu, ov075): dScEntry_c, the scene itself, and UnknownVsEntry. The campaign
# parked both as VS rows. They are enrolled here, apart from the other rows,
# so the commit that adds them can be dropped alone if that park is held: the
# arm touches no wireless or multiplayer code, only the flat D0 name the
# port's slot-17 thunk calls, and scene 6 is in the battery's standard hosted
# scene set.
#
# Each file's `#ifdef _MSC_VER` arm is the qualified, direct call to the one
# host destructor body its D1 file defines, then the class's own operator
# delete (Memory::Deallocate on data_020a0eac): the two calls the retired face
# in port/faces_sync.txt made. The `#else` side is the file's own text, byte
# for byte.
src/_ZN10dScEntry_cD0Ev.cpp
src/_ZN14UnknownVsEntryD0Ev.cpp
