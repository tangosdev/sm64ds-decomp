# Slice ADJ -- run link100 wave 7, the census's 23 GUESSED VTABLE rows.
#
# THIS FILE IS DELIBERATELY INERT. Every src/ line below is commented out, and
# no CMake block reads this file. That is not an oversight, it is the finding.
#
# All 23 rows were disassembled out of the ROM and ruled REAL_DECOMP before
# anything else was considered: port/tools/inferred_stub_adjudicated.txt's
# "run link100 wave 7 (lane ADJ)" section carries the per-row ruling, and
# runs/link100/out/ADJ/verify.txt carries the evidence (465 words compared, 375
# byte-identical, 90 reloc slots, 0 MISMATCH; every compiled relocation offset
# equal to the module's own config reloc rows inside the span).
#
# WHAT STOPS THE SEAT. inferred_stub_guard.py's SEATED is the conjunction of an
# active TU line AND a port/hal file naming the symbol outside comments -- a
# vtable fill. The house law says the same thing from the other side: a TU
# counts only when the ROM's own reference graph reaches it, and a keep-alive
# reference is forbidden. Measured on this tree, 0 of the 23 are named by any
# port/hal fill, and every in-module ROM edge any of them has is a vtable-slot
# `load` (not one has an in-module arm_call, so no linked ROM caller reaches
# one on its own). Every hal file that would carry the fill belongs to another
# lane, so this lane cannot complete a single seat.
#
# AN ACTIVE SLICE LINE IS NOT A SEAT, and two of these rows prove it:
# src/_ZN18daObjBkBillboard_cD0Ev.c and src/_ZN17daObjBk_Ukisima_cD0Ev.c have been on
# slice_gate59.txt for a while, they compile on every build (their .obj files
# are under build/port/CMakeFiles/walk_window.dir/), and neither symbol appears
# in walk_window.map, because nothing references them.
#
# Uncommenting a line here without ALSO landing the port/hal fill named in its
# blocker adds compile time and nothing else.
#
# ---- arm9 -----------------------------------------------------------------
# blocker: host array _ZTV10dCcAcPos_c[12] (hal/actor_vtables.cpp:412),
# slot 3 filled there; and the src TU is a zero-argument tail-call veneer, which is
# receiver-destroying on the host (the shape hal/fdr_arm9_fader_seat.cpp:167 writes up).
#src/func_02014a20.c
# blocker: host array data_0208ea6c[12] (hal/scene_boot.cpp:1219); slots 3 and 4 are
# TRANSCRIBED rather than forwarded, and scene_boot.cpp:1533-1560 records the decision.
#src/_ZN10dFdDummy_c14SetForwardTimeEj.cpp
#src/_ZN10dFdDummy_c15SetBackwardTimeEj.cpp
# blocker: same table, slot 2; zero-argument veneer onto _ZN5Fader13AdvanceInterpEv.
#src/_ZN10dFdDummy_c11AdvanceFadeEv.c
# blocker: hal/fdr_arm9_fader_seat.cpp:167-183 rules this row explicitly -- slot 0x20
# forwards through fdr_s20 to the same target the ROM veneer jumps to, and the src TU
# is host-broken (no parameter, no argument).
#src/_ZN7dWipe_c8SetToEndEv.c
#
# ---- ov002 ----------------------------------------------------------------
# blocker: _ZTV13BigBrickBlock[32] (hal/actor_classes.cpp:711) does not name it.
#src/_ZN13BigBrickBlock11OnAttacked1ER8dActor_c.c
# blocker: a HOST DEFINITION of the same symbol exists under a PORT_HOST_ABI tag at
# hal/actor_classes_ov002_w17.cpp:109 (ROM Itanium slot 5 is ModelAnim::Render, MSVC
# slot 5 is ModelAnim::Virtual18). Adding this TU double-defines the symbol.
#src/_ZN13daObjWakame_c6RenderEv.cpp
#
# ---- ov003 (dScTitle_c) ---------------------------------------------------
# blocker: the class's table data_ov003_020b1650 is ABSENT from the port -- two src
# TUs declare it extern, nothing defines or mounts it. These six would not link.
#src/_ZN10dScTitle_cD0Ev.c
#src/_ZN10dScTitle_c16OnPendingDestroyEv.c
#src/_ZN10dScTitle_c6RenderEv.cpp
#src/_ZN10dScTitle_c8BehaviorEv.cpp
#src/_ZN10dScTitle_c16CleanupResourcesEv.c
#src/_ZN10dScTitle_c13InitResourcesEv.cpp
#
# ---- ov006 ----------------------------------------------------------------
# blocker: zero-argument veneers; the seat thunk passes the receiver INTO the veneer
# (lane BNP's own ruling) and tailjump_guard asserts the jmp form survives.
#src/_ZN14dScMgD3DBase_c19OnHitFromUnderneathEv.c
#src/_ZN14dScMgD3DBase_c15OnHitByMegaCharEv.c
# blocker: slot 17 of the mounted tables data_ov006_0213d9cc / 0213dbbc is served by
# port/unmatched/MgPachinko_Dtor.cpp and MgPachinko2_Dtor.cpp; the VT/HEAP placeholders
# would also need a per-TU COMPILE_DEFINITIONS rename.
#src/_ZN15dScMgPachinko_cD0Ev.c
#src/_ZN16dScMgPachinko2_cD0Ev.c
# already on slice_mga361.txt:222; slot 2 is deliberately paid by
# port/unmatched/MgFlower_Slot2.cpp because the ROM's r1 rides through.
#src/_ZN19dScMgSingle3DBase_c18AfterInitResourcesEj.cpp
#
# ---- ov015 -- THE CLOSEST FOUR TO A SEAT IN THE WHOLE 23 ------------------
# Both TUs are already on slice_gate59.txt (:57 and :79), both compile every build,
# and neither symbol reaches walk_window.map, because the class fill does not name
# it. Their tables ARE in the port: hal/actor_classes_wf.cpp:415 defines
# _ZTV18daObjBkBillboard_c (aliased _ZTV18daObjBkBillboard_c) and :543 defines
# _ZTV17daObjBk_Ukisima_c (aliased _ZTV17daObjBk_Ukisima_c), and hal_fill_pole_billboard_vtable
# / hal_fill_rotating_platform_wf_vtable already fill slots 0/3/6/9/16 of each with
# __fastcall thunks. Each of these two rows is SLOT 17, the deleting destructor, one
# word past the slot-16 D1 those fills already seat (pb_d1 -> _ZN18daObjBkBillboard_cD1Ev,
# rp_d1 -> _ZN17daObjBk_Ukisima_cD1Ev). So the seat is one thunk and one vt[17] line per
# class in hal/actor_classes_wf.cpp, plus the per-TU placeholder rename in
# out/ADJ/bugs.md. That file is not this lane's; the proposal is in
# out/ADJ/seatability.md.
#src/_ZN18daObjBkBillboard_cD0Ev.c
#src/_ZN17daObjBk_Ukisima_cD0Ev.c
# blocker: slots 23 and 24 of _ZTV13PoleBillboard (hal/actor_classes_wf.cpp:472, filled
# at :501), which that fill leaves on its shared default. Both bodies are zero-argument
# tail-call veneers onto func_ov015_02111414, so the thunk has to pass the receiver in
# and tailjump_guard has to see the jmp form survive, the way lane BNP did it for the
# ov006 pair.
#src/_ZN14KnockDownPlank8OnKickedER8dActor_c.c
#src/_ZN14KnockDownPlank11OnAttacked2ER8dActor_c.c
#
# ---- ov064 ----------------------------------------------------------------
# blocker: the mounted table _ZTV19BowserPuzzleManager is "raw DS words, pack-checked, never
# dispatchable" (hal/actor_classes_bowserpuzzle.cpp:351-355); dispatch runs through the
# host array port_bp_shell_vtable[31].
#src/_ZN19BowserPuzzleManagerD0Ev.c
