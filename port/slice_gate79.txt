# Gate 79: STAR_MARKER (180), ov002. The invisible course-star marker. A plain
# Actor subclass (the Rabbit shape), 20-slot vtable, own slots 0/3/6/9/12/16/17.
# Init/Clean/Behavior/Render/OnPendingDestroy are real C++ methods; D0 is
# extern-C .c; D1 is a real C++ destructor in a .cpp (MSVC ??1StarMarker@@UAE@XZ,
# reached through a __fastcall shim aliased onto it in hal/actor_classes.cpp).
# Factory + the eight lifecycle TUs + the one Behavior helper. Its SpawnInfo is
# mounted in ov002_syms.txt; the vtable is a host array the registry fills.
src/d_a_star_base.c
src/_ZN10StarMarker13InitResourcesEv.cpp
src/_ZN10StarMarker16CleanupResourcesEv.cpp
src/_ZN10StarMarker8BehaviorEv.cpp
src/_ZN10StarMarker6RenderEv.cpp
src/_ZN10StarMarker16OnPendingDestroyEv.cpp
# _ZN10StarMarkerD1Ev.cpp is a real C++ destructor; MSVC re-synthesizes its base
# and member dtor calls under MSVC manglings (??1Model@@QAE@XZ ...) that do not
# resolve to the extern-C ROM bodies, so its body is a host copy in
# port/unmatched/StarMarker_D1.cpp -- the ROM D1's own four teardown calls plus
# the vtable restore, byte-faithful (it is exactly D0 minus the Deallocate).
# RETIRED at SLICE1: body now in src/_ZN10StarMarkerD1Ev.cpp. MSVC folds the ROM destructor variants into one symbol, so only one of a class two per-function destructor TUs can be compiled.
#src/_ZN10StarMarkerD0Ev.cpp
# the Behavior's one helper (the marker's tracked-actor follow path)
src/_ZN10StarMarker7CollectEv.cpp
