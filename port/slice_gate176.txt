# Gate 176: SCUTTLEBUG (255), the spider daSpd_c, and ov071's FIRST mount. The
# whole class is matched src -- the factory, InitResources/Behavior/Render (real
# MSVC members against include/Scuttlebug.h), Cleanup/OnPendingDestroy/D0 (plain
# C), the two interaction overrides (OnYoshiTryEat _ZN10Scuttlebug13OnYoshiTryEatEv,
# OnAimedAtWithEgg _ZN10Scuttlebug16OnAimedAtWithEggEv) and OnTurnIntoEgg (_ZN10Scuttlebug13OnTurnIntoEggER6Player),
# the eighteen state-machine halves the two dispatchers reach, the helpers those
# reach, and ov071's four sinits (all self+arm9 reach, verified).
#
# EXCLUDED from the slice, host-copied in port/unmatched/Scuttlebug_StateDispatch.cpp:
#   func_ov071_021202b4.cpp -- the ENTER dispatcher; mwcc PMF read MSVC mis-forms
#   func_ov071_02120278.cpp -- the MAIN dispatcher; `c->pp + 1` mwcc/MSVC stride
# EXCLUDED, host thunk in the fill (hal/actor_classes_scuttlebug.cpp):
#   _ZN10ScuttlebugD1Ev.cpp -- the auto-emitted-member-dtor form, the flame D1
#   treatment; the thunk runs the D0 chain minus the final Deallocate.
#
# The vtable _ZTV10Scuttlebug stays OUT (host array the registry fills). The
# state records the sinit builds carry DS code addresses; the seat over the
# eighteen SOURCE PMFs (data_ov071_02122b60..02122be8) rewrites them to host
# bodies before __sinit_ov071_021226ac copies them into data_ov071_02122fa8.
#
# Cross-overlay reach is ov002 only (the engine overlay, always mounted): six
# Player methods -- IncMegaKillCount, IsOnShell, Hurt, Bounce, IsCollectingCap,
# RegisterEggCoinCount -- all linked and faced.
src/actors/Scuttlebug.cpp
# Render is NOT here: it dispatches ModelAnim slot 5 through a ROM-order
# local shadow (the Whomp/Fish case); the host copy is in
# port/unmatched/ModelAnim_Renders.cpp.
# RETIRED at SLICE1: body now in src/_ZN10ScuttlebugD1Ev.cpp. MSVC folds the ROM destructor variants into one symbol, so only one of a class two per-function destructor TUs can be compiled.
#src/_ZN10ScuttlebugD0Ev.cpp
src/__sinit_ov071_021226ac.c
src/__sinit_ov071_021228c8.c
src/__sinit_ov071_02122a1c.c
src/__sinit_ov071_02122a64.c
# Three arm9 helpers the state bodies call, matched C-form src the supervisor
# added after the closure audit: the raycast probe, the non-vanish player
# finder and the wall-result flag read.
src/_ZN8dActor_c17DetectRaycastClsnER7Vector3S1_b.cpp
src/_ZN8dActor_c22ClosestNonVanishPlayerEv.cpp
src/_ZNK10dBgCh_Actr14GetResultFlag1Ev.cpp
# The anim loader InitResources reaches through decl_common's friendly
# AnimLoadFile name: the STATIC Animation::LoadFile, matched src, aliased in
# the fill.
src/_ZN9Animation8LoadFileER13SharedFilePtr.cpp
