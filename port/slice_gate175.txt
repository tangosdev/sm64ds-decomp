# Gate 175: BLUE_FLAME (317) + RED_FLAME (316), the cave's flames (ov002, the
# engine overlay -- always mounted). One class, two matched factories, both
# installing _ZTV9BlueFlame; RED_FLAME's registry row is fill=0, the
# BrickBlock/GreenShellBlockTag shape. Level 13 names RED_FLAME x17.
#
# The whole class is C-linkage matched src: the two factories, InitResources,
# Behavior, D0, and the two tiny tail overrides (OnYoshiTryEat 0x020b59a0,
# OnTurnIntoEgg 0x020b599c). No pointer-to-member table anywhere, so no host
# copies and no seat. D1 is a host thunk in the fill
# (hal/actor_classes_flame.cpp): the matched _ZN11daObjFire_cD1Ev.cpp is the
# auto-emitted-member-dtor form.
#
# Player::Burn rides along: the Behavior's contact path calls it by C name;
# the matched body is the real MSVC method, and the burn state it changes the
# player into (data_ov002_021100dc, __sinit_ov002_021019d0) was already
# constructed and its St_BurnFire bodies already linked -- the face in the
# fill bridges the C spelling.
src/actors/daObjFire_c.cpp
src/_ZN6Player4BurnEv.cpp
