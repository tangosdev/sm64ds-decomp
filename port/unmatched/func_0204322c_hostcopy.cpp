/* RETIRED HOST COPIES of src/func_0204322c.c (the RENDER Process) and
 * src/func_020432e4.c (the CLEANUP Process), run linkfull lane PMF2. Both
 * matched TUs are on port/slice_w27_pmf2.txt, built from hostgen copies whose
 * VIRTUAL_CALL rows (port/tools/hostgen.py) resolve the three static
 * {vtable byte offset, 1} records each wrapper passes to fBase_c::Process into
 * the vtable slots they name -- render 9/10/11, cleanup 3/4/5 -- the treatment
 * the init and behaviour wrappers (func_0204335c, func_02043288) have had since
 * lane shadow-A. This file defines nothing and stays only because
 * LEVELBOOT_HOST_SOURCES names it by path.
 */
