/* RETIRED HOST COPY of _ZN7daTrs_c13InitResourcesEv (ov063 0x0211b9bc, 0x9a0
 * bytes), vtable slot 0 of _ZTV7daTrs_c. Nothing in this file is compiled any
 * more.
 *
 * It was a faithful mirror of the matched TU, kept because the TU's old K&R
 * `extern int _ZN8dActor_c13ClosestPlayerEv();` declaration tripped
 * tools/closestplayer_guard.py. The synced TU declares ClosestPlayer with its
 * receiver and compiles clean under the port's own compile line, and what
 * still held it out was its port/faces_sync.txt FORWARD row, which defined the
 * decorated member beside it (port/slice_w5a.txt's note on this line). Run
 * linkfull lane SMALLS2 flipped that row to REVERSE and put
 * src/game/actors/daTrs_c/_ZN7daTrs_c13InitResourcesEv.cpp on
 * port/slice_w31_smalls2.txt, so slot 0's flat name is the generated face into
 * daTrs_c::InitResources, which is the ROM's own code (MATCHING 2004/b56,
 * --strict-relocs, at ov063 0x0211b9bc). The mirror and the matched body make
 * the same calls in the same order.
 *
 * The file stays, empty, because port/slice_w5a.txt still lists it; deleting
 * it is a one-line change there.
 */
