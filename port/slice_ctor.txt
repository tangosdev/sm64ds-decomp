# The ROM's own static-initialiser runner, at Entry's own point.
#
# Run link100, lane CTOR, rung C1a. hal/ctor_runner.cpp carries the derivation;
# this is the list of matched TUs that become reachable because of it, and why
# each one links.
#
#   src/func_02072f94.c        CodeWarrior's .ctor walker. Entry calls it at
#                              0x020048a8, one instruction before the tail jump
#                              to main; hal/rom_main.cpp's port_rom_main_run()
#                              now calls it at exactly that point. It walks the
#                              table hal/ctor_runner.cpp publishes at the ROM's
#                              own 0x02086b60.
#
# THE EIGHT INITIALISERS THIS RUNG ANSWERS FOR. Every name each of them
# references was resolved against this tree's own build/port/walk_window.map
# before the file was written, not guessed:
#
#   src/__sinit_02074d90.c     zeroes data_0209b468[0] and [1]. Hosted in
#                              hal/actor_vtables.cpp:234. No other reference.
#   src/__sinit_02074da8.c     func_02011a5c(data_0209b53c). The array is
#                              hal/actor_vtables.cpp:604; the body is below.
#   src/__sinit_02074dbc.c     empty body.
#   src/__sinit_02074dc0.c     empty body.
#   src/__sinit_02074e80.c     empty body.
#   src/__sinit_02074fb8.c     stores &data_0208ee14 then &data_02094390 into
#                              data_020a0c68[0], and 0 into [1]. data_02094390
#                              is hal/arm9_tables_link100.cpp, data_020a0c68 is
#                              hal/scene_link100_boot.cpp:202, and data_0208ee14
#                              is hosted by hal/ctor_runner.cpp because nothing
#                              else in the port hosts it.
#   src/__sinit_020750b4.c     empty body.
#   src/__sinit_02075150.c     empty body.
#
# TWO MORE TUs RIDE IN AND BOTH ARE REAL EDGES, not keep-alives:
#
#   src/func_02011a5c.c        __sinit_02074da8's only callee. Self-contained:
#                              it zeroes the struct it is handed and names no
#                              other symbol.
#   src/_ZN5Scene14GraphCallback2Ev.c
#                              the third word of data_0208ee14. Its three
#                              siblings (GraphCallback0/1/3) were already in
#                              the link; this one had no reference until the
#                              table that names it was hosted.
#
# ---- RUNG C1b: SIX MORE INITIALISERS AND ONE CALLEE -----------------------
#
# Each of the six wanted exactly one thing the port did not host: the 12-byte
# destruct-node cell func_020731dc pushes for it. hal/ctor_runner.cpp hosts all
# seven cells at their ROM spans (data_020a0ec8 is 8 bytes, the object, and
# data_020a0ed0 the node beside it -- __sinit_020750ec names both).
#
#   src/__sinit_02074dc4.c     func_0203b9b4(&data_0209d4b8, 1), a node at
#                              data_0209d4bc, then Timer's constructor on
#                              data_0209d4c8. Every other name it uses is
#                              already hosted (hal/auto_bss.cpp) or linked
#                              (func_0203b9b4, func_0201973c).
#   src/__sinit_02074e44.c     the same shape, a node at data_0209e654.
#   src/__sinit_02074fe4.c     func_02037eec(data_020a0cec) -- both linked and
#                              hosted -- and a node at data_020a0ce0.
#   src/__sinit_020750b8.c     zeroes data_020a0ebc (hal/auto_bss.cpp:413, an
#                              exact 12-byte host) and a node at data_020a0eb0.
#   src/__sinit_020750ec.c     zeroes data_020a0ec8 and a node at data_020a0ed0.
#                              CWSCOUT filed it under C1c because it names two
#                              symbols; the audit found no host writer of
#                              either, so it is a C1b row.
#   src/__sinit_0207511c.c     zeroes data_020a0edc (hal/auto_bss.cpp:417, an
#                              exact 8-byte host) and a node at data_020a0ee4.
#   src/func_0201964c.c        Timer's constructor, __sinit_02074dc4's callee.
#                              Its own callee Timer::ResetTimer is already in
#                              the link.
#
# NOT HERE, and each has its own rung in lane CWSCOUT's rungs.tsv. Seven of the
# 23 words are bound to a face in hal/ctor_runner.cpp that names the
# initialiser and what stopped it:
#
#   C1b, REFUSED  __sinit_02074e0c: its callee chain func_0201aa18 ->
#        func_0201aad4 -> func_0201aac8 -> func_02059ba0 passes an argument
#        through src/func_0201aac8.c, a tail-call veneer whose decompiled C
#        takes none. That value rides ARM r0 and does not ride a host call, so
#        linking it would call func_02059ba0 at Entry with whatever the host
#        left behind. A PORT_HOST_ABI ruling, not a linkage row.
#   C1b, REFUSED  __sinit_0207501c: it constructs a RaycastLine into
#        data_020a0d0c, which hal/clsn_vtable.cpp hosts as a 0x10 block in a
#        grouped section whose neighbours hal/mmc_vtable.cpp:148 asserts at ROM
#        spacing; the constructor writes past +0x64. That is a layout question
#        owned by those two files.
#   C1c  __sinit_02075054 and __sinit_02075154 stay faced -- see
#        out/CTOR/c1c_audit.txt: both have a live host second writer
#        (hal/actor_registry.cpp:412 seats the same five callbacks;
#        hal/auto_bss.cpp already hosts the same ROM bytes twice).
#        __sinit_02074edc, the third of that group, is TAKEN below.
#   C1d  TAKEN, below.
#   C1e  TAKEN, below.
#
# TWO INITIALISERS ARE ALREADY IN THE LINK AND ARE NOT LISTED AGAIN.
# src/__sinit_02073e6c.c is on slice_intro.txt and src/__sinit_02074f80.c is on
# slice_fdr.txt. Both were HAND-CALLED until this commit and both are now words
# in the table; the two hand calls are deleted in the same commit and the two
# words go through counting shims so the run can say each ran exactly once.
# ---- RUNG C1d: THE arm9 CLIPPER'S OWN INITIALISER -------------------------
#
#   src/__sinit_02074e84.cpp   two pointer stores into data_0209f3c4 (the
#                              second wins, the first is dead in the ROM),
#                              Clipper's constructor on data_0209f43c, and one
#                              func_020731dc push at data_0209f388.
#   src/_ZN7ClipperC1Ev.c      writes the vptr and calls Clipper::Func_020156DC
#                              with the ROM's own 4:3 seed. Its callee is
#                              already in the link (port/slice_gate10.txt).
#   src/_ZN7ClipperD1Ev.c      the first word of _ZTV7Clipper.
#
# hal/ctor_runner.cpp hosts what none of the three had: _ZTV7Clipper (two
# words; word 0 is D1 and word 1 is a NAMED TRAP, because src/_ZN7ClipperD0Ev.c
# calls base_dtor_Clipper, an invented name for 0x0203cbcc that nothing in this
# tree defines) and the 12-byte cell data_0209f388. It also carries the five
# MSVC spellings the //cpp initialiser emits, transcribed from dumpbin.
#
# ---- RUNG C1e: THE 19 CAMERA STATE OBJECTS, FILLED BY THE ROM ------------
#
#   src/__sinit_02073a24.c     38 struct copies out of arm9 .rodata into the
#                              nineteen State objects at 0x0209b008..0x0209b138.
#                              It calls nothing.
#
# hal/ctor_runner.cpp hosts the 38 eight-byte source pairs, which nothing in
# the port hosted. They keep their DS addresses: they are mwcc pointers to
# member and hal/camera_states.cpp's hal_call_camera_state_fn is what turns one
# into a host call. The nineteen DESTINATIONS are hal/camera_states.cpp's and
# stay its; that file bakes the same values in at compile time, and all 19 x 4
# words were compared against the image before this rung was taken (19 of 19
# identical, out/CTOR2/c1e_equality.txt), so the ROM's own initialiser writes
# exactly the bytes that are already there.
#
# ---- RUNG C1c: THE TWO arm9 FADER STATICS --------------------------------
#
#   src/__sinit_02074edc.c     constructs a FaderColor into data_0209f5e8 and a
#                              FaderBrightness into data_0209f5d0, and pushes a
#                              destruct node for each. Both destructors
#                              (_ZN10FaderColorD1Ev, _ZN15FaderBrightnessD1Ev)
#                              and all three vtables (data_0208eafc,
#                              data_0208eacc, data_0208eb2c) were already in
#                              the link, so this row is one TU.
#
# hal/ctor_runner.cpp hosts the two 12-byte node cells, which nothing hosted.
# It retires hal/method_faces.cpp's "do not wake the branch without giving
# data_0209f5d0 a real vptr first": the vptr is now data_0208eacc.
#
# AND IT MOVES ONE LIVE VPTR. hal/fader_wipes.cpp placement-news a host
# HalFaderWipe over data_0209f5e8 from a C++ static object, which runs before
# main; the ROM's initialiser runs at Entry and is therefore the last writer,
# so the installed colour fader dispatches hal/scene_boot.cpp's ten seated
# data_0208eb2c slots. The [ctor] line names the table each vptr landed on in
# every captured run.
#
src/func_02072f94.c
src/func_02011a5c.c
src/_ZN5Scene14GraphCallback2Ev.c
src/__sinit_02074d90.c
src/__sinit_02074da8.c
src/__sinit_02074dbc.c
src/__sinit_02074dc0.c
src/__sinit_02074e80.c
src/__sinit_02074fb8.c
src/__sinit_020750b4.c
src/__sinit_02075150.c
src/func_0201964c.c
src/__sinit_02074dc4.c
src/__sinit_02074e44.c
src/__sinit_02074fe4.c
src/__sinit_020750b8.c
src/__sinit_020750ec.c
src/__sinit_0207511c.c
src/__sinit_02074e84.cpp
src/_ZN7ClipperC1Ev.c
src/_ZN7ClipperD1Ev.c
src/__sinit_02073a24.c
src/__sinit_02074edc.c
