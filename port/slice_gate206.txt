# Gate 206 (run link60, lane A2): DIAMOND_LIFT (135), the first class seated
# out of ov043 -- Bowser in the Dark World's own overlay, level 35.
#
# THE SLICE IS ID 135 ALONE, and that is the whole recommendation of
# port/ov043_syms.txt: six of level 35's eight skipped instances, and the only
# one of the overlay's three placed classes whose factory is CLEAN
# (daObjKm1_Ukishima_c_classInit.c names _ZTV19daObjKm1_Ukishima_c outright, no placeholder).
# Ids 134 and 137 stay skipped: 137's factory hides its real table behind a
# placeholder AND spells an invented class name that means a DIFFERENT
# overlay's base table, and both 134 and 137 need ov002 base bodies this slice
# does not pull in. Their cost is written up in port/ov043_syms.txt; nothing
# about them is guessed at here.
#
# ov043 is already whole-mounted in PORT_LEVEL_OVERLAYS. This is the SECOND
# mount of the same overlay, per symbol, the dual-mount shape ov012/ov013/
# ov021/ov044/ov045/ov046 already use. 105 symbols, 42 pointer patches, 12
# cross-mount candidates.
#
# ==== THE WIDTH: 32, AND EVERY ROUTE AGREES ================================
# `python port/tools/vtspan.py . _ZTV19daObjKm1_Ukishima_c` on this tree:
#     tail 32   typeinfo 32   terminator 32   raw run 32   next dsd symbol 26
# with "slot 31 is Platform::Kill and slot 32 is not a code pointer". Four
# routes agree and only the next-dsd-symbol route disagrees, which is the
# documented under-read. The Platform shape, 32 slots. Six words are ov043's
# own: 0 InitResources 0x02111320, 3 CleanupResources 0x0211123c, 6 Behavior
# 0x021112a8, 9 Render 0x02111280, 16 D1 0x021111a0, 17 D0 0x021111e4. Slot 31
# is the shared Platform::Kill.
#
# ==== SLOT 9 IS THE SLOT-5 SHADOW, AND IT IS SAFE ==========================
# src/game/actors/d_a_obj_km1_ukishima.cpp dispatches slot 5 of a six-virtual shadow over
# the member at +0xd4. Adjudicated from CONSTRUCTION, the wave-19 method, and
# three matched TUs witness the same thing:
#   src/game/actors/d_a_obj_km1_ukishima.cpp   Model::LoadFile -> ModelBase::SetFile at c+0xd4
#   src/game/actors/d_a_obj_km1_ukishima.cpp   _ZN5ModelD1Ev(t + 0xd4)
#   src/game/actors/d_a_obj_km1_ukishima.cpp   _ZN5ModelD1Ev(t + 0xd4)
# A plain Model, and hal/cxxname_bridge.cpp dual-fills _ZTV5Model at [4] AND
# [5], so the shadow's slot 5 lands on Model::Render. The SlideDecoration
# ruling, not the Snufit/Swoop/Dorrie/MontyMole one. The matched TU is linked.
#
# ==== THE AUTHORIZATION THIS SEAT RIDES ON =================================
# FIVE of the eight TUs below carry the inferred-stub marker "recovered from
# vtable slot identity" -- _ZN19daObjKm1_Ukishima_c13InitResourcesEv, _ZN19daObjKm1_Ukishima_c16CleanupResourcesEv,
# _ZN19daObjKm1_Ukishima_c8BehaviorEv, _ZN19daObjKm1_Ukishima_c6RenderEv and _ZN19daObjKm1_Ukishima_cD0Ev. They are
# seated here on wave 16's pre-existing per-body ruling in
# port/tools/inferred_stub_adjudicated.txt:71-77, which grades all five
# REAL_DECOMP against w16.md#135 and whose own header note says the seating was
# "deferred (needs an ov043 per-symbol mount, which this lane did not build)".
# This lane builds that mount, so the deferral is what is being discharged; no
# new adjudication is made here and none of the five is re-graded. The other
# three TUs (the factory, the D1 and the sinit) carry no marker.
#
# ==== THE FOUR TRAPS, EACH RE-VERIFIED BY DISASSEMBLY ON THIS TREE =========
#
# (1) THE G0/G1 TRAP, the live one. src/game/actors/d_a_obj_km1_ukishima.cpp releases two
#     SharedFilePtrs spelled G0 and G1, and hal/cxx_aliases.cpp has already
#     bound the single global name _G0 to _data_020a0eac (the GAME HEAP
#     pointer) and _G1 to _data_ov002_0210e05c (SignPost's KCL). Neither would
#     fail at link, and the body would Release the game heap pointer as if it
#     were a SharedFilePtr -- the CannonHatch/MetalNet shape.
#     Re-verified here, disassembling ov043 0x0211123c..0x02111280:
#         0211125c  ldr r0, [pc, #0x14]   -> literal 0x02111278
#         02111260  bl  0x2017b64         (SharedFilePtr::Release)
#         02111264  ldr r0, [pc, #0x10]   -> literal 0x0211127c
#         02111268  bl  0x2017b64
#     and the two literals' relocs are
#         from:0x02111278 kind:load to:0x021125e8 module:overlay(43)
#         from:0x0211127c kind:load to:0x021125e0 module:overlay(43)
#     so G0 = 0x021125e8 and G1 = 0x021125e0, both ov043's OWN bss cells and
#     both in the mount. The remedy is the PATH_LIFT one: a per-source -D on
#     this file ONLY, to private names this lane owns, bound onto the mounted
#     objects with a C-name /alternatename in hal/actor_classes_ov043.cpp. It
#     cannot go straight to the mount names -- include/decl_common.h:396
#     declares `extern int G1[];` and MSVC rejects the redeclaration.
#
# (2) THE DESTRUCTOR'S G0 IS A DIFFERENT OBJECT AND IS LEFT ALONE.
#     src/game/actors/d_a_obj_km1_ukishima.cpp passes G0 to Memory::Deallocate as the HEAP.
#     Its literal at 0x02111238 relocs to 0x020a0eac module:main -- the game
#     heap -- which is exactly what cxx_aliases.cpp already binds _G0 to. No
#     rename on that file's G0. The per-source -D there covers VT0/VT1 only.
#
# (3) THE TWO ov047-SPELLED CELLS. src/game/actors/d_a_obj_km1_ukishima.cpp spells ov043's
#     own bss cells data_ov047_021125e8 and data_ov047_021125e0. Its load
#     sites are the same two addresses the sinit initialises, both
#     module:overlay(43); ov043 and ov047 are distinct objects sharing the
#     load window and neither ov047 name resolves in port/ today, so this is
#     a hard link error rather than a silent miscall. Renamed per source to
#     the ov043 names. Safe as a straight -D and not a private name:
#     decl_common.h declares data_ov047_021125e0/e8 as `extern int [];` and
#     declares no data_ov043_021125e0/e8 at all, so there is nothing to
#     conflict with.
#
# (4) THE PLACEHOLDER VTABLES. src/game/actors/d_a_obj_km1_ukishima.cpp (D0) writes VT0 then
#     VT1; src/game/actors/d_a_obj_km1_ukishima.cpp (D1) writes the same pair BY NAME,
#     _ZTV19daObjKm1_Ukishima_c then _ZTV10dBgActor_c. Both pairs are the same
#     two addresses, read off the D0's own literal pool:
#         from:0x02111230 kind:load to:0x021122b8 module:overlay(43)
#         from:0x02111234 kind:load to:0x0210ae38 module:overlay(2)
#     0x0210ae38 is the Platform base table and IS ALREADY SOLVED in this tree:
#     hal/actor_classes.cpp:583 defines `void *_ZTV10dBgActor_c[32]` and
#     hal_fill_platform_vtable fills it and _ZTV10dBgActor_c identically. So the
#     D1 needs no rename at all, and the D0 needs VT0/VT1 renamed to the two
#     real hosted names -- never to a placeholder, and never trusting one.
#     0x021122b8 is excluded from the mount and is the host array
#     hal/actor_classes_ov043.cpp declares. It is defined under the ROM's own
#     class name, which comes from the table's OWN RTTI record and not from a
#     dsd label: vtable[-1] = 0x02112270, whose word[1] names
#     "19daObjKm1_Ukishima_c". The factory's spelling _ZTV19daObjKm1_Ukishima_c
#     is an /alternatename onto it.
#
# ==== ONE SINIT RUNS, NOT FOUR ============================================
# port/ov043_syms.txt's cost estimate says "the four sinits behind a bring-up".
# Reading the four bodies says one, and the ov081 rule (a sinit for an
# unhosted class stays OFF) says the same:
#   __sinit_ov043_021117fc  IS id 135's -- Model file 1625 into
#       data_ov043_021125e8 and collision file 1626 into data_ov043_021125e0,
#       the exact two cells G0/G1 above release. RUNS.
#   __sinit_ov043_02111868  builds 0x02112610/0x02112608, another class's pair.
#   __sinit_ov043_021118d4  builds 0x02112638/0x02112630, a third class's pair.
#   __sinit_ov043_02111940  builds twenty cells and spells every one of them
#       with a sibling overlay's name -- SIXTEEN data_ov047_* and FOUR
#       data_ov052_*. The two halves do NOT behave the same and the earlier
#       reading here that "none of those resolve" was wrong: all four
#       data_ov052_* (0x02112680, 0x02112688, 0x02112690, 0x021126a8) DO
#       resolve in this link, and only the sixteen data_ov047_* are
#       unresolvable. So switching it on would be a hard link error on sixteen
#       names AND four silent bindings into another overlay's objects, which is
#       worse than the single failure mode first written down. The conclusion
#       is unchanged and now rests on the ov081 rule rather than on the link
#       error: the cells it builds belong to classes this slice does not host.
#       It stays OFF and is not on the slice.
# Running only 021117fc is what the hosted class needs and nothing more.
src/game/actors/d_a_obj_km1_ukishima.cpp
src/__sinit_ov043_021117fc.c
