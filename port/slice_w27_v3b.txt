# slice_w27_v3b -- run linkfull wave 27, lane V3B, THE VARIANT ROWS THAT
# WAITED ON HAL FILES.
#
# BATCH 2, FOUR DELETING DESTRUCTORS (D0, vtable slot 17). Each file below is a
# per-function src D0 TU carrying the same one Cls::~Cls() definition as its
# D1 sibling, which is why it was never compiled for the port: under MSVC that
# definition is the D1 file's ??1Cls a second time (LNK2005). It gains the
# campaign's `#ifdef _MSC_VER` arm (runs/linkfull/out/D0ARM1/campaign_recipe.txt):
# the ROM's flat D0 name, defined as the qualified, direct call to that one
# host destructor, then the class's own inline operator delete -- the two
# steps the ROM body makes. The `#else` side is the file's own text, byte for
# byte, so the mwccarm object is unchanged.
#
# What names each flat D0 on the port, and what it replaced there:
#   dScMgPachinko_c   hal/scene_mg.cpp's slot-17 face -> port_mg_pachinko_d0
#                     (port/unmatched/MgPachinko_Dtor.cpp, a hand copy until now)
#   BowserPuzzleManager  shl_d0, hal/actor_classes_bowserpuzzle.cpp (a hand
#                     teardown on the host shell table until now)
#   dScTitle_c        port_title_d0, hal/title_vtable_seat14d.cpp (a hand
#                     spelling of the same two steps until now)
#   ShadowModel       ShadowModel::Destructor0, hal/int4_rows.cpp (the body was
#                     port/unmatched/ModelFamily_Dtors_HostCopy.c's copy)
# tools/dtor_store_guard.py's SRC_ARMS names each of these files and re-proves
# from the ROM body on every build that the transient MSVC vtable the host
# destructor stores is never dispatched through before the base restore.
#
# Gated per file: mwccarm 2004/b56 object identical before and after,
# match.py 2004/b56 strict MATCHING, linkcheck VERIFIED blind 0, and the
# arm's object checked against the call list of the body it replaces
# (runs/linkfull/out/V3B/).
src/_ZN15dScMgPachinko_cD0Ev.cpp
src/_ZN19BowserPuzzleManagerD0Ev.cpp
src/_ZN10dScTitle_cD0Ev.cpp
src/_ZN11ShadowModelD0Ev.cpp
#
# BATCH 3, THREE COMPLETE-OBJECT DESTRUCTORS (D1, vtable slot 16), each named
# by a slot-16 host path that spelled its body by hand or aliased past it:
#   LightBeam     tr_d1's id-0x25 branch, hal/actor_classes.cpp (the TRAP branch
#                 of the shared table stays the host ruling it was)
#   MetalNetLift  aml_d1, hal/actor_classes_ov064_gate178.cpp
#   daDsnBase_c   actorport_d16t_daDsnBase_c, hal/actorport_dtor_bridge.cpp
#                 (an alias onto the inline COMDAT ??1daDsnBase_c until now)
# LightBeam and daDsnBase_c are emit forcers for an inline-in-header
# destructor, so their arm is the dtorfwd forwarder shape; MetalNetLift's
# file carries the out-of-line ~MetalNetLift() its compiled D0 sibling already
# defines, so its arm is the call to that one host symbol. port/slice_vtd1.txt
# refused daDsnBase_c's TU when it was enrolled WITHOUT an arm (it defined no
# name of its own); with the arm it defines the ROM's flat D1, which the
# ROM's _ZTV11daDsnBase_c holds in slot 16, and slot 16 names it.
src/_ZN9LightBeamD1Ev.cpp
src/_ZN12MetalNetLiftD1Ev.cpp
src/_ZN11daDsnBase_cD1Ev.cpp
