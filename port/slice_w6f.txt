# slice_w6f.txt -- lane-owned slice for the linkage campaign (run linkw,
# wave 6, lane w6-f). One matched src TU per line, path relative to the repo
# root, comments with #. THIS FILE HAS ONE OWNER (lane w6-f).
#
# FLAMETHROWER (318, ov095, RTTI daObjFlamethrower_c). The fill is
# hal/actor_classes_flamethrower.cpp; the mount append is ov095_syms.txt.
#
# The class has FIVE methods in the ROM and four of them are matched. The fifth,
# _ZN12Flamethrower8BehaviorEv (0x021368f0, 0x470 bytes), has NO matched TU --
# config/arm9/overlays/ov095/delinks.txt lists the other four and not this one,
# and the near-miss DB's best attempt still sits at 154 divergences. It is a
# HOST COPY, port/unmatched/Flamethrower_Behavior.c, transcribed per
# instruction from the ROM listing; when the decomp lands a byte match, that TU
# replaces the host copy on this line and the file is deleted.
src/d_a_obj_flamethrower.cpp
src/_ZN12Flamethrower13InitResourcesEv.cpp
src/_ZN12FlamethrowerD1Ev.cpp
# RETIRED at SLICE1: body now in src/_ZN12FlamethrowerD1Ev.cpp. MSVC folds the ROM destructor variants into one symbol, so only one of a class two per-function destructor TUs can be compiled.
#src/_ZN12FlamethrowerD0Ev.cpp

# ---- the reference edges ---------------------------------------------------
#
# The four TUs above and the Behavior host copy reference 13 distinct call
# targets plus their own construction closure. Every one of those was checked
# against walk_window.map on this lane's OWN baseline build (b8ff3d0b7,
# C:\tmp\wl19base) and all but four were ALREADY LINKED, so they are not listed
# here: DecIfAbove0_Short, _ZN5Sound8PlayLongEjjjRK7Vector3s,
# Matrix4x3_FromRotationXYZExt, MulVec3Mat4x3, Vec3_Add,
# _ZN8dActor_c10FindWithIDEj, _ZN6Player4BurnEv (gate 175 brought it in, with its
# face), _ZN5dCc_c5ClearEv, _ZN5dCc_c6UpdateEv,
# _ZN8dCcPos_c4InitERK7Vector35Fix12IiES4_jj,
# _ZN8dCcPos_cC1Ev/D1Ev, _ZN7fBase_cnwEj, _ZN8dActor_cC2Ev,
# _ZN8dActor_cD2Ev, _ZN6Memory10DeallocateEPvP4Heap, __cxa_vec_ctor,
# __cxa_vec_cleanup, _ZN7Vector3D1Ev, func_0203d384.
#
# The four particle emitters the Behavior fires -- one pair per variant --
#
#   func_020226fc   effect id 0x50   \  the (param & 0xff) != 0xff pair
#   func_02022774   effect id 0x4f   /
#   func_02022864   effect id 0x51   \  the == 0xff pair
#   func_020227ec   effect id 0x52   /
#
# ARE NOT LISTED HERE, and the reason is a trap worth writing down. They read as
# MISSING in the baseline walk_window.map, which is what sent the first draft of
# this file to add them -- but the map is an inventory of what SURVIVED the
# link, not of what was available to it. All four are already compiled into
# every target as hostgen'd objects (build/port/host-src/src/func_02022*.cpp,
# emitted from GATE29_GEN, which lists these same four .c files as hostgen
# INPUTS in slice_gate29.txt). Nothing in the baseline build referenced them, so
# /OPT:REF dropped the bodies and no public reached the map. Adding the .c forms
# here compiles a SECOND definition of each and the link dies with four
# LNK2005s (measured on this lane, build 1). The Behavior's calls are what make
# the existing hostgen'd bodies live; no slice line is needed or allowed.
#
# The rule this leaves behind: a "not in the map" reading is only a link-closure
# gap if the symbol is also absent from every gate's hostgen input list and from
# every other slice. Check both before adding a line.
