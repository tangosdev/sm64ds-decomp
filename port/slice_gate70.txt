# Gate 70: PIRANHA_PLANT (actor 250, ov084), Whomp's Fortress.
#
# _ZTV12PiranhaPlant / _ZTV7daPkn_c, ov084 0x02130c28. daPkn_c_classInit
# installs it cleanly and its own SpawnInfo +4 halfword reads 250. ov084 has
# been mounted since gate 32 (GOOMBA/BOB_OMB_BUDDY), so this class costs no
# overlay work.
#
# A 31-slot Enemy table. Its Behavior dispatches a nine-state pointer-to-member
# over a COMPLETE class, which MSVC narrows to a 4-byte stride against the ROM's
# 8-byte {function, delta} table -- the gate-16 stride collision, narrowing this
# time. Behavior and Render are host copies (the ModelAnim slot-5 Render and the
# PMF Behavior) in port/unmatched/PiranhaPlant_States.cpp, which also carries the
# nine-state seat. Every state and helper is matched src, so no hole and no trap.
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# _ZN7daPkn_c8BehaviorEv IS NOT HERE: the pointer-to-member case (host copy).
# _ZN7daPkn_c6RenderEv IS NOT HERE: the ModelAnim slot-5 case (host copy).
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# slot 29 (OnAimedAtWithEgg), its own
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# the nine states its Behavior dispatches, all matched
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# func_ov084_0212f460 IS NOT HERE: C++ inside a .c (the gate-9 case), so it goes
# through hostgen -- see GATE_L7_SYMS in port/CMakeLists.txt.
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# the piranha's own closure: the two per-frame helpers Behavior calls, the
# spawn/init helpers and the aim path
# func_ov084_0212f204: r0-passthrough. Calls Actor::ClosestPlayer() with no
# argument (this rode ARM r0). HOST COPY passes `r4` explicitly at
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp. Byte-locked src unchanged.
# src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
# QUARANTINED at the main->port sync (origin/main 9c7f38a24): main consolidated ov084's
# PiranhaPlant bodies into this one TU, and the TU carries func_ov084_0212f204, whose
# zero-argument _ZN8dActor_c13ClosestPlayerEv() call closestplayer_guard refuses (thiscall
# with no receiver faults on null this + 0x5c). The port already hosts that function in
# port/unmatched/Actor_ClosestPlayer_OverlayReaders.cpp, which the raw TU would now
# duplicate. Restore this row once the host copy covers the whole consolidated TU.
#src/actors/daPkn_c.cpp
