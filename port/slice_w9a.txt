# Run linkw wave 9, lane w9-reg, slice A: TILTING_PLATFORM_LLL (id 72, ov064).
#
# The matched closure of id 72, swept by the call graph over ov064's own relocs
# from the factory and from every vtable-bound method. FIVE TUs; everything
# else the class reaches is already linked, which is why the slice is this
# short:
#
#   slots 6 and 9   _ZN15daObjGuragura_c8BehaviorEv / _ZN15daObjGuragura_c6RenderEv, the shared ov002
#                   TILTING pair -- both already in walk_window.map for ov045's
#                   id 141 (hal/actor_classes_ov045.cpp, TILTING_PLATFORM_BFS)
#   the veneer      func_ov002_020b6244 / func_ov002_020b60fc, the ov002 base
#     targets       Init/Cleanup pair the two veneers tail-call -- both already
#                   in the map, same reason
#   the factory     _ZN7fBase_cnwEj, _ZN10dBgActor_cC2Ev -- already in the map
#     spine
#   slot 31         _ZN10dBgActor_c4KillEv (ov002 0x020ee55c) -- already in the map
#   slots 1..30     the arm9 Actor/ActorBase defaults, all already linked
#
# So the marginal linkage of this class is exactly the five lines below, and
# every claimed gain was checked against walk_window.map BEFORE it was claimed
# rather than after -- /OPT:REF strips an unreferenced TU, so a slice line with
# no caller is zero linkage. All five are reached from the registry row
# (factory + fill) that lands with them.
#
# NOT IN THIS SLICE, on purpose: hal/actor_classes_ov064_w9.cpp declares
# _ZTV14daObjFl_Gura_c as HOST storage (the ov015/ov016/ov080 rule), so the
# vtable is not mounted and no TU spells it but the fill.
#
# THE SIXTH TU is the class's own static initialiser, the gate-177/178
# convention (each of those slices carries its own __sinit_ov064_* lines). It
# builds the two SharedFilePtrs InitResources reads -- model file 1523 into
# 0x0211c768 with func_02017ab4 as its destructor and node 0x0211c778, clsn file
# 1524 into 0x0211c770 with SharedFilePtr_Destruct_Clsn and node 0x0211c784 --
# and its body reads back exactly the cells the reloc sweep predicted, which is
# what says the mount list and the sinit agree.
src/game/actors/d_a_obj_fl_gura.cpp
src/__sinit_ov064_0211b078.c
