# GATE SMALLS (run link100, lane SMALLS) -- two leftovers earlier lanes
# measured, wrote down and did not take. One line each; the derivation each
# one needed is below it.
#
# ---------------------------------------------------------------------------
# RUNG 1: MOTHER_PENGUIN'S TENTH ov018 TU
# ---------------------------------------------------------------------------
#
# src/func_ov018_02111bf0.c -- the floor/wall collision pass at Actor+0x1a8,
# called by func_ov018_02112234 and func_ov018_02111e28 (both linked, both with
# an overlay(18) relocation at the call site). Lane MPG linked nine of the
# mother penguin's ten ov018 TUs and left this one host-copied, because its
# symbol was spoken for: gate 188's port/unmatched/Jrb_Aliases.cpp aliased
#     _func_ov018_02111bf0 -> _func_ov016_02111bf0
# for three UNAGI bodies in ov016 that spell their own module's 0x02111bf0 with
# the ov018 prefix (ov016 and ov018 share load base 0x021111a0). Defining the
# LHS defeats the alias and reroutes Unagi into the penguin's collision pass --
# alternatename_guard.py failed MPG's first link on exactly that, LHS
# 0001:00188350 against RHS 0001:00148d00 -- so MPG host-copied the body under
# port_mpg_clsn_pass and named the honest fix in its own banner.
#
# THE HONEST FIX IS THE GUARD'S OWN R1/R2 RECIPE, out of the wave-5 review that
# alternatename_guard.py's header records: when a newly sliced TU would define
# an alias LHS, DELETE THE DEAD ALTERNATENAME and recompile the TUs that
# referenced the LHS with a per-source -DLHS=RHS. Applied here that is
#   * the alternatename removed from unmatched/Jrb_Aliases.cpp (its banner now
#     carries the history), and
#   * -Dfunc_ov018_02111bf0=func_ov016_02111bf0 on the three ov016 callers,
#     src/func_ov016_02111534.c, src/func_ov016_02111758.cpp and
#     src/func_ov016_021118b4.cpp (all three linked through slice_gate188.txt).
# Every reference is accounted for: those three are the only spellings of the
# ov018 name outside ov018 itself, and hal/../unmatched/Unagi_StateDispatch.cpp
# carries a declaration of it that nothing in that file calls.
#
# With the LHS free, MPG's two callers stop being renamed onto the host copy in
# the gate-mpg block and reach the ROM's own body, and
# unmatched/MotherPenguin_ClsnPass.cpp -- a line-for-line copy of this file
# under a port name -- comes out of the three target lists and off disk.
#
src/func_ov018_02111bf0.c
