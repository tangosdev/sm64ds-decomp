# The ov020 data HAUNTED_CHAIR (326) names.
#
# ov020 is level 12's own overlay (data/stage/teresa_house, Big Boo's Haunt),
# already mounted WHOLE in PORT_LEVEL_OVERLAYS (its LVL_Overlay object tables are
# walked across symbol boundaries by count). This is the SECOND mount, per
# symbol, the gate-59 treatment ov015 gets: HauntedChair reaches its SpawnInfo
# record and the model SharedFilePtr its InitResources loads through BY NAME, and
# --pack keeps the runs that are really one array contiguous. The two mounts are
# disjoint pointer graphs on purpose, the same way ov015 is in both lists.
#
# THE VTABLE IS LEFT OUT, the ov015 rule: _ZTV12HauntedChair (0x02114a14, also
# _ZTV9daChair_c) is a HOST array the registry fills; mounting it would hand the
# factory DS code addresses. It is a 31-slot Enemy table whose own overrides are
# slots 0/3/6/9/16/17 and whose rest are the shared Actor/ActorBase bodies
# already in the build.
#
# g_profile_CHAIR delta-sizes to 12 bytes (data_ov020_021149fc sits at
# +12 in ROM), NOT a :0x1c pin like ov019's record: --pack keeps 021149fc
# contiguous at its ROM offset, so a read past +12 still lands on ROM bytes.
# The +4 id halfword the registry cross-checks reads 326 either way.
#
# data_ov020_02114af0 is HauntedChair's model SharedFilePtr, in BSS past the
# image (the overlay image ends at 0x02114aa0); __sinit_ov020_0211372c
# Constructs it with handle 0x2d0 at boot (port_actor_overlays_sinits), and
# data_ov020_02114af8 is the anim-release node that sinit pairs with it. The
# book classes' four SharedFilePtrs (aa0/aa8/ab0/ab8), built by the other ov020
# sinit, are NOT mounted: BOOK_SHOT / BOOKEND / BOOK_SHOT_SPAWNER stay skipped
# (BookShot's state-9 behaviour calls an undecompiled ov063 function).
g_profile_CHAIR data_ov020_021149fc
data_ov020_02114af0 data_ov020_02114af8

# run linkw wave 5 (lane w5-A): the BOOK CAST joins -- Bookend (327),
# BookShotSpawner (328), BookShot (325) and the unnamed BookSwitch record
# (213, g_profile_BOOK_SWITCH; its factory is the unnamed daBook_c_classInit_BOOK_SWITCH,
# settled by the record's own bytes -- every record in this cluster is
# config-`ambiguous` and each was verified: w0 = its own factory inside
# ov020, +4 halfword = the id). The cluster 0x02114844..0x021148d8 is ONE
# contiguous --pack run; dsd split two records (Bookend +0x14/data_02114874,
# BookShot +4/data_021148b8), and the registry's +4 id readback crosses those
# splits, so the run's contiguity is load-bearing. data_ov020_021148b8's span
# also carries the spawner table's RTTI header (inert). The two vtables
# (_ZTV15BookShotSpawner 0x021148d8 with its interior splits
# data_ov020_021148e0/021148f0, _ZTV8BookShot 0x0211495c) stay OUT -- host
# arrays hal/actor_classes_ov063.cpp fills.
_ZTI8daBook_c _ZTS11daBookGen_c
g_profile_KILLER_BOOK data_ov020_02114874
g_profile_BOOK_GENERATOR data_ov020_0211488c
g_profile_BOOK_SWITCH
g_profile_SHOOT_BOOK data_ov020_021148b8
# the two 12-byte nodes between BookShot's table and HauntedChair's record
_ZTS9daChair_c _ZTI9daChair_c
# the four book SharedFilePtrs __sinit_ov020_02113674 builds (models
# 0x2c8/0x2cb, anims 0x2c9/0x2ca) and their destructor-chain nodes; the
# node at 0x02114ad8 is 12 bytes by the ROM's own write (func_020731dc
# stores three words) -- dsd's interior split data_ov020_02114adc would
# floor it to 4, so the node is pinned and the split skipped.
data_ov020_02114aa0 data_ov020_02114aa8 data_ov020_02114ab0
data_ov020_02114ab8 data_ov020_02114ac0 data_ov020_02114acc
data_ov020_02114ad8:0xc data_ov020_02114ae4
# the book state machine's tuning word (4 bytes, next symbol 0211482c)
data_ov020_02114828
