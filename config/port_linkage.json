{
  "_comment": [
    "The stamped LINKED tier: how many matched translation units actually reach the",
    "PC port's binary. This is a STAMP, not a live counter, and it cannot be anything",
    "else from main: port/tools/linkage.py reads build/port/walk_window.map, which only",
    "exists after an MSVC build of the port, and CI on main has no such build.",
    "",
    "BEFORE STAMPING, LOOK FOR A BUILD THAT ALREADY EXISTS. Every port lane carries its",
    "own build tree, so a current walk_window.map is usually already on disk:",
    "    ls -la */build/port/walk_window.map",
    "across the port worktrees, newest first, then run the tool against that root. The",
    "first version of this file was stamped off a five-day-old branch while a map from",
    "the night before sat unread one directory away, and published 26.5% when the real",
    "figure was 34.5%. A stale stamp is still a real measurement, just of the wrong run.",
    "",
    "To refresh: run `python port/tools/linkage.py [root]` and update the numbers below",
    "together with branch, commit and measuredAt. Never move the count without also",
    "moving the provenance fields - tools/tiers.py drops a stamp missing any of them,",
    "which is the intended behaviour for a number nobody can trace.",
    "",
    "basis records what the figure is a measurement OF. The port branches are not merged,",
    "so this is the best single lane rather than a combined total. All seven live lanes,",
    "measured 2026-08-12: cons 3881, lk4 3877, lk1 3829, lk6 3829, lk2 3823, lk3 3819,",
    "lk5 3819. Overlapping but not identical seats, so the merged figure is higher and",
    "the published number stays a floor.",
    "",
    "LINKED grows when matched TUs become reachable in the port's linked call graph.",
    "Actor methods and free functions both count. Enrollment alone does not increase",
    "the count when the release build still dead-strips the unreachable object."
  ],
  "linkedTus": 10595,
  "matchedTus": 11328,
  "measuredAt": "2026-09-13",
  "branch": "port/link100",
  "commit": "9bb3c454f",
  "stale": false,
  "basis": "integration branch port/link100 at its pushed tip 9bb3c454f, measured against that tip's own gate build walk_window.map (C:/tmp/l1-int, gates FOLD2 and FOLD3: tree clean, build exit 0, linkage 10595 exact, the eleven newly seated names on their own matched objects, six static guards clean, battery ALL GREEN on both loop arms, eight proofs green, level-1 selftest position unchanged with the frame crosscheck at 0, one captured scene-to-level transition through the fader path). Over the 10584 stamp: +5 from lane ADJSEAT (the four adjudicated ov015 vtable rows, two deleting destructors and PoleBillboard slots 23/24 on the ROM veneers, plus func_ov015_02111414 the veneers jump to; tail-jump guard at 37 frames), +4 from the four CleanupResources seats (PoleLift ov045, MadPiano ov063, FloatingFloorLllBig ov022, BowserFireSeaArena ov060: vtable slot 3 filled from the matched TUs, host thunks retired) and +2 from sound rung R5 (func_020506fc and func_02050038, the ARM9 sound thread, its record and 1 KB stack hosted as one run at ROM span). The commit id names the tip on the history rewritten by the 2026-09-13 identity scrub; the gated tree differs from it only in four scrubbed text files. NOTE for the next stamp: the main-to-port sync (port/l6-sync) folds 2368 one-function TUs into class TUs, so once it lands the denominator becomes 8960 distinct TU stems, not 11328, and the percentage basis changes with it."
}
