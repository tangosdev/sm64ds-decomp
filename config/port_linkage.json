{
  "_comment": [
    "The stamped LINKED tier: how many matched translation units actually reach the",
    "PC port's binary. This is a STAMP, not a live counter, and it cannot be anything",
    "else from main: port/tools/linkage.py reads build/port/walk_window.map, which only",
    "exists after an MSVC build of the port, and CI on main has no such build.",
    "",
    "BEFORE STAMPING, LOOK FOR A BUILD THAT ALREADY EXISTS. Every port lane carries its",
    "own build tree, so a current walk_window.map is usually already on disk:",
    "    ls -la */build/port/walk_window.map",
    "across the port worktrees, newest first, then run the tool against that root. The",
    "first version of this file was stamped off a five-day-old branch while a map from",
    "the night before sat unread one directory away, and published 26.5% when the real",
    "figure was 34.5%. A stale stamp is still a real measurement, just of the wrong run.",
    "",
    "To refresh: run `python port/tools/linkage.py [root]` and update the numbers below",
    "together with branch, commit and measuredAt. Never move the count without also",
    "moving the provenance fields - tools/tiers.py drops a stamp missing any of them,",
    "which is the intended behaviour for a number nobody can trace.",
    "",
    "basis records what the figure is a measurement OF. The port branches are not merged,",
    "so this is the best single lane rather than a combined total. All seven live lanes,",
    "measured 2026-08-12: cons 3881, lk4 3877, lk1 3829, lk6 3829, lk2 3823, lk3 3819,",
    "lk5 3819. Overlapping but not identical seats, so the merged figure is higher and",
    "the published number stays a floor.",
    "",
    "LINKED grows when matched TUs become reachable in the port's linked call graph.",
    "Actor methods and free functions both count. Enrollment alone does not increase",
    "the count when the release build still dead-strips the unreachable object."
  ],
  "linkedTus": 10523,
  "matchedTus": 11328,
  "measuredAt": "2026-09-09",
  "branch": "port/link100",
  "commit": "91f525963",
  "stale": false,
  "basis": "integration branch port/link100 at its pushed tip 91f525963, measured against that tip's own battery build walk_window.map (C:/tmp/l1-int, gate INT30: tree clean, reconfigure with the four hostgen register TUs, battery ALL GREEN at floor 10523 with all 51 level and 37 scene rows including the Game Over scene, the six proofs, the loopback pair with the ROM's own wireless worker created, entered, slept and woken on the parent, the four-player proof, the rollback ladder at eight wide with every session row green including seven windows at 160 ms, and a captured level under the ROM's own frame loop), and confirmed by the hosted-runner port-linkage workflow on every push of port/** and every pull request into the shipping branch. +2 over the previous stamp of 10521, +490 over the 2026-09-08 morning's 10033, +1402 over the 2026-08-30 stamp of 9121. Since the 10521 stamp: two more overlay bodies seated as the ROM's own, the intro Bowser dispatch body func_ov018_02111bf0 and the Jolly Roger Bay alias body func_ov002_020bd664 (+2); the hosted ARM7 posting the port-receive indication once per completed round so the ROM's own receive engine banks the parent's datagram on the child every frame (0); and the thread switch a ROM body asks for from inside the VBlank handler deferred to the interrupt return the cartridge's way, decided by the ROM's own processor-mode check now that the host answers IRQ mode inside a handler (0). Running: the ROM's own send and copy on the wireless path (the last host face on it), the same mode answer on the two remaining handler paths, the decomp-side honesty of the matched count."
}
