//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov079/FortressWall  (8 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x021271e4  src/_ZN12FortressWallD1Ev.cpp
 *   [1] 0x02127228  src/_ZN12FortressWallD0Ev.cpp
 *   [2] 0x02127280  src/_ZN12FortressWall4KillEv.cpp
 *   [3] 0x021272e0  src/_ZN12FortressWall24OnHitByCannonBlastedCharER8dActor_c.cpp
 *   [4] 0x02127300  src/_ZN12FortressWall16CleanupResourcesEv.cpp
 *   [5] 0x02127364  src/_ZN12FortressWall6RenderEv.cpp
 *   [6] 0x021273a4  src/_ZN12FortressWall8BehaviorEv.cpp
 *   [7] 0x02127494  src/_ZN12FortressWall13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "FortressWall.h"
#include "Sound.h"
#include "decl_common.h"
#include "SharedFilePtr.h"
#include "dBgW.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow struct 'Base' */
struct Base {
    virtual int vf0(int);
    virtual int vf1(int);
    virtual int vf2(int);
    virtual int vf3(int);
    virtual int vf4(int);
    virtual int vfunc(int a);
};

/* shadow namespace 'Sound' */
namespace Sound {
int PlaySecretSound(dActor_c *actor, u16 *state);
}

/* shadow struct 'E12' */
struct E12 { void *p; int pad[2]; };

extern "C" {
extern "C" void _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_( u32 id, Fix12i x, Fix12i y, Fix12i z);
extern struct E12 data_ov079_02128058[];
extern struct E12 data_ov079_0212805c[];
extern "C" void func_020393a4(int *collider, int range);
extern "C" int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(void *, int a, int b);
extern void *_ZN5Model8LoadFileER13SharedFilePtr(void *);
extern int _ZN9ModelBase7SetFileEP8BMD_Fileii(void *, void *, int, int);
extern void _ZN10dBgActor_c21UpdateModelPosAndRotYEv(void *);
extern void _ZN10dBgActor_c19UpdateClsnPosAndRotEv(void *);
extern void *_ZN7dBgW_Kc8LoadFileER13SharedFilePtr(void *);
extern int _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(void *self, void *kcl, void *mtx, int f, short s, void *blk);
extern struct E12 data_ov079_02128060[];
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN12FortressWall13InitResourcesEv, 0x02127494, size 0xe8 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWall13InitResourcesEv
/* recovered: named members + shared header, real C++ method */
int FortressWall::InitResources()
{
    int b = (int)(actorID == 0x30);
    if (b != 0) {
        mVariant = 0;
        mStarId = (unsigned char)(*(s32 *)&param1);
        if (mStarId == 0xff)
            mStarId = 0;
    } else {
        mVariant = 1;
    }
    {
        int idx = mVariant;
        void *mdl = _ZN5Model8LoadFileER13SharedFilePtr(data_ov079_02128058[idx].p);
        _ZN9ModelBase7SetFileEP8BMD_Fileii(&mModel, mdl, 1, -1);
        _ZN10dBgActor_c21UpdateModelPosAndRotYEv(this);
        _ZN10dBgActor_c19UpdateClsnPosAndRotEv(this);
        {
            int idx2 = mVariant;
            void *kcl = _ZN7dBgW_Kc8LoadFileER13SharedFilePtr(data_ov079_0212805c[idx2].p);
            _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(&mMeshCollider, kcl, &mClsnMat, 0x199, mAngleY, data_ov079_02128060[idx2].p);
        }
    }
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN12FortressWall8BehaviorEv, 0x021273a4, size 0xf0 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWall8BehaviorEv
/* Signature deliberately copied from the local declaration above: the
   ROM name carries by-value class parameters (e.g. Fix12<int>), which
   mwccarm passes differently at the call site, so declaring the true
   types breaks the byte match. See notes/mwccarm-codegen.md 6az. */
int FortressWall::Behavior()
{
    int ok = (actorID == 0x30);
    if (ok != 0 && mBroken != 0) {
        if (mMeshCollider.IsEnabled() != 0) {
            mMeshCollider.Disable();
        }
        if (Sound::PlaySecretSound(this, &mBreakSoundState) != 0) {
            Vector3 pos;
            pos.x = mPosX;
            pos.y = mPosY;
            pos.z = mPosZ;
            pos.y += 0xc8000;
            dActor_c::Spawn(0xb2, mStarId | 0x40, pos, (Vector3_16 *)0, mAreaId, -1);
            MarkForDestruction();
        }
        return 1;
    }
    func_020393a4((int *)&mMeshCollider, 0x240000);
    _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(this, 0x240000, 0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN12FortressWall6RenderEv, 0x02127364, size 0x40 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWall6RenderEv
/* recovered: named members + shared header, real C++ method */
int FortressWall::Render()
{
    if (mBroken != 0) return 1;
    ((Base *)&mModel)->vfunc(0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN12FortressWall16CleanupResourcesEv, 0x02127300, size 0x64 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWall16CleanupResourcesEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
int FortressWall::CleanupResources()
{
  if(((dBgW *)&mMeshCollider)->IsEnabled())
    ((dBgW *)&mMeshCollider)->Disable();
  ((SharedFilePtr *)data_ov079_02128058[mVariant].p)->Release();
  ((SharedFilePtr *)data_ov079_0212805c[mVariant].p)->Release();
  return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN12FortressWall24OnHitByCannonBlastedCharER8dActor_c, 0x021272e0, size 0x20 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWall24OnHitByCannonBlastedCharER8dActor_c
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
// recovered name: FortressWall_OnHitByCannonBlastedChar
/* recovered: renamed to Class_Method */
/* daObjBk_Kabe_c::OnHitByCannonBlastedChar - recovered from vtable slot
   identity. The ROM body is just a virtual dispatch to slot 31 (Kill) on
   itself -- getting hit by a cannon-blasted character kills the actor --
   so this calls the real virtual Kill() rather than a raw vtable-slot
   shim. Nothing in the tree reads this slot's return value (see
   dActor_c.h's note by the declaration), so the missing explicit return
   matches the ROM, which never sets r0 here either. */
int FortressWall::OnHitByCannonBlastedChar(dActor_c &other)
{
    Kill();
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- _ZN12FortressWall4KillEv, 0x02127280, size 0x60 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWall4KillEv
/* FortressWall::Kill() at ov079 0x02127280, 0x60 bytes -- vtable slot 31.
 *
 * ATTRIBUTED BY THE VTABLE. _ZTV12FortressWall (ov079 0x021280b0) carries
 * 0x02127280 at slot 31 -- config/arm9/overlays/ov079/relocs.txt has
 * `from:0x0212812c kind:load to:0x02127280` -- while _ZTV10dBgActor_c's word at the
 * same slot (from:0x0210aeb4) relocates to 0x020ee55c, _ZN10dBgActor_c4KillEv.
 * Different addresses in the same slot: this is the class's own override.
 *
 * The odd one out of this family. It plays the sound FIRST, spawns particle
 * 0x121 at the wall's own position with no vertical offset, and does NOT call
 * PoofDustAt at all. Then it branches on the actor id: id 0x30 is the breakable
 * fortress wall (daObjBk_Kabe_c_classInit_BK_KABE00), which only sets mBroken and
 * stays alive; every other id falls through to MarkForDestruction.
 *
 * THE `int b` INTERMEDIATE IS LOAD-BEARING, the same way it is in
 * daObjFallBlock_c::Kill: `if (actorID == 0x30)` folds the test
 * into one cmp/branch, while the ROM materialises the comparison into a register
 * and then tests THAT. Writing the int is what asks for the second shape.
 *
 * Particle::System::NewSimple stays spelled as its mangled name -- its
 * parameters are Fix12<int> BY VALUE and declaring the true types changes how
 * the caller passes them; argued in full at dBgActor_c::Kill. */
void FortressWall::Kill()
{
    Sound::PlayBank3(0xf, *(Vector3 *)&mCamSpacePosX);
    _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_(0x121, mPosX, mPosY, mPosZ);
    int isBreakable = (actorID == 0x30);
    if (isBreakable) {
        mBroken = 1;
        return;
    }
    MarkForDestruction();
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN12FortressWallD1Ev, 0x021271e4, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN12FortressWallD1Ev
/* recovered: real C++ destructor -- the compiler emits the whole body
 *
 * Two vtable stores and three destructor calls, every one a consequence of
 * `struct FortressWall : dBgActor_c`: its own vptr, then dBgActor_c's -- inlined,
 * because dBgActor_c's destructor is defined in its class body -- then
 * dBgActor_c's Model and dBgW_KcMbg, then dActor_c. This class adds no
 * member with a destructor of its own.
 */
FortressWall::~FortressWall()
{
}
