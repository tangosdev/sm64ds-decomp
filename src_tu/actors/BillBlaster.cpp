//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov079/BillBlaster  (8 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x02126dbc  src/_ZN11BillBlasterD1Ev.cpp
 *   [1] 0x02126e00  src/_ZN11BillBlasterD0Ev.cpp
 *   [2] 0x02126e58  src/_ZN11BillBlaster4KillEv.cpp
 *   [3] 0x02126ecc  src/_ZN11BillBlaster15OnHitByMegaCharER6Player.cpp
 *   [4] 0x02126f04  src/_ZN11BillBlaster16CleanupResourcesEv.cpp
 *   [5] 0x02126f64  src/_ZN11BillBlaster6RenderEv.cpp
 *   [6] 0x02126f8c  src/_ZN11BillBlaster8BehaviorEv.cpp
 *   [7] 0x02127090  src/_ZN11BillBlaster13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "BillBlaster.h"
#include "Sound.h"
#include "Player.h"
#include "dBgActor_c.h"
#include "dBgW.h"
#include "SharedFilePtr.h"
#include "decl_common.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow struct 'Base' */
struct Base { virtual void v0(); virtual void v1(); virtual void v2(); virtual void v3(); virtual void v4(); virtual void m(int); };

/* shadow struct 'Derived' */
struct Derived { char pad[0xd4]; Base base; };

/* shadow typedef 'FixV' */
typedef int FixV;

/* shadow struct 'dActor_c' */
struct dActor_c;

/* shadow struct 'dBgActor_c_helper' */
struct dBgActor_c_helper {
    int UpdateKillByMegaChar(short, short, short, FixV);
    int IsClsnInRange(FixV, FixV);
};

/* shadow struct 'Actor_s' */
struct Actor_s {
    static dActor_c* FindWithID(unsigned int);
    dActor_c* ClosestPlayer();
    static dActor_c* Spawn(unsigned int, unsigned int, const Vector3&, const Vector3_16*, signed char, short);
};

/* shadow struct 'SharedFilePtr' DROPPED: include/SharedFilePtr.h (already included above)
 * is the more complete observation -- it carries Release()/LoadFile(); the legacy
 * InitResources shard's local `{ int x; }` only ever had its address taken. */

extern "C" {
extern "C" void _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_( u32 id, Fix12i x, Fix12i y, Fix12i z);
extern void* data_ov079_02127f64[];
extern struct SharedFilePtr data_ov079_02128300;
extern struct SharedFilePtr data_ov079_021282f0;
extern "C" int _ZN10dBgActor_c20UpdateKillByMegaCharEsss5Fix12IiE(void *, short, short, short, FixV);
extern "C" int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(void *, FixV, FixV);
extern "C" dActor_c* _ZN8dActor_c10FindWithIDEj(unsigned int);
extern "C" dActor_c* _ZN8dActor_c13ClosestPlayerEv(void *);
extern "C" dActor_c* _ZN8dActor_c5SpawnEjjRK7Vector3PK10Vector3_16as(unsigned int, unsigned int, const Vector3&, const Vector3_16*, int, int);
FixV Vec3_HorzDist(const Vector3* a, const Vector3* b);
short Vec3_HorzAngle(const Vector3 *v0, const Vector3 *v1);
int AngleDiff(int a, int b);
extern void *_ZN5Model8LoadFileER13SharedFilePtr(struct SharedFilePtr &f);
extern void _ZN9ModelBase7SetFileEP8BMD_Fileii(void *self, void *file, int a, int b);
extern void _ZN10dBgActor_c21UpdateModelPosAndRotYEv(void *self);
extern void _ZN10dBgActor_c19UpdateClsnPosAndRotEv(void *self);
extern void *_ZN7dBgW_Kc8LoadFileER13SharedFilePtr(struct SharedFilePtr &f);
extern void _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
void *self, void *kcl, const void *mtx, int fix, short s, void *clps);
extern int IsStarCollectedInCurLevel(int a);
extern signed char data_0209f2f8;
extern int data_0209caa0[];
extern unsigned char data_0209f220;
int func_ov079_02127090(void *self);
/* reconciled: data_ov079_02128300 is a SharedFilePtr (Model::LoadFile takes SharedFilePtr&); the more
 * complete spelling was applied to the first declaration above. */
/* reconciled: data_ov079_021282f0 is a SharedFilePtr (Model::LoadFile takes SharedFilePtr&); the more
 * complete spelling was applied to the first declaration above. */
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN11BillBlaster13InitResourcesEv, 0x02127090, size 0x124 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlaster13InitResourcesEv
// recovered name: daObjBkKillerdai_c_InitResources
/* recovered: renamed to Class_Method */
/* daObjBkKillerdai_c::InitResources - recovered from vtable slot identity */
s32 BillBlaster::InitResources() {
    void * self = (void *)this;
    char *c = (char*)self;
    void *m;
    signed char st;

    _ZN5Model8LoadFileER13SharedFilePtr(data_ov079_02128300);
    _ZN5Model8LoadFileER13SharedFilePtr(data_ov079_021282f0);
    m = _ZN5Model8LoadFileER13SharedFilePtr(*(struct SharedFilePtr*)data_ov079_02127f64[0]);
    _ZN9ModelBase7SetFileEP8BMD_Fileii(c + 0xd4, m, 1, -1);
    _ZN10dBgActor_c21UpdateModelPosAndRotYEv(c);
    _ZN10dBgActor_c19UpdateClsnPosAndRotEv(c);
    m = _ZN7dBgW_Kc8LoadFileER13SharedFilePtr(*(struct SharedFilePtr*)data_ov079_02127f64[1]);
    _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
        c + 0x124, m, c + 0x2ec, 0x199, *(short*)(c + 0x8e),
        (void*)data_ov079_02127f64[2]);

    *(int*)(c + 0x320) = 0;

    st = data_0209f2f8;
    if (st == 0x12) {
        if (data_0209caa0[1] & 0x204) return 0;
    }
    if (st == 7) {
        if (data_0209f220 != 1) {
            if (IsStarCollectedInCurLevel(1) != 0) goto ret1;
        }
        if (*(int*)(c + 0x60) >= 0xdac000) return 0;
    }
ret1:
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN11BillBlaster8BehaviorEv, 0x02126f8c, size 0x104 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlaster8BehaviorEv
// recovered name: daObjBkKillerdai_c_Behavior
/* recovered: shared common types, renamed to Class_Method */
/* daObjBkKillerdai_c::Behavior - recovered from vtable slot identity */
/* FixV, not Fix12: this file now includes BillBlaster.h, which pulls in
   the real Fix12<int> template (math/Fix12.h) transitively, so a bare
   `typedef int Fix12;` here would redefine that template's name. FixV
   still passes ABI-identically to Fix12<int> (both one register). Vector3
   and Vector3_16 are likewise the real types now, pulled in the same way
   -- local shadows for them would redefine those classes too. */
/* Named _helper, not dBgActor_c: a local shadow with a body would redefine
   the real dBgActor_c class this file now sees through BillBlaster.h. */
/* Signature deliberately copied from the local declaration above: the
   ROM name carries by-value class parameters (e.g. Fix12<int>), which
   mwccarm passes differently at the call site, so declaring the true
   types breaks the byte match. See notes/mwccarm-codegen.md 6az. */
/* Signature deliberately copied from the local declaration above: the
   ROM name carries by-value class parameters (e.g. Fix12<int>), which
   mwccarm passes differently at the call site, so declaring the true
   types breaks the byte match. See notes/mwccarm-codegen.md 6az. */
int dBgActor_c_helper::UpdateKillByMegaChar(short, short, short, FixV);
dActor_c* Actor_s::FindWithID(unsigned int);
dActor_c* Actor_s::ClosestPlayer();
dActor_c* Actor_s::Spawn(unsigned int, unsigned int, const Vector3&, const Vector3_16*, signed char, short);
int dBgActor_c_helper::IsClsnInRange(FixV, FixV);

s32 BillBlaster::Behavior() {
    char* c = (char*)this;
    dBgActor_c_helper* self = (dBgActor_c_helper*)c;
    if (_ZN10dBgActor_c20UpdateKillByMegaCharEsss5Fix12IiE(self, 0x2000, 0, 0, 0xc8000))
        return 1;
    if (!_ZN8dActor_c10FindWithIDEj(*(unsigned int*)(c + 0x320))) {
        dActor_c* p = _ZN8dActor_c13ClosestPlayerEv((Actor_s*)c);
        FixV dist = Vec3_HorzDist((Vector3*)(c + 0x5c), (Vector3*)((char*)p + 0x5c));
        short ang = Vec3_HorzAngle((Vector3*)(c + 0x5c), (Vector3*)((char*)p + 0x5c));
        if (AngleDiff(ang, *(short*)(c + 0x8e)) < 0x2000 && dist > 0x320000 && dist < 0x5dc000) {
            long long yll = *(int*)(c + 0x60);
            int z = *(int*)(c + 0x64);
            int x = *(int*)(c + 0x5c);
            int y = (int)yll + 0x96000;
            Vector3 pos;
            *(volatile int*)&pos.x = x;
            *(volatile int*)&pos.z = z;
            *(volatile int*)&pos.y = y;
            dActor_c* spawned = _ZN8dActor_c5SpawnEjjRK7Vector3PK10Vector3_16as(0xde, 0, pos, (Vector3_16*)(c + 0x8c), *(signed char*)(c + 0xcc), -1);
            *(int*)(c + 0x320) = *(int*)((char*)spawned + 4);
            *(int*)((char*)spawned + 0x3dc) = (int)c;
        }
    }
    _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(self, 0, 0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN11BillBlaster6RenderEv, 0x02126f64, size 0x28 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlaster6RenderEv
// recovered name: daObjBkKillerdai_c_Render
/* recovered: renamed to Class_Method */
/* daObjBkKillerdai_c::Render - recovered from vtable slot identity */
s32 BillBlaster::Render() {
    Derived * d = (Derived *)this; Base *b = &d->base; b->m(0); return 1; }

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN11BillBlaster16CleanupResourcesEv, 0x02126f04, size 0x60 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlaster16CleanupResourcesEv
// recovered name: daObjBkKillerdai_c_CleanupResources
/* recovered: renamed to Class_Method, declarations from a shared header */
/* recovered: renamed to Class_Method */
/* daObjBkKillerdai_c::CleanupResources - recovered from vtable slot identity */
s32 BillBlaster::CleanupResources() {
    char * t = (char *)this;
  if(((dBgW *)(t+0x124))->IsEnabled())
    ((dBgW *)(t+0x124))->Disable();
  ((SharedFilePtr *)data_ov079_02127f64[0])->Release();
  ((SharedFilePtr *)data_ov079_02127f64[1])->Release();
  ((SharedFilePtr *)&data_ov079_02128300)->Release();
  ((SharedFilePtr *)&data_ov079_021282f0)->Release();
  return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN11BillBlaster15OnHitByMegaCharER6Player, 0x02126ecc, size 0x38 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlaster15OnHitByMegaCharER6Player
// recovered name: daObjBkKillerdai_c_OnHitByMegaChar
/* recovered: renamed to Class_Method */
/* daObjBkKillerdai_c::OnHitByMegaChar - recovered from vtable slot identity.
   unk_08e/unk_094 in the old shadow struct are dBgActor_c's own mAngleY /
   mPrevAngleY (declared in dBgActor_c.h, inherited by BillBlaster), so this
   now reads/writes them by name through `this` instead of a raw shadow
   cast. */
void BillBlaster::OnHitByMegaChar(Player& p) {
    p.IncMegaKillCount();
    this->KillByMegaChar(p);
    mAngleY = mPrevAngleY;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- _ZN11BillBlaster4KillEv, 0x02126e58, size 0x74 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlaster4KillEv
/* BillBlaster::Kill() at ov079 0x02126e58, 0x74 bytes -- vtable slot 31.
 *
 * ATTRIBUTED BY THE VTABLE. _ZTV11BillBlaster (ov079 0x02127fb8) carries
 * 0x02126e58 at slot 31 -- config/arm9/overlays/ov079/relocs.txt has
 * `from:0x02128034 kind:load to:0x02126e58` -- while _ZTV10dBgActor_c's word at the
 * same slot (from:0x0210aeb4) relocates to 0x020ee55c, _ZN10dBgActor_c4KillEv. Two
 * different addresses in the same slot is what an override IS, so this function
 * is this class's own Kill and not an inherited one.
 *
 * Same shape as dBgActor_c::Kill with this class's own constants: particle 0x8d
 * rather than 0xa, spawned 0x96000 -- 150 units of 20.12 -- above the blaster
 * rather than 100, and bank-3 sound 0xf rather than 0x41. The blaster does
 * destroy itself, so the tail is MarkForDestruction.
 *
 * The second Vector3 is copied MEMBERWISE on purpose: Vector3 declares a
 * destructor (types.h), so a whole-object assignment compiles to an ldm/stm
 * pair, four instructions where the ROM has six. Particle::System::NewSimple
 * stays spelled as its mangled name -- its parameters are Fix12<int> BY VALUE
 * and declaring the true types changes how the caller passes them. Both points
 * are argued in full at dBgActor_c::Kill. */
void BillBlaster::Kill()
{
    Vector3 pos;
    Vector3 dustPos;
    Fix12i x = mPosX;
    Fix12i y = mPosY + 0x96000;
    Fix12i z = mPosZ;
    pos.x = x;
    pos.y = y;
    pos.z = z;
    _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_(0x8d, pos.x, pos.y, pos.z);
    dustPos.x = pos.x;
    dustPos.y = pos.y;
    dustPos.z = pos.z;
    PoofDustAt(dustPos);
    Sound::PlayBank3(0xf, *(Vector3 *)&mCamSpacePosX);
    MarkForDestruction();
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN11BillBlasterD1Ev, 0x02126dbc, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN11BillBlasterD1Ev
/* recovered: real C++ destructor -- the compiler emits the whole body.
 * Vtable slot 16: one vtable store, the members in reverse, then ~dBgActor_c. */
BillBlaster::~BillBlaster()
{
}
