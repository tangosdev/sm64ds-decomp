//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov035/SpinningPlatform  (8 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x021116ec  src/_ZN16SpinningPlatformD1Ev.cpp
 *   [1] 0x02111738  src/_ZN16SpinningPlatformD0Ev.cpp
 *   [2] 0x02111798  src/_ZN16SpinningPlatform12UpdateShadowEv.cpp
 *   [3] 0x021118a8  src/_ZN16SpinningPlatform11UpdateModelEv.cpp
 *   [4] 0x021118f0  src/_ZN16SpinningPlatform16CleanupResourcesEv.cpp
 *   [5] 0x02111934  src/_ZN16SpinningPlatform6RenderEv.cpp
 *   [6] 0x0211195c  src/_ZN16SpinningPlatform8BehaviorEv.cpp
 *   [7] 0x02111a98  src/_ZN16SpinningPlatform13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "SpinningPlatform.h"
#include "SharedFilePtr.h"
#include "dBgCh_Gnd.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow struct 'V3' */
struct V3 { int x, y, z; };

extern "C" {
extern "C" void Matrix4x3_FromRotationY(void *m, short ang);
extern "C" int _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j( void *self, void *sm, void *mtx, int a, int b, int d, unsigned int e);
extern "C" short data_02082214[];
extern "C" void Matrix4x3_FromRotationXYZExt(void *, int, int, int);
extern SharedFilePtr RotatingClockHand_ClsnFile;
extern SharedFilePtr RotatingClockHand_ModelFile;
extern int DecIfAbove0_Short(char *p);
extern int RandomIntInternal(char *p);
extern void func_020393a4(int *p, int v);
extern void func_02039394(int *p, int v);
extern int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(char *c, int a, int b);
extern void _ZN10dBgActor_c19UpdateClsnPosAndRotEv(char *c);
extern unsigned char data_0209f2c0[];
extern int data_0209e650[];
extern short data_ov035_02112b80[];
extern "C" void _ZN4dBgW22UpdatePosWithTransformERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_();
extern "C" void _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block( void *self, KCL_File *f, const Matrix4x3 &m, int fix, short sh, CLPS_Block &b);
extern "C" void func_020393d4(void *p, void *v);
extern "C" void func_020396c0(void *p, int value);
extern "C" CLPS_Block data_ov035_02112238;
extern SharedFilePtr data_ov035_02112cb0;
extern SharedFilePtr data_ov035_02112cb8;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- _ZN16SpinningPlatform13InitResourcesEv, 0x02111a98, size 0x100 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatform13InitResourcesEv
/* Declared by final name, not as a member: the ROM's SetFile takes Fix12<int> where
   this call passes 0x1000, and Fix12<int> is an aggregate with no converting
   constructor from int. Declared `int fix` it mangles ...RK9Matrix4x3isR10CLPS_Block,
   which nothing defines. */
int SpinningPlatform::InitResources()
{
    void *mf = Model::LoadFile(data_ov035_02112cb0);
    mModel.SetFile((BMD_File *)mf, 1, -1);
    mShadowModel.InitCuboid();
    UpdateModel();
    UpdateClsnPosAndRot();
    KCL_File *kf = (KCL_File *)dBgW_Kc::LoadFile(data_ov035_02112cb8);
    _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
        &mMeshCollider, kf,
        mClsnMat, 0x1000, mAngleY, data_ov035_02112238);
    func_020393d4(&mMeshCollider,
                  (void *)&_ZN4dBgW22UpdatePosWithTransformERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_);
    func_020396c0(&mMeshCollider, 0);
    V3 v;
    v.x = mPosX;
    v.y = mPosY;
    v.z = mPosZ;
    v.y = v.y - 0xa000;
    dBgCh_Gnd rg;
    rg.SetObjAndPos(*(Vector3 *)&v, (dActor_c *)0);
    mFloorPosY = v.y;
    if (rg.DetectClsn() != 0)
        mFloorPosY = rg.clsnY;
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN16SpinningPlatform8BehaviorEv, 0x0211195c, size 0x13c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatform8BehaviorEv
int SpinningPlatform::Behavior()
{
    unsigned char idx = data_0209f2c0[0];
    mPrevAngleX = data_ov035_02112b80[idx];
    if (idx == 2) {
        if (DecIfAbove0_Short((char *)&mRandTimer) == 0) {
            int r = (unsigned short)((unsigned)RandomIntInternal((char *)data_0209e650) >> 16);
            if ((unsigned)r >= 0x7fff) mRandDirection = 1;
            else mRandDirection = -1;
            mRandTimer = (short)((r % 4 + 1) * 0x1e);
            mRandFrames = mRandTimer;
        } else {
            if ((int)mRandTimer < (int)mRandFrames - 5) {
                /* Keeping the member-address temporary is load-bearing: CW
                 * retains the 0x92 pointer in r2 and the earlier this+0x300
                 * base in r0, exactly as the cartridge does. */
                short *angleStep = &mPrevAngleX;
                *angleStep = (short)(*angleStep * mRandDirection);
            } else {
                mPrevAngleX = 0;
            }
        }
    }
    func_020393a4((int *)&mMeshCollider, 0x180000);
    func_02039394((int *)&mMeshCollider, 0x1000);
    /* As above, materialising the inherited field address preserves the
     * cartridge's add-r3/load/store sequence. */
    short *angle = &mAngleX;
    *angle = (short)(*angle + mPrevAngleX);
    UpdateModel();
    UpdateShadow();
    if (_ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(
            (char *)this, 0x180000, 0x1000))
        UpdateClsnPosAndRot();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN16SpinningPlatform6RenderEv, 0x02111934, size 0x28 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatform6RenderEv
int SpinningPlatform::Render()
{
    mModel.Render(0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN16SpinningPlatform16CleanupResourcesEv, 0x021118f0, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatform16CleanupResourcesEv
int SpinningPlatform::CleanupResources()
{
    if (mMeshCollider.IsEnabled()) {
        mMeshCollider.Disable();
    }
    RotatingClockHand_ModelFile.Release();
    RotatingClockHand_ClsnFile.Release();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN16SpinningPlatform11UpdateModelEv, 0x021118a8, size 0x48 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatform11UpdateModelEv
void SpinningPlatform::UpdateModel()
{
    Matrix4x3_FromRotationXYZExt(&mModel.mat4x3, mAngleX, mAngleY, mAngleZ);
    mModel.mat4x3.m[9] = mPosX >> 3;
    mModel.mat4x3.m[10] = mPosY >> 3;
    mModel.mat4x3.m[11] = mPosZ >> 3;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- _ZN16SpinningPlatform12UpdateShadowEv, 0x02111798, size 0x110 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatform12UpdateShadowEv
/* Fix12<int> by-value arguments are a measured mwccarm ABI wall: spelling this
 * as the class method homes the wrappers and no longer reproduces the ROM. */
int SpinningPlatform::UpdateShadow()
{
    int d1 = mPosY - mFloorPosY;
    int ad = d1 < 0 ? -d1 : d1;
    if (ad > 0x7d0000) return ad;
    int r2 = ad + 0x350000;
    int half = ad / 2;
    if (r2 < 0x350000) r2 = 0x350000;
    mClipOffsetY = -half;
    mClipRadius = (int)(r2 + ((unsigned)r2 >> 31)) >> 4;
    Matrix4x3_FromRotationY(&mShadowMat, mAngleY);
    mShadowMat.m[9] = mPosX >> 3;
    mShadowMat.m[10] = mFloorPosY >> 3;
    mShadowMat.m[11] = mPosZ >> 3;
    int idx = (unsigned short)mAngleX >> 4;
    int s = data_02082214[(idx << 1) + 1];
    int sa = s < 0 ? -s : s;
    int scaled = (int)(((long long)sa * 0x26c000 + 0x800) >> 12);
    return _ZN8dActor_c18DropShadowScaleXYZER11ShadowModelR9Matrix4x35Fix12IiES5_S5_j(
        this, &mShadowModel, &mShadowMat, 0x12c000, 0x32000,
        scaled + 0x50000, 0xf);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN16SpinningPlatformD1Ev, 0x021116ec, size 0x4c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN16SpinningPlatformD1Ev
/* Real compiler-spelled complete destructor. mwccarm derives the reverse
 * teardown order from the class: ShadowModel, then dBgActor_c's moving mesh
 * collider and Model, then the dActor_c base chain. */
SpinningPlatform::~SpinningPlatform()
{
}
