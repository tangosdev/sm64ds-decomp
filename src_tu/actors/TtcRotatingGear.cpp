//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov065/TtcRotatingGear  (6 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x0211b7f0  src/_ZN15TtcRotatingGearD1Ev.cpp
 *   [1] 0x0211b834  src/_ZN15TtcRotatingGearD0Ev.cpp
 *   [2] 0x0211b88c  src/_ZN15TtcRotatingGear16CleanupResourcesEv.cpp
 *   [3] 0x0211b8d0  src/_ZN15TtcRotatingGear6RenderEv.cpp
 *   [4] 0x0211b8f8  src/_ZN15TtcRotatingGear8BehaviorEv.cpp
 *   [5] 0x0211ba88  src/_ZN15TtcRotatingGear13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "TtcRotatingGear.h"
#include "decl_common.h"
#include "SharedFilePtr.h"
#include "dBgW.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow struct 'Base' */
struct Base { virtual void v0(); virtual void v1(); virtual void v2(); virtual void v3(); virtual void v4(); virtual void m(int); };

/* shadow struct 'Derived' */
struct Derived { char pad[0xd4]; Base base; };

extern "C" {
extern int TtcRotatingGear_ClsnFile[];
extern int TtcRotatingGear_ModelFile[];
extern u8  data_0209f2c0;      /* clock hand setting */
extern int data_0209e650;      /* the shared RNG seed */
extern u8  data_ov065_0211c0d4[];  /* per-setting timer table, 16-byte stride */
extern u8  data_ov065_0211c0d0[];  /* per-setting speed table, 16-byte stride */
u16 DecIfAbove0_Short(void *p);
int RandomIntInternal(int *seed);
int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(dBgActor_c *self, int a, int b);
extern "C" void* _ZN5Model8LoadFileER13SharedFilePtr(void*);
extern "C" void _ZN9ModelBase7SetFileEP8BMD_Fileii(void*, void*, int, int);
extern "C" void _ZN10dBgActor_c21UpdateModelPosAndRotYEv(void*);
extern "C" void _ZN10dBgActor_c19UpdateClsnPosAndRotEv(void*);
extern "C" void* _ZN7dBgW_Kc8LoadFileER13SharedFilePtr(void*);
extern "C" void _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block( void*, void*, void*, int, short, void*);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN15TtcRotatingGear13InitResourcesEv, 0x0211ba88, size 0xf4 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15TtcRotatingGear13InitResourcesEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
/* Mangled ROM names need extern "C" in a .cpp file, or the compiler re-mangles them
   into phantoms no module defines. */
int TtcRotatingGear::InitResources()
{
    void* mdl;
    void* kcl;
    unsigned int ip;
    unsigned int b;
    mdl = _ZN5Model8LoadFileER13SharedFilePtr(data_ov065_0211d98c);
    _ZN9ModelBase7SetFileEP8BMD_Fileii(((char*)this) + 0xd4, mdl, 1, -1);
    _ZN10dBgActor_c21UpdateModelPosAndRotYEv(((char*)this));
    _ZN10dBgActor_c19UpdateClsnPosAndRotEv(((char*)this));
    kcl = _ZN7dBgW_Kc8LoadFileER13SharedFilePtr(data_ov065_0211d97c);
    _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
        ((char*)this) + 0x124, kcl, ((char*)this) + 0x2ec, 0x199, mAngleY, func_021121b8);
    mHomePosX = mPosX;
    mHomePosY = mPosY;
    mHomePosZ = mPosZ;
    mMoveDir = 0;
    ip = (&data_0209f2c0)[0];   /* kept the first, scalar declaration; index at the use site */
    b = mMoveDir;
    mMoveTimer = *(unsigned short*)(data_ov065_0211c0d4 + (ip << 4) + (b << 3));
    b = mMoveDir;
    mVertSpeed = *(int*)(data_ov065_0211c0d0 + (ip << 4) + (b << 3));
    mTerminalVelocity = -0x3c000;
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN15TtcRotatingGear8BehaviorEv, 0x0211b8f8, size 0x190 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15TtcRotatingGear8BehaviorEv
/* TtcRotatingGear::Behavior -- Tick Tock Clock's lifts.
 *
 * data_0209f2c0 is the level's clock hand setting. 3 ("random") pins the lift
 * at the top of its travel; otherwise it runs on a timer, and each time the
 * timer expires the gear clamps itself back inside [mHomePosY, mHomePosY +
 * 0x14a000], flips mMoveDir, and reloads speed and timer from the two 16-byte
 * per-setting tables. Setting 2 overrides the timer with a random multiple of
 * 0x14 in [0xa, 0x6e].
 *
 * The control flow is the ROM's, gotos and all -- the clamp reads as an
 * if/else but compiles to the interleaved form only in this shape.
 *
 * IsClsnInRange carries Fix12<int> by value (wall 6az), so it stays an extern-C
 * mangled free function; UpdateModelPosAndRotY, UpdateClsnPosAndRot and
 * UpdatePos are members.
 */
int TtcRotatingGear::Behavior()
{
    if (data_0209f2c0 == 3) {
        mPosY = mHomePosY + 0x14a000;
        UpdateModelPosAndRotY();
        if (_ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(this, 0, 0) != 0)
            UpdateClsnPosAndRot();
        goto ret1;
    }

    if (DecIfAbove0_Short(&mMoveTimer) == 0) {

        s32 lower, upper, y, in;
        UpdatePos(0);
        lower = mHomePosY;
        y = mPosY;
        upper = lower + 0x14a000;
        if (y < lower)
            goto Lzero;
        if (y <= upper) {
            in = 1;
            goto Lchk;
        }
Lzero:
        in = 0;
Lchk:
        if (in != 0)
            goto tail;
        {
            s32 v = lower;
            if (y >= lower) {
                s32 t = upper;
                if (y <= upper)
                    t = y;
                v = t;
            }
            mPosY = v;
        }

        {
            /* THE LAUNDERED ADDRESS IS LOAD-BEARING, per-site. Spelling this
               as `mMoveDir ^= 1' or as a load/store pair through the member
               changes the function's SIZE (999 words, i.e. a length mismatch),
               not just its allocation -- the same RMW-address CSE described in
               notes/mwccarm-codegen.md. Taking the address through an integer
               first defeats it. */
            u8 *tog = (u8 *)((int)this + 0x32e);
            u8 dir = *tog;
            *tog = dir ^ 1;
            {
                int idx = data_0209f2c0;
                u8 t = mMoveDir;
                mMoveTimer = *(u16 *)(&data_ov065_0211c0d4[idx * 16] + t * 8);
                t = mMoveDir;
                mVertSpeed = *(s32 *)(&data_ov065_0211c0d0[idx * 16] + t * 8);
                if (data_0209f2c0 != 2)
                    goto tail;
                {
                    u32 r = (u32)RandomIntInternal(&data_0209e650);
                    mMoveTimer = (u16)((r % 6) * 0x14 + 0xa);
                }
            }
        }

    }

tail:
    UpdateModelPosAndRotY();
    if (_ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(this, 0, 0) != 0)
        UpdateClsnPosAndRot();
ret1:
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN15TtcRotatingGear6RenderEv, 0x0211b8d0, size 0x28 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15TtcRotatingGear6RenderEv
/* recovered: named members + shared header, real C++ method */
int TtcRotatingGear::Render()
{
 Base *b = &((Derived *)this)->base; b->m(0); return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- _ZN15TtcRotatingGear16CleanupResourcesEv, 0x0211b88c, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15TtcRotatingGear16CleanupResourcesEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
int TtcRotatingGear::CleanupResources()
{
    if (((dBgW *)((char *)&(*(u8 *)&mMeshCollider)))->IsEnabled()) {
        ((dBgW *)((char *)&(*(u8 *)&mMeshCollider)))->Disable();
    }
    ((SharedFilePtr *)(TtcRotatingGear_ModelFile))->Release();
    ((SharedFilePtr *)(TtcRotatingGear_ClsnFile))->Release();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN15TtcRotatingGearD1Ev, 0x0211b7f0, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN15TtcRotatingGearD1Ev
/* recovered: real C++ destructor -- the compiler emits the whole body
 *
 * Two vtable stores and three destructor calls, every one a consequence of
 * `struct TtcRotatingGear : dBgActor_c`: its own vptr, then dBgActor_c's -- inlined,
 * because dBgActor_c's destructor is defined in its class body -- then
 * dBgActor_c's Model and dBgW_KcMbg, then dActor_c. This class adds no
 * member with a destructor of its own.
 */
TtcRotatingGear::~TtcRotatingGear()
{
}
