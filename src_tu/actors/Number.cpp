//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov002/Number  (8 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x020f0894  src/_ZN6NumberD1Ev.cpp
 *   [1] 0x020f08cc  src/_ZN6NumberD0Ev.c
 *   [2] 0x020f0918  src/func_ov002_020f0918.c
 *   [3] 0x020f093c  src/_ZN6Number16CleanupResourcesEv.cpp
 *   [4] 0x020f0994  src/_ZN6Number6RenderEv.cpp
 *   [5] 0x020f0a60  src/_ZN6Number8BehaviorEv.cpp
 *   [6] 0x020f0bd4  src/_ZN6Number13InitResourcesEv.cpp
 *   [7] 0x020f0d90  src/d_a_obj_number.c
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "decl_Actor.h"
#include "decl_Model.h"
#include "decl_common.h"
#include "Number.h"
#include "SharedFilePtr.h"
#include "types.h"
#include "TextureSequence.h"
#include "decl_ActorBase.h"
#include "decl_TextureSequence.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow structs 'dActor_c'/'Model'/'TextureSequence'/'Number' deleted --
 * Number.h now declares real inheritance and pulls in the real classes via
 * dActor_c.h/Model.h/TextureSequence.h; these flat-era shadow copies
 * redefined all four. Same fix as every prior pilot's flat-header rewrite. */

/* shadow struct 'Obj' */
struct Obj {
    virtual void v0();
    virtual void v1();
    virtual void v2();
    virtual void v3();
    virtual void v4();
    virtual void m(int a);
};

/* shadow struct 'V3' */
struct V3 { int x, y, z; };

/* shadow struct 'SharedFilePtr' deleted -- SharedFilePtr.h (included above)
 * declares the real class, colliding with this flat-era shadow copy. The
 * real class deliberately has NO fields (its own header: layout not
 * recovered project-wide) but this TU needs both views of the same four
 * global objects -- CleanupResources calls the real class's Release()
 * method, InitResources reads the raw .file field the real class doesn't
 * expose. One local layout shadow backs the extern declarations; the
 * real-class call sites cast through it. */
struct SharedFilePtrLayout { void *p; void *file; };

/* shadow struct 'BMD_File' */
struct BMD_File;

/* shadow struct 'BTP_File' */
struct BTP_File;

extern "C" {
extern void _ZN15TextureSequenceD1Ev(void *);
extern void *data_020a0eac;
extern void* _ZN8dActor_c15FindWithActorIDEjPS_(u32 id, void* prev);
extern void _ZN7fBase_c18MarkForDestructionEv(void* thiz);
extern void _ZN15TextureSequence6UpdateER15ModelComponents(void* ts, void* mc);
extern int data_0209b454;
extern void _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_(unsigned int id, int x, int y, int z);
extern char *_ZN8dActor_c10FindWithIDEj(unsigned int id);
extern void Matrix4x3_FromTranslation(void *m, int x, int y, int z);
void _ZN15TextureSequence8LoadFileER13SharedFilePtr(SharedFilePtr& f);
void* _ZN5Model8LoadFileER13SharedFilePtr(SharedFilePtr& f);
int _ZN9ModelBase7SetFileEP8BMD_Fileii(void* self, void* file, int a, int b);
void _ZN15TextureSequence7SetFileER8BTP_Filei5Fix12IiEj(void* self, void* btp, int a, int b, unsigned int c);
extern SharedFilePtrLayout data_ov002_0210da08;
extern SharedFilePtrLayout data_ov002_0210da28;
extern SharedFilePtrLayout data_ov002_0210d9e8;
extern SharedFilePtrLayout data_ov002_0210d9a8;
/* TUBUILD CONFLICT -- alternate declaration of _ZN7fBase_c18MarkForDestructionEv, from the legacy file for _ZN6Number8BehaviorEv, NOT applied: extern void _ZN7fBase_c18MarkForDestructionEv(char *self); */
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 7 -- daObjNumber_c_classInit, 0x020f0d90, size 0x40 */
/* -------------------------------------------------------------------------- */
// @symbol daObjNumber_c_classInit
/* recovered: vtable identified, globals resolved, declarations from a shared header */
/* recovered: vtable identified, globals resolved */
/* resolved: VT0 = _ZTV6Number */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int *daObjNumber_c_classInit(void)
{
    int *p = (int *)_ZN7fBase_cnwEj(336);
    if (p) {
        _ZN8dActor_cC2Ev(p);
        p[0] = (int)(_ZTV6Number + 2);
        _ZN5ModelC1Ev((char *)p + 0xd4);
        _ZN15TextureSequenceC1Ev((char *)p + 0x124);
    }
    return p;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN6Number13InitResourcesEv, 0x020f0bd4, size 0x1bc */
/* -------------------------------------------------------------------------- */
//cpp
// @symbol _ZN6Number13InitResourcesEv
/* recovered: named members + shared header, real C++ method */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int Number::InitResources()
{
    if (param1 & 0x10) {
        _ZN15TextureSequence8LoadFileER13SharedFilePtr(*(SharedFilePtr*)&data_ov002_0210da08);
        void* m = _ZN5Model8LoadFileER13SharedFilePtr(*(SharedFilePtr*)&data_ov002_0210da28);
        if (_ZN9ModelBase7SetFileEP8BMD_Fileii(((char*)this) + 0xd4, m, 1, 1) == 0)
            return 0;
        TextureSequence::Prepare(*(BMD_File*)data_ov002_0210da28.file,
                                 *(BTP_File*)data_ov002_0210da08.file);
        _ZN15TextureSequence7SetFileER8BTP_Filei5Fix12IiEj(
            ((char*)this) + 0x124, data_ov002_0210da08.file, 0x40000000, 0, 0);
        /* 0x12c is +0x8 inside the TextureSequence at 0x124 -- its Animation base's
           currFrame. The cartridge's own ~Number proves the extent; see
           tools/dtor_members.py. */
        mTextureSequence.currFrame = (int)((((unsigned int)(param1 & 0xf) % 10) << 16) >> 4);
    } else {
        _ZN15TextureSequence8LoadFileER13SharedFilePtr(*(SharedFilePtr*)&data_ov002_0210d9e8);
        void* m = _ZN5Model8LoadFileER13SharedFilePtr(*(SharedFilePtr*)&data_ov002_0210d9a8);
        if (_ZN9ModelBase7SetFileEP8BMD_Fileii(((char*)this) + 0xd4, m, 1, 1) == 0)
            return 0;
        TextureSequence::Prepare(*(BMD_File*)data_ov002_0210d9a8.file,
                                 *(BTP_File*)data_ov002_0210d9e8.file);
        _ZN15TextureSequence7SetFileER8BTP_Filei5Fix12IiEj(
            ((char*)this) + 0x124, data_ov002_0210d9e8.file, 0x40000000, 0, 0);
        mTextureSequence.currFrame = (int)((((unsigned int)(param1 & 0xf) % 10) << 16) >> 4);
    }

    mState = 0;
    mStartPosX = mPosX;
    mStartPosY = mPosY;
    mStartPosZ = mPosZ;
    mVertSpeed = 0x14000;
    mVertAccel = -0x2000;
    mTerminalVelocity = -0x32000;
    *(short*)(((char*)this) + 0x100 + 0x4c) = 0;
    mOwnerUniqueID = 0;
    mFollowOffsetY = 0;
    return 1;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN6Number8BehaviorEv, 0x020f0a60, size 0x174 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN6Number8BehaviorEv
/* recovered: named members + shared header, real C++ method */
int Number::Behavior()
{
    struct V3 pos;

    if (mDelay != 0) return 1;

    *(int *)((int)((char *)&mVertSpeed)) += mVertAccel;
    if (mVertSpeed < mTerminalVelocity)
        mVertSpeed = mTerminalVelocity;
    *(int *)((int)((char *)&mPosY)) += mVertSpeed;

    switch (mState) {
    case 0:
        if (mPosY < mStartPosY) {
            mPosY = mStartPosY;
            mVertSpeed = 0xf000;
            (*(u8 *)((int)((char *)&mState)))++;
        }
        break;
    case 1:
        if (mPosY < mStartPosY) {
            _ZN8Particle6System9NewSimpleEj5Fix12IiES2_S2_(0xd2, mPosX, *(volatile int *)((char *)&mPosY), mPosZ);
            _ZN7fBase_c18MarkForDestructionEv(((char *)this));
        }
        break;
    }

    pos.x = mPosX;
    pos.y = mPosY;
    pos.z = mPosZ;
    if (mOwnerUniqueID != 0) {
        char *other = _ZN8dActor_c10FindWithIDEj(mOwnerUniqueID);
        if (other != 0) {
            struct V3 *op = (struct V3 *)((int)(other + 0x5c));
            int oy;
            pos.x = op->x;
            pos.y = oy = op->y;
            pos.z = op->z;
            pos.y = oy + (mFollowOffsetY + (mPosY - mStartPosY));
        }
    }
    /* 0x0f0 is +0x1c inside the Model at 0x0d4 -- its mat4x3. The cartridge's own
       ~Number proves the Model's extent; see tools/dtor_members.py. */
    Matrix4x3_FromTranslation((void *)((char *)&mModel.mat4x3), pos.x >> 3, pos.y >> 3, (*(volatile int *)&pos.z) >> 3);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN6Number6RenderEv, 0x020f0994, size 0xcc */
/* -------------------------------------------------------------------------- */
// @symbol _ZN6Number6RenderEv
/* recovered: named members + shared header, real C++ method */
int Number::Render()
{
    if (data_0209b454 & 0x4000000) {
        void* a = 0;
        u32 id = 0xb2;
        for (;;) {
            if ((a = _ZN8dActor_c15FindWithActorIDEjPS_(id, a)) == 0)
                break;
            if (*(int*)((char*)a + 0x43c) == 6 && *(u16*)((char*)a + 0x496) == 0x64) {
                _ZN7fBase_c18MarkForDestructionEv(((char*)this));
                return 1;
            }
        }
    }

    if (mDelay != 0) {
        u16* p = (u16*)(int)((char*)&mDelay);
        *p = *p - 1;
        return 1;
    }

    _ZN15TextureSequence6UpdateER15ModelComponents(((char*)this) + 0x124, ((char*)this) + 0xdc);
    ((Obj*)((char*)&mModel))->m(0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN6Number16CleanupResourcesEv, 0x020f093c, size 0x58 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN6Number16CleanupResourcesEv
/* recovered: named members + shared header, real C++ method */
int Number::CleanupResources()
{
  if(param1 & 0x10){
    ((SharedFilePtr *)(&data_ov002_0210da28))->Release();
    ((SharedFilePtr *)(&data_ov002_0210da08))->Release();
  } else {
    ((SharedFilePtr *)(&data_ov002_0210d9a8))->Release();
    ((SharedFilePtr *)(&data_ov002_0210d9e8))->Release();
  }
  return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov002_020f0918, 0x020f0918, size 0x24 */
/* -------------------------------------------------------------------------- */
extern "C" {  /* .c-derived member: C linkage for the whole block */
void func_ov002_020f0918(char *a, char *b)
{
    if (!b)
        return;
    *(int *)(a + 0x138) = *(int *)(b + 4);
    *(int *)(a + 0x148) = *(int *)(a + 0x60) - *(int *)(b + 0x60);
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 1 -- _ZN6NumberD0Ev, 0x020f08cc, size 0x4c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN6NumberD0Ev
/* Deleted: keeping this hand-written mangled-name free-function body next to
   the real out-of-line Number::~Number() (D1, below) is the mwcc ICE this
   series has hit repeatedly (ELFgen.c:483). The real D1 makes the compiler
   synthesize D0 itself, byte-identical to the ROM (confirmed by verify, not
   assumed). This legacy body stored the coined alias _ZTV13daObjNumber_c
   directly rather than this project's own _ZTV6Number -- expect the
   WingFeather/#1727 linkcheck --partial pitfall on this ordinal. */

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN6NumberD1Ev, 0x020f0894, size 0x38 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN6NumberD1Ev
Number::~Number()
{
}
