//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov027/ChillBully+daIDonketu_c  (7 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x021115c4  src/_ZN10ChillBullyD1Ev.cpp
 *   [1] 0x02111618  src/_ZN10ChillBullyD0Ev.cpp
 *   [2] 0x02111680  src/func_ov027_02111680.c
 *   [3] 0x021116f0  src/func_ov027_021116f0.cpp
 *   [4] 0x02111770  src/_ZN10ChillBully8BehaviorEv.cpp
 *   [5] 0x0211181c  src/_ZN10ChillBully13InitResourcesEv.cpp
 *   [6] 0x0211186c  src/ChillBully_Spawn.c
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "ChillBully.h"
#include "decl_common.h"
#include "decl_ActorBase.h"
#include "decl_Enemy.h"
#include "decl_ModelAnim.h"
#include "decl_dCcAc_c.h"
#include "decl_ShadowModel.h"
#include "decl_dBgCh_Actr.h"

extern "C" {
extern "C" int func_ov064_02116d1c(char *c);
extern int func_ov064_02116ec0(void *);
extern int _ZN8dActor_c9TrackStarEjj(void *, unsigned int, unsigned int);
extern int data_ov064_0211b768[];
extern int data_ov027_02113930[];
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- ChillBully_Spawn, 0x0211186c, size 0x5c */
/* -------------------------------------------------------------------------- */
// @symbol ChillBully_Spawn
/* recovered: vtable identified, declarations from a shared header */
/* recovered: vtable identified */
/* vtable identified: VT0 = data_ov064_0211b768 */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int *ChillBully_Spawn(void)
{
    int *p = (int *)_ZN7fBase_cnwEj(1020);
    if (p) {
        _ZN12dEnemyBase_cC2Ev(p);
        p[0] = (int)data_ov064_0211b768;
        _ZN9ModelAnimC1Ev((char *)p + 0x110);
        _ZN10dBgCh_ActrC1Ev((char *)p + 0x174);
        _ZN7dCcAc_cC1Ev((char *)p + 0x33c);
        _ZN11ShadowModelC1Ev((char *)p + 0x370);
        p[0] = (int)data_ov027_02113930;
    }
    return p;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN10ChillBully13InitResourcesEv, 0x0211181c, size 0x50 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10ChillBully13InitResourcesEv
/* recovered: real C++ method -- vtable slot 0, previously func_ov027_0211181c
 *
 * daOts_c leaves slot 0 pure virtual (the word in its vtable is a literal zero), so
 * every one of its three children has to supply this. ChillBully's points the shared
 * file table at its own list and then claims a star slot.
 */
int ChillBully::InitResources()
{
    mFileTable = (int)data_ov027_021138f4;
    int r = func_ov064_02116ec0((char *)this);
    mStarIdx = param1 & 0xf;
    mStarSlot = _ZN8dActor_c9TrackStarEjj((char *)this, mStarIdx, 2);
    return r;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN10ChillBully8BehaviorEv, 0x02111770, size 0xac */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10ChillBully8BehaviorEv
/* recovered: real C++ method -- vtable slot 6, previously func_ov027_02111770
 *
 * daOts_c leaves slot 6 pure virtual, so each of its three children supplies its own.
 * ChillBully's is the star-carrying one: if an invincible character killed it, it
 * poofs, drops its star a little above itself, and marks itself for destruction;
 * otherwise it falls through to the shared behaviour in ov064.
 *
 * Every call below is a real method now, so the compiler mangles the names and the
 * signatures become falsifiable -- including UntrackAndSpawnStar's `s8 &`, which is
 * why mStarSlot is signed. See the long note on that symbol in dActor_c.h.
 */
int ChillBully::Behavior()
{
    int r = UpdateKillByInvincibleChar(mWithMeshClsn, mModelAnim, 0);
    if (r != 0) {
        if (r == 2) {
            Vector3 v;
            TriplePoofDust();
            v.x = mPosX;
            v.y = mPosY;
            v.z = mPosZ;
            v.y = v.y + 0x64000;
            UntrackAndSpawnStar(mStarSlot, (mStarIdx | 0x40) & 0xff, v, 4);
            MarkForDestruction();
        }
        return 1;
    }
    return func_ov064_02116d1c((char *)this);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- func_ov027_021116f0, 0x021116f0, size 0x80 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov027_021116f0
/* ChillBully's star drop, shared with the Behavior above it.
 *
 * It used to carry the recovered name `daIDonketu_c::AfterClsn`, attributed "from
 * vtable slot identity" -- but this address is in NO slot of ChillBully's vtable.
 * All 31 were checked against daOts_c's and dEnemyBase_c's; the four this class overrides
 * are 0 (InitResources), 6 (Behavior) and 16/17 (the destructor pair). So the name
 * is withdrawn and the placeholder kept until something actually names it.
 */
#include "ChillBully.h"
#include "decl_common.h"

extern "C" {
extern int func_ov064_0211616c(void *);

int func_ov027_021116f0(char *c)
{
    ChillBully *self = (ChillBully *)(void *)c;
    int r = func_ov064_0211616c(c);
    if (r == 0) return r;
    self->TriplePoofDust();
    int v[3];
    v[0] = self->mPosX;
    v[1] = self->mPosY;
    v[2] = self->mPosZ;
    v[1] += 0x64000;
    self->UntrackAndSpawnStar(self->mStarSlot, (self->mStarIdx | 0x40) & 0xff,
                              *(const Vector3 *)v, 4);
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- func_ov027_02111680, 0x02111680, size 0x70 */
/* -------------------------------------------------------------------------- */
// @symbol func_ov027_02111680
// recovered name: daIDonketu_c_Kill
/* recovered: renamed to Class_Method, declarations from a shared header */
/* recovered: renamed to Class_Method */
/* daIDonketu_c::Kill - recovered from vtable slot identity */
extern "C" {  /* .c-derived member: C linkage for the whole block */
int func_ov027_02111680(char* self){
    if(*(unsigned short*)(self+0x100) < 0xa){
        *(int*)(self+0x98) = 0;
        int r = func_ov064_02116110(self, 0x700);
        if(r != 0) return r;
        unsigned short* p = (unsigned short*)(self+0x100);
        if(*p == 9) *p = 0;
        return (int)p;
    }
    *(int*)(self+0x98) = 0x14000;
    unsigned short* p = (unsigned short*)(self+0x100);
    unsigned int v = *p;
    if(v > 0x23){ v = 0; *p = 0; }
    return v;
}
}

/* -------------------------------------------------------------------------- */
/* ROM ordinals 0/1 -- _ZN10ChillBullyD1Ev (0x021115c4, size 0x54) and
 * _ZN10ChillBullyD0Ev (0x02111618, size 0x68). One definition; the compiler
 * emits both D1 and D0 from it (matches the established pattern in e.g.
 * AmbientSoundEffects.cpp, Tree.cpp and every other merged TU with a
 * destructor pair -- defining ChillBully::~ChillBully() twice in one TU is
 * an ODR violation the compiler rejects outright, not a codegen question).
 *
 * D1 (slot 16): own vptr, then daOts_c's -- inlined, because that class's
 * destructor is defined in its class body -- then the base's four members in
 * reverse declaration order, then dEnemyBase_c. D0 (slot 17) is the same plus
 * Memory::Deallocate via the inherited dEnemyBase_c::operator delete.
 * ChillBully adds no member with a destructor of its own, only two bytes.
 *
 * Byte-for-byte the same shape as Bully's and BigBully's, which is what three
 * siblings sharing a base look like.
 */
ChillBully::~ChillBully()
{
}
