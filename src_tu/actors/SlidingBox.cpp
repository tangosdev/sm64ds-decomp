//cpp
/* SHADOW translation unit -- generated by tools/tubuild.py create.
 * ov016/SlidingBox  (7 function(s))
 *
 * NOT ENROLLED, NOT CANONICAL. This file contributes nothing to the ROM
 * build. It is a STARTING POINT (plan sec 7.3): local shadow declarations
 * below were carried verbatim from the legacy files, not reconciled against
 * real project headers -- that judgement call is left to a human/LLM review,
 * the way pilot #1 reconciled PoleLift::Render and ::CleanupResources by hand
 * (see notes/tu-reconstruction-pilot-report.md sec 5.2).
 *
 * FUNCTION ORDER IS DELIBERATELY THE REVERSE OF THE ROM'S -- mwccarm 2004/b56
 * emits one .text section per function, in the REVERSE of source order, so
 * the highest-address ROM function is written FIRST here. Do not reorder;
 * see notes/tu-reconstruction-pilot-report.md sec 3 for the one documented
 * exception (a destructor's D0/D1/D2 group has compiler-chosen order).
 *
 * Assembled from these legacy one-function sources (ROM address order):
 *   [0] 0x02112ff8  src/_ZN10SlidingBoxD1Ev.cpp
 *   [1] 0x02113044  src/_ZN10SlidingBoxD0Ev.cpp
 *   [2] 0x021130a4  src/_ZN10SlidingBox11UpdateModelEv.cpp
 *   [3] 0x021130ec  src/_ZN10SlidingBox16CleanupResourcesEv.cpp
 *   [4] 0x02113130  src/_ZN10SlidingBox6RenderEv.cpp
 *   [5] 0x02113158  src/_ZN10SlidingBox8BehaviorEv.cpp
 *   [6] 0x02113434  src/_ZN10SlidingBox13InitResourcesEv.cpp
 */

/* Includes: union of the legacy files', first-seen in ROM-ascending
 * processing order. NOT verified for header ordering constraints (e.g. a
 * common.h-before-X rule) -- watch for new compile errors after this. */
#include "SlidingBox.h"
#include "decl_common.h"
#include "SharedFilePtr.h"
#include "SurfaceInfo.h"

/* Local shadow declarations carried from the legacy files verbatim.
 * NOT reconciled against real project headers -- check include/*.h for
 * each of these before compiling; a real header should usually win. */
/* shadow namespace 'cstd' */
namespace cstd { int fdiv(int a, int b); }

/* shadow namespace 'Sound' */
namespace Sound {
u32 PlayLong(u32 handle, u32 player, u32 soundID,
             const Vector3 &position, s16 arg);
}

extern "C" {
/* Matrix4x3_FromRotationXYZExt is already declared by decl_common.h above as
 * (void*, int, int, int). Keeping that FIRST declaration and casting at the use
 * site; redeclaring it with Matrix4x3* here is "illegal function overloading". */
extern int data_ov016_02114e74[];
extern short data_02082214[];
void dBgCh_Actr_UpdateContinuous_Veneer(void *p);
void *_ZNK10dBgCh_Actr14GetFloorResultEv(void *self);
int _ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_(void *self, int a, int b);
/* The two SetFile/Init names carry Fix12<int> by value. Defining or calling
* them as ordinary methods changes mwccarm's ABI, so these stay raw at the
* measured by-value-class wall documented in dBgActor_c.h. */
extern void _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(void* thiz, void* kcl, void* mtx, int fix, short s, void* clps);
extern void func_020393d4(void* p, void* v);
extern void _ZN10dBgCh_Actr4InitEP8dActor_c5Fix12IiES3_P10Vector3_16S5_(void* thiz, void* act, int fix, int t, void* vr, int t2);
extern void _ZN4dBgW22UpdatePosWithTransformERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_(void);
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 6 -- _ZN10SlidingBox13InitResourcesEv, 0x02113434, size 0xdc */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10SlidingBox13InitResourcesEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
int SlidingBox::InitResources()
{
    BMD_File *modelFile = (BMD_File *)Model::LoadFile(
        *(SharedFilePtr *)data_ov016_02114e74);
    mModel.SetFile(modelFile, 1, -1);
    UpdateModel();
    UpdateClsnPosAndRot();
    KCL_File *clsnFile = (KCL_File *)dBgW_Kc::LoadFile(
        *(SharedFilePtr *)data_ov016_02114e6c);
    _ZN10dBgW_KcMbg7SetFileEP8KCL_FileRK9Matrix4x35Fix12IiEsR10CLPS_Block(
        &mMeshCollider, clsnFile, &mClsnMat, 0x199, mAngleY, data_ov016_02113bac);
    func_020393d4(&mMeshCollider, (void*)&_ZN4dBgW22UpdatePosWithTransformERS_P8dActor_cR5dBgPiR7Vector3P10Vector3_16S8_);
    _ZN10dBgCh_Actr4InitEP8dActor_c5Fix12IiES3_P10Vector3_16S5_(&mWithMeshClsn, this, 0x14000, 0x14000, 0, 0);
    mVertAccel = -0x2000;
    mTerminalVelocity = -0x14000;
    mShip = 0;
    mState = 0;
    mSoundID = 0;
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 5 -- _ZN10SlidingBox8BehaviorEv, 0x02113158, size 0x2dc */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10SlidingBox8BehaviorEv
/* recovered: named members + shared header, real C++ method */
int SlidingBox::Behavior()
{
    Vector3 normal;

    switch (mState) {
    case 0:
        mShip = dActor_c::FindWithActorID(0x39, 0);
        if (mShip == 0) {
            MarkForDestruction();
            break;
        }
        mState++;
        /* fallthrough */
    case 1:
        UpdatePos(0);
        dBgCh_Actr_UpdateContinuous_Veneer((char *)&mWithMeshClsn);
        if (mWithMeshClsn.IsOnGround()) {
            mState++;
            mBasePos.x = mPosX;
            mBasePos.y = mPosY;
            mBasePos.z = mPosZ;
        }
        break;
    case 2: {
        s16 *shipAngles = &mShip->mAngleX;
        int spd;
        mAngleX = shipAngles[0];
        mAngleY = shipAngles[1];
        mAngleZ = shipAngles[2];
        mPrevAngleY = mAngleY;
        mHorzSpeed = data_02082214[((u16)mAngleX >> 4) * 2] * 0x8c;
        mHorzPos += mHorzSpeed;
        spd = mHorzPos;
        if (spd >= 0x4ff000)
            mHorzPos = 0x4ff000;
        else if (spd < -0x32000)
            mHorzPos = -0x32000;
        mPosX = mBasePos.x + (int)(((long long)mHorzPos * data_02082214[((u16)mAngleY >> 4) * 2] + 0x800) >> 12);
        mPosZ = mBasePos.z + (int)(((long long)mHorzPos * data_02082214[((u16)mAngleY >> 4) * 2 + 1] + 0x800) >> 12);
        mHorzSpeed = 0;
        UpdatePos(0);
        dBgCh_Actr_UpdateContinuous_Veneer((char *)&mWithMeshClsn);
        if (mWithMeshClsn.IsOnGround()) {
            void *fr = _ZNK10dBgCh_Actr14GetFloorResultEv((char *)&mWithMeshClsn);
            ((SurfaceInfo *)((char *)fr + 4))->CopyNormalTo(normal);
            if (normal.y != 0) {
                mVertSpeed = -(cstd::fdiv(
                    (int)(((long long)normal.x * unk_0a4 + 0x800) >> 12)
                  + (int)(((long long)normal.z * unk_0ac + 0x800) >> 12),
                    normal.y) + 0x8000);
            }
        }
        if (DistToCPlayer() < 0x7d0000) {
            int vel = mHorzSpeed;
            if (vel < 0)
                vel = -vel;
            if (vel > 0x3000) {
                mSoundID = Sound::PlayLong(
                    mSoundID, 3, 0x9f, *(Vector3 *)&mCamSpacePosX, 0);
            }
        }
        break;
    }
    }

    UpdateModel();
    if (_ZN10dBgActor_c13IsClsnInRangeE5Fix12IiES1_((char *)this, 0, 0)) {
        UpdateClsnPosAndRot();
    }
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 4 -- _ZN10SlidingBox6RenderEv, 0x02113130, size 0x28 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10SlidingBox6RenderEv
/* recovered: named members + shared header, real C++ method */
int SlidingBox::Render()
{
    mModel.Render(0);
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 3 -- _ZN10SlidingBox16CleanupResourcesEv, 0x021130ec, size 0x44 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10SlidingBox16CleanupResourcesEv
/* recovered: named members + shared header, real C++ method, declarations from a shared header */
/* recovered: named members + shared header, real C++ method */
int SlidingBox::CleanupResources()
{
    if (mMeshCollider.IsEnabled()) {
        mMeshCollider.Disable();
    }
    ((SharedFilePtr *)(data_ov016_02114e74))->Release();
    ((SharedFilePtr *)(data_ov016_02114e6c))->Release();
    return 1;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 2 -- _ZN10SlidingBox11UpdateModelEv, 0x021130a4, size 0x48 */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10SlidingBox11UpdateModelEv
void SlidingBox::UpdateModel()
{
    Matrix4x3_FromRotationXYZExt((void *)&mModel.mat4x3, mAngleX, mAngleY, mAngleZ);
    mModel.mat4x3.m[9] = mPosX >> 3;
    mModel.mat4x3.m[10] = mPosY >> 3;
    mModel.mat4x3.m[11] = mPosZ >> 3;
}

/* -------------------------------------------------------------------------- */
/* ROM ordinal 0 -- _ZN10SlidingBoxD1Ev, 0x02112ff8, size 0x4c */
/* -------------------------------------------------------------------------- */
// @symbol _ZN10SlidingBoxD1Ev
/* Real compiler-spelled complete destructor. The empty body is intentional:
 * mwccarm synthesises destruction of mWithMeshClsn followed by the inlined
 * dBgActor_c base teardown from SlidingBox's declared layout. */
SlidingBox::~SlidingBox()
{
}
