# VS16 - sixteen-player versus

Lane: the owner's order, "yes hit that 16 players hard and fast". The deepest
deliberate MOD in the project's history: the ROM's own wire protocol and data
widths, widened in the port's hosting and host layer, for online play only.
The DS-faithful local path stays default and byte-identical.

Coordinator: sm64ds-decomp-6c. Reviewer judges every phase; the coordinator
merges. Nothing is ever pushed from this lane.

Game/service worktree: `C:\tmp\vs16` branch `port/vs16` off cons
`952b3c135` (`port-mount-noseat-cluster`, "Merge port/lobby-v2union"). Real
copies, never junctions: `extracted/` 2308 files, `tools/mwccarm` 250 files,
`tools/bin/dsd.exe`. Asset catalog generated (2072 named NitroFS files).
Build helpers and their own TEMP `C:\tmp\vs16bld` live in `C:\tmp\vs16-out\`.
Evidence: `C:\tmp\vs16-out\`.

Launcher worktree: branch `lobby/vs16` off `master` `2b6b0a2` (the basic-labels
commits are in). The shared checkout at `Documents\SM64DSLauncher` is another
lane's live tree and is not touched.

---

## 2026-09-01 -- CHECKPOINT, PHASE 1: THE SWEEP

Every place the port, the ROM code it links, the service and the launcher
assume four players. Read from the tree at `952b3c135`, with file:line, not
estimated. The SEAT16 map (`port/tools/lobby/README.md`, "THE CEILINGS BETWEEN
4 AND 16") is the starting point; this sweep re-derives it and **overturns
three of its conclusions**, all in the direction of 16 being cheaper than the
map thought.

### The three findings that change the plan (none of them overturn its premise)

**FINDING 1 -- the score array is ALREADY sixteen-ready, on both sides.**
The map called `data_0209f310` a hard four-byte ceiling. It is not, in the port:

- The host already gives the band **32 bytes**, as a guarded contiguous pair --
  `signed char data_0209f310[1]` + `signed char data_0209f311[31]`
  (`port/hal/actor_classes_star.cpp:236-241`), placement-checked at run time by
  `port_vsstar_layout_check` and at link time by `gxband_guard.py`'s `vsstar`
  band. 32 bytes holds 16 scores with 16 to spare.
- The ROM's own summing reader is **already generic**: `src/NumVsStarsObtained.c`
  walks `data_0209f21c` (the player-count byte) entries from `f310`, not four.
- `HUD::RenderStarCount` (`src/_ZN3HUD15RenderStarCountEv.cpp:38`) draws
  `data_0209f310[data_0209f250]` -- **the local player's count only**. The
  in-match star HUD is not a four-column band at all.

**FINDING 2 -- the ROM's four-column star band is the RESULTS SCREEN, and it
does not bind.** Brief item 8, confirmed at source level. `4 - count` /
`data_ov075_0211c6e8[d]` lives in `src/func_ov075_0211621c.c:56-63`, which is
the VS **results row**, reached only from scene 7. `hal/scene_boot.cpp:3980`
registers scene **6** (`SCENE_VS_MENU`) and nothing else in ov075; the results
face list in `hal/scene_vs_menu.cpp:275` is explicitly **EMPTY**, with the
six-line row that would register scene 7 written out in a comment as
deliberately-not-taken. The port's match end is the port's own winner banner
(`hal/star_flow.cpp:1110-1145`), which computes the winner from latched scores
rather than from `func_ov075_021165b0`. **So the `d == -1` out-of-bounds at five
players is not on any path this port runs.** It stays a real reason not to
register scene 7, and that is now a documented constraint rather than a wall.

**FINDING 3 -- most of the ROM's per-player loops are bounded by
`data_0209f21c`, not by a literal 4.** Generic already, verified by reading each
one: `NumVsStarsObtained`, `NumRedCoins`, `NumCoins`, `ResetInput`,
`SetTouchScreenDelay`, `Actor::ClosestPlayer`, `Actor::ClosestNonVanishPlayer`,
`LoadEntranceObjects`, and `Stage::CheckInput`'s **main** loop. Even
`func_020308d0` (SetNumPlayers) is generic upward: its first loop is `i < n`.
The hard fours that remain in ROM code are concentrated in the ov075 results
scene (not linked into the match) and in a handful of clear/reset paths.

### THE ACTUAL WALL, stated precisely

`mPlayerNo` is two bits **on the unpack from the spawn-flag word only**, not in
storage. `src/_ZN6Player13InitResourcesEv.cpp:72`:

```c
a = *(unsigned int*)(c + 8);
*(u8*)(c + 0x6d8) = (a >> 6) & 3;
```

The destination is a **whole byte**. The flag word is what is two bits wide:
bits 0..2 character, 3..5 f1, 6..7 player number, 8..11 sublevel
(`hal/level_boot.cpp:4356` packs exactly that). So slot 5 cannot be *expressed
in the spawn flags*, but it can be *stored* and *indexed by* perfectly well.

The one consequence inside `InitResources` is at `:105`:

```c
Lac:
    if (*(u8*)(c + 0x6d8) == data_0209f250) { ...local player binding... }
```

In VS mode (`data_0209f2d8 == 1`) the rest of the function's `mPlayerNo` uses
are skipped -- the `data_02092128` rewrite block at `:79-91` is inside
`if (t == 0)` and `t` is `(data_0209f2d8 == 1)`. So the identity problem reduces
to two things and nothing else:

1. make the ROM's own local-player test at `:105` reach the right answer for
   each spawning slot, and
2. repair `*(u8*)(actor + 0x6d8)` to the true slot after `Actor::Spawn` returns.

Both are doable **entirely in the port's spawn caller**, with no `src/` edit.
That is the phase 2 design and it is written up below the table.

### THE SWEEP TABLE

Legend: **KNOB** = a number, nothing structural. **HOST** = the port's own
hosted storage, widen it. **WIRE** = on the wire, needs the version bump.
**ROM** = byte-matched `src/`, cannot move -- must be worked around.
**GENERIC** = already correct at 16, listed so nobody re-derives it.

#### A. Wire identity and the packet

| # | what | file:line | today | verdict |
|---|---|---|---|---|
| A1 | `kCommsMaxPlayers` | `port/hal/comms_seam.h:237` | `4` | KNOB, and it sizes everything below |
| A2 | `Packet.live` | `port/hal/comms_loopback.cpp:295` | `unsigned char`, 8 bits | **WIRE** - the one real wire break |
| A3 | `Packet.have` | `comms_loopback.cpp:296` | `unsigned int`, 32 bits | GENERIC - already holds 16 |
| A4 | `Packet.blocks[4][0x20]` | `comms_loopback.cpp:297` | 144 B packet | **WIRE** - 528 B at 16 |
| A5 | `kPacketBytes = 0x90` + `static_assert` | `comms_loopback.cpp:299,309` | pinned | WIRE, moves with A4 |
| A6 | `g_live` (host-side mask) | `comms_loopback.cpp:517` | `unsigned` (32-bit) | GENERIC - the *storage* is fine |
| A7 | `popcount4()` | `comms_loopback.cpp:777` | counts 4 bits | KNOB - 5 call sites |
| A8 | slot assignment bound | `comms_loopback.cpp:1281-1282` | `< kCommsMaxPlayers` | KNOB via A1 |
| A9 | per-slot UDP ports `base+k` | `comms_loopback.cpp:856-865, 2365-2366` | 4-port span | KNOB, **loopback/direct only**; over the relay each peer holds one socket (`:882`) |
| A10 | `g_aux`, `g_stage`, `g_latched`, `g_peer_addr`, `g_peer_known` | `comms_loopback.cpp:506,521,523,872,873` | `[kCommsMaxPlayers]` | KNOB via A1 |
| A11 | conductor's `SM64DS_VS_PLAYERS` clamp | `comms_conductor.cpp:901,910,1048` | `1..kCommsMaxPlayers` | KNOB via A1 |
| A12 | relay session seats | `port/tools/relay/relay.py` `MAX_CHILDREN` | knob, default 3 | **ALREADY CLIMBED** (SEAT16), proven at 15 |
| A13 | relay payload cap | `relay.py` `MAX_PAYLOAD` | 700 B | GENERIC - 528 fits, 172 spare |
| A14 | relay rate cap | `relay.py` `RATE_PPS` 120 | | GENERIC - parent sends ONE copy (`comms_loopback.cpp:918-925`), relay fans out |

**The wire version bump is A2 + A4 together.** They are the same break: a peer
that reads a 144-byte packet cannot read a 528-byte one, and an 8-bit live mask
cannot name slot 8. Old clients must be refused cleanly, not
half-understood -- designed below.

#### B. The Ctrl input band

| # | what | file:line | today | verdict |
|---|---|---|---|---|
| B1 | `data_0209f498[24]` (the whole-record view) | `port/hal/auto_bss.cpp:175` | `int[24]` = 96 B = `0x18 * 4` | HOST |
| B2 | `data_0209f4a2/4a4/4a6` (the split views) | `auto_bss.cpp:167-169` | `[0x18 * 4]` each | HOST |
| B3 | `Stage::CheckInput` main loop | `src/_ZN5Stage10CheckInputEv.cpp:80-83` | bounded by `data_0209f21c` | **GENERIC** |
| B4 | `Stage::CheckInput` clear branch | `src/_ZN5Stage10CheckInputEv.cpp:63` | hard `k < 4` | ROM - clears only 4 records; slots 4..15 keep stale input on that path |
| B5 | `ResetInput` | `src/ResetInput.c:21` | bounded by `data_0209f21c` | **GENERIC** |
| B6 | `data_0209f350[8]` (the per-player stylus-mode byte array) | `auto_bss.cpp:176` | 8 | HOST |
| B7 | `data_020a0e58[8]` (raw pad held/pressed pairs) | `auto_bss.cpp:275` | 8 ints = 4 players x 2 | HOST |
| B8 | `data_020a0de8` (TouchInfo) | `auto_bss.cpp:254` | 1-byte head of a placed band | HOST, needs its band re-measured |
| B9 | `data_020a0e40[8]` (local player index / live TouchInfo index) | `actor_vtables.cpp:522` | scalar in an 8-int cell | GENERIC |

**VS4P's finding re-derived and it HOLDS.** None of B1/B2 is
`__declspec(allocate(".dsstate..."))`, so none is address-pinned and widening
one shifts nothing. Confirmed by reading `auto_bss.cpp:160-176`: plain
definitions, no `#pragma section`, no placement. The ROM extent argument
(`f498..f4f7` is exactly `0x18*4` with `f4f8` the next symbol) is about the
**cartridge's** address space and does not bind the host, which is the whole
point of hosting. **So the Ctrl band is again "just sizes" on the host.**

#### C. ROM-wide per-player data the port hosts

| # | symbol | hosted at | width today | needed at 16 | verdict |
|---|---|---|---|---|---|
| C1 | `data_0209f310` scores | `actor_classes_star.cpp:236-241` | **32 B** (1+31, guarded pair) | 16 | **ALREADY WIDE** |
| C2 | `data_0209f394` per-slot `Player*` | `cxxname_bridge.cpp:317` | `int[8]` = 8 ptrs | 16 | HOST |
| C3 | `data_0209f21c` player count | `cxxname_bridge.cpp:316` | `int[8]` cell, read as `u8` | scalar | GENERIC |
| C4 | `data_0209fc5c` / `data_0209fc5d` ready bytes | `scene_vs_menu.cpp:245,247` | `[1]` + `[3]` grouped pair = 4 B | 16 | HOST - **and it is a placed, band-guarded pair** (`gxband_guard` 'ready' band), so widening it must keep contiguity |
| C5 | `data_0209fc64` slot->player map | `auto_bss.cpp:395` | `char[4]` | 16 | HOST |
| C6 | `data_0209fc50` players-in-match | `auto_bss.cpp:394` | `[4]` cell, scalar | scalar | GENERIC |
| C7 | `data_0209f358` coins | `actor_vtables.cpp:416` | `short[4]` | 16 | HOST |
| C8 | `data_0209f30c` | `auto_bss.cpp:358` | `[4]` | 16 | HOST |
| C9 | `data_02092128` per-player character | `actor_vtables.cpp:473` | `[0x40]` | 16 | **ALREADY WIDE** |
| C10 | `data_0209f250` local player index | `auto_bss.cpp:563` | `[4]` cell, scalar | scalar | GENERIC |
| C11 | entrance/level per-player records `data_0209f31c/f328/f334/f348/f338/f214/f258/f2e8/f25c/f2d0/f200/f254/f2fc/02092114/020a0d8c` | `level_boot.cpp:2784-2887` | `[4]` each | 16 | HOST - the bulk widening job |
| C12 | `data_0209f33c[8]` carried actor | `auto_bss.cpp:177` | 8 | 16 | HOST |
| C13 | `data_0209fc68[8]` | `auto_bss.cpp:186` | 8 | 16 | HOST |

**C4 and C1 are the two that are not free**, because they are placed
grouped-section pairs whose contiguity a guard checks. Widening them means
widening the guard's expectation in the same commit, not just the array.

#### D. Identity and spawn

| # | what | file:line | verdict |
|---|---|---|---|
| D1 | `mPlayerNo` unpack `(a >> 6) & 3` | `src/_ZN6Player13InitResourcesEv.cpp:72` | **ROM** - two bits in the flag word; the destination byte is full width |
| D2 | local-player binding test | `src/_ZN6Player13InitResourcesEv.cpp:105` | ROM - the one in-VS consumer of the truncated value |
| D3 | flag packing `(i << 6)` | `port/hal/level_boot.cpp:4356` | HOST - the port writes this |
| D4 | extra-player spawn bound `i < n && i < 4` | `port/hal/level_boot.cpp:4309` | HOST - hard 4 |
| D5 | extras placed 40 units apart, comment says `OVERLAP` | `level_boot.cpp:4315` | HOST - a stand-in, not spawn points |
| D6 | `LoadEntranceObjects` (the ROM's real seat loop) | `src/_Z19LoadEntranceObjects...c:56,82` | **GENERIC** - `while (i < data_0209f21c)`, gated on `data_0209fc5c[i]` |
| D7 | arena entrance record count | level data, read at `level_boot.cpp:2101` | **DATA** - 4 real records on arena 0; 16 need authoring |

#### E. Names, colours, HUD, marker

| # | what | file:line | verdict |
|---|---|---|---|
| E1 | `g_vs_names[4][17]` | `port/hal/star_flow.cpp:1028` | HOST |
| E2 | names reader: exactly 3 commas | `star_flow.cpp:1058` | HOST - the coordinated 16-field move |
| E3 | names reader loops + log | `star_flow.cpp:1036,1064,1069,1079,1082,1094` | HOST |
| E4 | `g_end_scores[4]` | `star_flow.cpp:934` | HOST |
| E5 | winner banner `%d - %d - %d - %d` | `star_flow.cpp:1125-1127,1138-1140` | HOST - the display question |
| E6 | match-end latch loops `i < 4` | `star_flow.cpp:1228,1235,1331,1333` | HOST |
| E7 | MATCH OVER marker `scores=%d,%d,%d,%d` | `star_flow.cpp:1338-1341` | HOST - and the launcher parses it |
| E8 | `VsColorSpec g_vsc[4]` | `port/hal/fs_mods.cpp:2132` | HOST |
| E9 | colours reader: exactly 3 commas, len cap 55 | `fs_mods.cpp:2162,2179` | HOST - moves with E2 |
| E10 | colours slot loops | `fs_mods.cpp:2192,2199,2207` | HOST |
| E11 | ROM results row `4 - count`, `data_ov075_0211c6e8[d]` | `src/func_ov075_0211621c.c:56-63` | **ROM, DOES NOT BIND** - scene 7 unregistered (Finding 2) |
| E12 | ROM winner calc, eight `< 4` loops | `src/func_ov075_021165b0.c` | ROM, does not bind - no results scene to receive it |
| E13 | VS lobby scene loops `i < 4` | `src/func_ov075_02114fa8.c:12` and friends | ROM - scene 6 IS registered; **open question** whether a lobby-started match ever enters it |

#### F. The lobby service and the launcher

| # | what | file:line | verdict |
|---|---|---|---|
| F1 | `DIAL_HARD_MAX` / `MAX_SEATS` / `GAME_MAX_PLAYERS` | `port/tools/lobby/app/server.py:137,167` | **ALREADY KNOBBED** to 16 by SEAT16 |
| F2 | `build_names` four fields | `server.py:1490-1500` | contract change |
| F3 | `build_colors` four fields | `server.py:1502-1524` | contract change, moves with F2 |
| F4 | `VERB_FIELDS` | `server.py` | the single contract authority - extend properly, do not hand-write a gate |
| F5 | launcher `plan.Players > 4` refusal | `LobbyLaunch.cs:76` | launcher |
| F6 | launcher names validator `fields.Length != 4` | `LobbyLaunch.cs:133` | launcher, moves with F2 |
| F7 | launcher colours validator `fields.Length != 4` | `LobbyLaunch.cs:173` | launcher, moves with F3 |
| F8 | launcher 4-port span allocator | `LobbyLaunch.cs:250-271` | launcher - **loopback/direct only** (see A9) |
| F9 | launcher marker parser, 4 scores | `LobbyLaunch.cs:366-368` | launcher, moves with E7 |

### THE PREMISE HOLDS -- proceeding to phase 2

Nothing in this sweep overturns the plan. Three things make it *cheaper* than
the map predicted (the scores are already wide, the results-screen OOB does not
bind, and most ROM loops are already generic), and one thing makes it
*sharper*: the identity problem is not "the ROM cannot store slot 5", it is
"the ROM's spawn-flag word cannot carry slot 5 and one ROM branch reads the
truncated value". That has a clean fix in the port's own spawn caller.

### The phase 2 design, in one place

1. **Wire.** `kCommsMaxPlayers` 4 -> 16; `Packet.live` `unsigned char` ->
   `uint16_t`; `blocks[16][0x20]`; `kPacketBytes` 0x90 -> 0x210. Bump the
   packet `version` byte and **refuse** a mismatched version rather than
   half-parse it. Old client meets wide session: refused with a named reason.
   The default path must produce byte-identical behaviour at 2P/4P.
2. **Identity.** Around each `Actor::Spawn` for slot `i`, the port sets
   `data_0209f250` so the ROM's own `:105` test reaches the right answer, then
   writes the true slot into `*(u8*)(actor + 0x6d8)` after the call. No `src/`
   edit. Every ROM reader that indexes by the identity byte then sees the true
   slot, and the host arrays are wide enough to be indexed.
3. **Hosted widths.** Table C, mechanically, with the two guarded bands
   (`vsstar`, `ready`) and their guards moved together.
4. **Spawns.** Replace the `i < 4` stand-in with deterministic ring placement
   around each real entrance record; document per arena.
5. **Names + colours.** The coordinated 16-field move at the new contract
   version, across `star_flow.cpp` / `fs_mods.cpp` / `server.py` `VERB_FIELDS`
   / the launcher, in one commit set.
6. **HUD.** Correct counts for N; display defaults to the simplest honest
   thing; capture 2-3 layout options for the owner and never judge them here.

Next: phase 2, starting with the wire.
